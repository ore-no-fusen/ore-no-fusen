# 計画書：付箋の整列（タグのかたまりの中を色で並べる）

作成日: 2026-06-17
最終更新: 2026-06-22
対象バージョン: v4.0.2 ベース
ブランチ: develop
状態: **設計確定（未決定なし。実装に進める）**

---

## 1 目的

散らばった付箋を、ボタン1発で **タグごとのかたまり**に整列させる。
さらにそのタグのかたまりの中を **色（黄→赤→青→白→黒）** で小分けして並べる。
「どのタグに何がどれだけあるか」を瞬時に把握できるようにする。

MVP「大事なことは貼っておけばいい」の延長線上にある機能。
貼った付箋が増えても、タグと色のかたまりで俯瞰できれば「そこに残る」価値が保たれる。

---

## 2 前提タスク：白・黒の付箋色を先に追加する（整列より先）

整列は「色の固定順」で列を分けるため、**白・黒が付箋色として存在しないと固定順に組み込めない**。
後から色を足すと整列のロジックも触り直す二度手間になるので、**白・黒の追加を先に実施する**。

- 追加先: `app/hooks/useStickyNoteContextMenu.ts` の色サブメニュー（現在 青/赤/黄の3色）
- 「一度に一つ変える」の原則に従い、**整列とは別タスク・別コミット**で行う
- 追加する色: 白 `#fafaf0`（クリーム寄り）／黒 `#cfd8dc`（青み淡グレー＝パステル調）
- 本文は黒字のまま。実物プレビューで黒字の読みやすさを確認のうえ確定した
- **メニューの並び順を整列順に統一（確定）**: 現状メニューは「🔵青→🌸赤→💛黄」で、整列順（黄→赤→青）と逆。
  この機会に **💛黄→🌸赤→🔵青→⬜白→⬛黒** に並べ替える。
  - 理由: 色を選ぶメニューと整列の色順がバラバラだと、ユーザーも開発者も混乱する。順番を1つに揃える。
- メニュー文言（i18n）: 白・黒のラベルを `lib/i18n.ts` に追加（ja/en 両方）。

**ブランチ運用（確定）**: この前提タスクは規模が小さいので **`develop` で直接**作業する。
（リリース手順書 `docs/010_RELEASE.md` の「アプリに影響する小さい変更」に該当）。
整列が白黒5色を前提にするため、**この前提タスクを先に develop に入れてから**整列本体に着手する。

---

## 3 並べ方（確定した仕様）

### 3.0 ★新整列ルール（全面作り直し・確定 26-06-22／これが最新の正）

> 旧 3.1〜（タグ=横レーン × 全色を整列）は実機で「重なりで下が見えない／3枚目がおかしい」と NG。
> ユーザーの新ルール（実機スクショの青付箋）に基づき**全面的に作り直す**。以降 3.1〜は旧仕様の参考。**配置の正は本節。**

**★本質（最重要・これを外すと全部間違える・26-06-23）**
- **本番にはレーンの線も「◆タグ名」見出しも無い**。タグの違いを表す手段は **付箋の「高さ（Y位置）」だけ**。
- ゆえに **別タグの付箋を同じ高さに置くと、ユーザーにはタグの区別がつかない（＝タグが混ざって見える）**。**タグごとに必ず高さ（縦位置）を変える**ことが、タグを見分ける唯一の方法＝**最優先**。
- 次の優先度＝**文字切れ問題**: 下へ行くほど付箋が縦に積み重なり、下の付箋のタイトルが上に隠れて切れる。これを防ぐのが「上げ目（空きへ詰める）」。
- **トレードオフ**: 「タグを見分ける高さの差」 vs 「文字切れを防ぐ詰め」。文字切れを気にして詰めすぎると、タグ間の高さの差分が消えてタグの区別がつかなくなる。**高さの差を最優先で確保した上で、その範囲内で詰める**。
- モック上でレーンの線・見出しを出すのは**禁止**（本番に無いものを出すと、同じ高さに別タグを置く判断ミスに気づけない）。

**振り分け（全ケース網羅・確定 26-06-23）**

整列対象の付箋は **2 種類だけ**に分ける。

- **カンバン本体（行＝タグ）** = **タグありで、色が 黄・赤・青** の付箋。
  - 行＝タグごと（`tags[0]`）。**レーン順（上下）＝黄の枚数→同数なら赤の枚数→同数なら青の枚数 の辞書順で多い方が上**（確定 26-06-23）。上から下へ。**1 つの行には 1 つのタグの付箋だけ**（別タグが同じ行に混ざらない）。
    - 由来＝処理フロー: 黄(もやっとした思いつき・Ctrl+Nで生む)→赤(黄の果てのTODO)→青(赤の果ての結果・済み)。青は時間が経つと血肉になりデスクトップ不要→タグフォルダ(保管庫)へ。**黄をどんどん処理していくゲーム**。黄を多く抱えるタグ＝処理すべきことが多い＝上に置く。
  - **レーン間の隙間 LANE_GAP=48px（確定 26-06-23・モックで確認）**: 上げ目をしないレーン（色が被る）は「前レーン最下端 + LANE_GAP」に置く。少し隙間を空けることでタグの境目が分かりやすい（高さ＝タグ区別の補強）。
  - **上げ目は「色が被らないレーンだけ」**に適用する（色が被るレーンは LANE_GAP のまま・上げない）。これを外すと隙間が詰まり LANE_GAP が効かない（モックのバグで判明）。
  - 列＝黄→赤→青の**固定3列**（全行で同じ色は同じ列X＝縦に揃う）。**列幅は詰める**（全データを走査し、各色列に必要な幅＝最大付箋幅＋同色階段のはみ出しぶん、だけ確保して隣とぶつからない最小間隔にする）。
  - 各行の Y は必ずその行の範囲内（別タグの付箋と同じ Y に来ない）。
- **タグなし置き場（旧称「その他」）** = 下記をすべてここに集める。
  - **タグなしの付箋**（色を問わず・黄赤青白黒その他すべて）
  - **色が 白・黒・5色外**の付箋（**タグが付いていてもここに入れる**）
  - 置き場所＝**一番上の行（1行目）の右端のさらに右**に、**白黒その他が互いに重ならないよう**右下階段でまとめる（stepX≈18 / stepY≥50）。多くて縦にあふれたら下にはみ出して良い。

> 呼称: モック・UI 上のラベルは「**タグなし**」に統一する（中身は「タグなし＋白黒＋5色外」だが、ユーザーから見える代表は「タグなし」）。

**配置の核（NG=「重なりで下が見えない」の修正）**
- 同色の重ね = **右下方向に階段**（古いが下・新しいが上＝**左上＝タイトル/書き出しが見える**）。
- stepY に最小値（≈50px）を保ち、**重なっても下のタイトルが必ず見える**ことを最優先（3枚目がおかしかった原因＝重なりで下が潰れていた）。
- **重ねてよいのは「同じタグ・同じ色」の階段だけ**。別タグの付箋同士は 1px でも重ねない（矩形衝突を全ペアで禁止）。
- **行の上げ目（文字切れ対策・ただし高さ差は残す・確定 26-06-23）**: 下へ行くほど縦に積もり文字切れが出る。空きへ詰めて緩和するが、**タグ間の高さの差は必ず残す**（高さ＝タグ区別の唯一手段なので）。
  - **条件**: **2行目以降の行**で、**その行の使う色集合が、直前の1行の使う色集合と1つも被らない**とき（同じ列に縦に並ぶ色が無い）。被る色が1つでもあれば上げない。1行目は対象外。
  - **食い込み量（確定 26-06-23・数値基準）= 「一つ上のレーンの黄色列の一番奥（最下）の付箋」の高さの半分まで**。つまり上のレーンの黄色最下付箋の**下半分にだけ**食い込んでよい。それ以上は上げない（上のレーンのタイトル部＝上半分は隠さず、タグの境目を残す）。
    - 上の行に黄色が無い場合は、上のレーンの最下付箋の高さの半分を基準にする（黄色が無ければ実在する付箋の高さで代用）。
  - 加えて、上げても**直前レーンのどの付箋とも矩形が重ならない**こと（同じ列に縦並びしない範囲）。上端は揃えたまま行ごと上げる（列ごとにギザギザにはしない）。
  - **累積させない**: 比較は常に「直前の1行」とだけ。連鎖して際限なく上がらない。
  - **行の単調性（必須）**: 各レーンの最上端付箋の top は、直前レーンの最上端付箋の top より必ず下（上げ目で単調性が壊れるなら上げ量を減らす）。これで「別タグが同じ高さに並ぶ」のを機械的に禁止する。
  - 例: 上の行＝黄・赤（青なし）、今の行＝青だけ → 色が被らない → 上のレーンの黄色最下付箋の高さの半分まで上げる。逆に 上の行＝黄・赤、今の行＝赤・青 → 赤が被る → 上げない。
- 全付箋を主ディスプレイ1枚に集約（4.4）。付箋サイズは変えない。

```
◆仕事(最多) [黄▤][赤 ][青▤]              [タグなし置き場]→ 黄▤ 青 白 …（1行目の右端の右に階段）
─────────────────────────────────       （タグなし付箋＋白黒＋5色外をここへ）
◆家庭      [黄 ][   ][青 ]   ← 黄・青が上の行と被る → 上げない（普通に下へ）
─────────────────────────────────
◆趣味      [   ][赤 ][   ]
─────────────────────────────────
◆旅行      [   ][   ][青▤]   ← 直前(趣味=赤)と色が被らない → 空き分だけ上に詰める
   ▤ = 同色複数 → 右下に階段（左上＝タイトルが見える・stepY≥50・同タグ同色のみ重ねる）
```

> 旧仕様との主な違い: ①カンバン本体は「**タグあり かつ 黄赤青**」のみ（旧は全付箋）。②**白・黒・5色外は、タグの有無に関わらず「タグなし置き場」へ**（旧は青の右の固定列／タグありは行内）。③タグなし置き場は**1行目の右端の右**にまとめる（旧はタグなしレーン）。④2行目以降で直前行と色が1つも被らない行は、**空いている分だけ**上に詰める（半分固定ではない・累積なし）。⑤列幅を詰める。⑥重ねるのは同タグ同色だけ。

---

### 3.1 基本レイアウト（カンバン方式：タグ=横レーン × 色=列・確定 26-06-21）

> ⚠️ 以下 3.1〜3.6 は**旧仕様（26-06-21）**。3.0 の新ルールで置き換え済み。実装は 3.0 に従う。

**カンバン方式**。タグを**横レーン（行）として上から下へ**積み、各レーンの中を**色（黄→赤→青→白→黒）の列**に分け、同じ色は**右下に階段状に重ねて**左上（タイトル・書き出し）が見えるようにする。

- **タグ = 横レーン（1タグ1行）**：付箋が多いタグが上、下へ積む（4.2）。タグが増えても**下に伸びるだけ＝横は破綻しない**。
- **色 = 列（列Xを全レーンで揃える＝カンバン・確定 26-06-21）**：黄→赤→青→白→黒 の各色に**固定の列X**を割り当て、全タグ（レーン）で同じ色は同じXに揃う（表のように縦に揃う）。
  - **黄・赤・青（主軸）**: その色が0枚のレーンも**そのX位置を空ける（詰めない）**＝抜けてたらそのエリアを抜かす。
  - **白・黒（おまけ・重要度低）**: 青列の右の固定Xから左寄せで出す（開始Xは全レーン揃う）。その他色は白黒の右。
  - 各固定列Xは「その色の列に入る付箋の最大幅（重なり込み）を全レーンで見た幅」を黄→赤→青→白→黒の順に足して算出。
- **同色 = 右下に階段重ね**：同じタグ・同じ色の付箋を、後の付箋ほど右下にずらして重ねる。前の付箋の**左上が残る**ので、上から順にタイトル・書き出しが読める。重ね量は実装の初期値で右≈18px / 下≈24px（調整可）。新しいのが手前・左上（積み順は 3.2.1）。
- **付箋のサイズは変えない**（fold もリサイズもしない）。
- 全付箋を**主ディスプレイ1枚**に集約（4.4）。
- **レーンの題（◆仕事 等の見出し）は将来課題（今は作らない・確定 26-06-21）**。題は付箋ではない別の表示物で、付箋以外のラベル窓/オーバーレイを描く新しい仕組みが要り複雑。まずは題なし＝付箋のカンバン配置だけで完成させる。タグごとに行（レーン）が分かれていれば位置で区別できる。題が必要になったら別途設計（出し方＝小窓 or オーバーレイ等を調査）。

```
◆仕事    [黄▤][赤▤][青 ][白▤][黒 ]   ← 1タグ=1レーン。色は横、同色は右下に階段重ね
─────────────────────────────────
◆家庭    [黄▤][赤 ][青▤][白 ][黒 ]   ← 次のタグは下のレーンへ
─────────────────────────────────
◆旅行    [黄 ][   ][青▤▤▤]              ← 黄赤青は列X共通。赤0枚でもそのエリアは空ける
─────────────────────────────────
◆タグなし [黄▤][赤 ][白 ][黒 ]          ← タグなしは一番下のレーン（4.3）
   ▤ = 同色が複数 → 右下に階段状に重なって左上が見える
```

### 3.1.1 折りたたみ付箋・タグ優先（確定 26-06-21）

- **折りたたみ（folded）付箋は折りたたんだ高さで計算する**。folded 付箋は実際の表示が高さ **40px**（タイトルバーのみ・`useWindowManager.ts` のデフォルト最小化高さ）。
  整列の高さ計算では、folded 付箋の height を **40** として扱う（幅は元のまま）。これで最小化した付箋が場所を取らず詰まる。
  - 実装注意: `extract_meta_from_content`（logic.rs）は folded フラグを返すが、`ArrangeNote` には folded が無い。**ArrangeNote に folded を足し**、部品②のメタ取得で渡す。arrange.rs の高さ参照を「folded なら 40、そうでなければ note.height」にする。
- **タグ付きを優先（大きく・きれいに）＝タグなしレーンは深く詰める**。
  タグ付きレーンは重なり浅め（見やすく大きく）に保ち、**タグなしレーンの重なりを深く詰めて**画面の余りをタグ付きに回す。
  実装: 縦の重なり動的計算（5.2.2）で、タグなしレーンに割ける高さを小さめに（例: タグ付き優先で配分）、タグ付きレーンには余裕を多めに配分する。係数は実装調整。タグなしは今も最後のレーン（4.3）。

1. 全付箋から **タグ一覧**を作る（各付箋は `tags[0]` のレーンに振り分け・4.1）。
2. タグ（レーン）順にループ（多い順・4.2）。各レーンで:
   - 黄→赤→青→白→黒 の固定順で色の列を作る（空の色は詰める・3.3）。
   - 各色列の中で、同色を右下に階段重ね（重ね量 右≈18/下≈24・積み順 3.2.1）。
   - レーンの高さ = そのレーンで一番深く重なった色列の下端。
3. **次のタグは下のレーンへ**（前レーンの最大下端 + レーン間マージン）。これをタグ数ぶんくり返す。
4. 縦に伸びて画面下を超える場合の扱いは 5.2.2（上端は画面内・はみ出し許容）。

> ある色が0枚の列は **詰める（左に寄せる）**。列幅を無駄にしない。
> 旧レイアウト（タグを横の大ブロックとして左右に並べる）は、タグ数が増えると横破綻するため**カンバン方式に変更**（26-06-21・実機モックで確定）。

### 3.2.1 縦の積み順（同じタグ・同じ色の中・確定 26-06-20）

同じタグ・同じ色の付箋を縦に積むとき、**作成日（seq）順で新しいものが上**。

- 順序キー: ファイル名 `{seq}_{date}_{context}.md` の **seq（連番＝作った順）**。seq が大きいほど新しい。
- 並び: **seq 降順（新しい＝上、古い＝下）**。
- 採用理由: 更新日 `updated` は整列で変えない設計（5.3）で並びが揺れにくいが、ユーザーは「作った順・新しいのが上」を選択。seq は作成順で固定なので安定（再整列しても同じ並び）。
- **実装方針（arrange.rs は変えない）**: arrange.rs は入力 `notes` の順序を保って各色列に積む（並べ替えしない）。
  よって **lib.rs 側（部品②のメタ取得）で notes を seq 降順にソートしてから** `calculate_arrange_by_tag_positions` に渡せば、各列が新しい順に積まれる。二重ソートなし。
  - seq は `ArrangeNote` に無いので、ソート用に各付箋の seq を別途取得する（ファイル名 or frontmatter の seq）。
    既存の `parse_filename`（logic.rs）が `(seq, created, context)` を返すので流用できる。
- **変更容易性（確定 26-06-20・方針A）**: 並び順は後で変える可能性がある。今は「seq 降順（新しいのが上）」固定だが、
  **並び順を決めるソート処理を lib.rs の1か所に集約**しておく（比較関数 or ソートキー1か所）。
  将来「更新日順」「古いのが上」等に変えたくなったら、そこを1行直すだけで切り替えられるようにする。
  実行時に切り替えるUI（設定画面）は今は作らない。必要になったらこの1か所を土台に設定画面（方針B）へ発展させる。

### 3.3 色の固定順

実装の色定義（`app/hooks/useStickyNoteContextMenu.ts` のコンテキストメニュー）に合わせる。

| 順 | 表示名 | 色コード | 備考 |
|----|-------|---------|------|
| 1 | 黄 | `#f7e9b0` | デフォルト付箋色 |
| 2 | 赤（ピンク） | `#ffcdd2` | メニュー上は「ピンク 🌸」 |
| 3 | 青 | `#80d8ff` | |
| 4 | 白 | `#fafaf0` | クリーム寄りの白。**前提タスクで追加**（第0章） |
| 5 | 黒 | `#cfd8dc` | 青み淡グレー（パステル調の「黒」）。**前提タスクで追加**（第2章） |

- 黄→赤→青→白→黒 の順で列を作る。
- **5色以外の付箋（確定）**: 黒のさらに右に「その他」列を作ってまとめる。
  - 補足: アプリ上は色変更がこの5色の固定選択のみで（`useStickyNoteContextMenu.ts`、自由入力なし）、
    通常は5色以外の付箋は発生しない。frontmatter を手で書き換えた等で5色外の値が来た場合の受け皿。
  - その他列の中の並びは色コード順で安定化する（実装時）。

### 3.4 あふれた時（トランプ風の重ね）

1つの色の付箋が多くて画面の下端を超えたら、超えた分を **少しずつ下にずらして重ねる**（トランプのカードのように）。

- 同じ色は必ず1列を保つ（次の列に折り返さない）
- 重ね send の量（例: 40px ずつ）は実装時に調整

```
│ ┌──────┐ │  下端まではフルの高さで積む
│ │ 黄 A │ │
│ │┌─────┴┐│  下端を超えたら
│ ││黄 B  ││  少しずつずらして重ねる
│ │└┌─────┴┐
│ │ │黄 C  │  ← トランプ風
│ └─└──────┘
```

### 3.5 起動方法（UI）

整列はアプリ全体に関わる操作なので、特定の付箋ウィンドウのボタンではなく、
**タスクトレイのメニュー**から起動する。

- 追加先: `src-tauri/src/tray.rs` の既存メニュー（新規メモ／検索／全部隠す…が並ぶ場所）
- メニュー項目（例）: 「タグで整列する (Arrange by Tag)」を追加
- 押した瞬間に全付箋が並ぶ。元の自由配置は上書きされる（整列 ON/OFF トグルではない）
- 直後に **「整列前に戻す (Undo Arrange)」** を出せるようにする（取り消し・7.x 参照）
- 既存実装に倣う: `MenuItem::with_id` で項目追加 → `on_menu_event` の `match` に分岐追加。
  新しい仕組みは不要（トレイメニューは既にある）。
- ショートカット併設は任意（既存の `Ctrl+Shift+H` 等と衝突しないキーにする）

### 3.6 配置パラメータ（実装時の初期値・調整可）

| 項目 | 初期値 | 説明 |
|------|-------|------|
| 開始位置 | `x=40, y=40` | 画面左上の余白 |
| 色の列の横送り | その列で一番広い付箋の幅 + 20px | 列が重ならない幅 |
| タグ間の追加の余白 | 40px | タグのかたまりの境目を分かりやすくする |
| 行の縦送り（下端まで） | 各付箋の高さ + 20px | フルで積む |
| 行の縦送り（あふれ後） | 40px ずつ | トランプ風の重ね |

---

## 4 複数タグ・並び順・出力先（確定）

### 4.1 複数タグ
- 複数タグが付いた付箋は、**最初のタグ（`tags[0]`）のかたまりにだけ置く**。
- 付箋＝1ウィンドウなので、同じ付箋を複数のタグに重複表示することはできない（物理的に不可）。
- そのため、2個目以降のタグのかたまりには、その付箋は現れない。

### 4.2 タグのかたまりを左から並べる順
- **付箋が多いタグ順**（左ほど多い）。関心ごとの大きさ順に並ぶ。
- 付箋を足すと並びが変わりうるが、「関心ごとの量で俯瞰する」目的に合う。
- 同数のタグが並んだときの順序（タイブレーク）は実装時に決める（例: タグ名順で安定化）。
- **枚数の数え方（確定）**: 「そのかたまりに**実際に置かれる**付箋数」で数える
  ＝各付箋を `tags[0]` のタグにだけ計上する。
  - 理由: 4.1 で付箋は `tags[0]` のかたまりにしか置かれない。もし全タグで二重に数えると、
    順位を決める枚数と、実際に並ぶ列の長さがズレて、スカスカの列が左に来るなど不自然になる。
  - 注意: 既存の `get_all_unique_tags`（`logic.rs`）は全タグを集める＋名前順ソート。
    整列のタグ順はこれをそのまま使えないので、**`tags[0]` 基準で集計し直す**必要がある。

### 4.3 タグなしの付箋
- タグが1つも付いていない付箋は、**全タグのかたまりの右端に「タグなし」ブロック**としてまとめる。
- その中も色の固定順（黄→赤→青→白→黒）で並べる。

### 4.4 どの画面（モニター）に出すか（`tag → color`・全付箋を1モニターに集約）

**確定（26-06-20 変更）**: 全付箋を**メインモニター1枚に集めて**整列する。グルーピングは **タグ → 色** のみ（モニターでは分けない）。

- 整列を押すと、どのモニターにあった付箋も**メインモニターの作業領域に集約**され、タグのかたまり（4.2 の順）× 色の固定順（3.3）で並ぶ。
- **タグが複数モニターに分裂しない**＝「タグごとに俯瞰する」目的を最優先。
- 配置の基準は**メインモニターの作業領域（workArea＝タスクバー等を除いた範囲）**。1枚分。
- **モニター判定は不要**: 旧仕様（`monitor → tag → color`・付箋を元モニター内で整列）は破棄。
  各付箋の所属モニター判定（`monitorFromPoint` / Rust `monitor_from_point`）は**使わない**。
  → 未検証の Rust monitor 判定 API が不要になり、実装が単純化（旧・部品⑥は削除。5.8 参照）。
- 集約先の「メインモニター」＝**Windows の主ディスプレイ**（ユーザー確定）。Tauri の `primary_monitor()` で取得し、サイズ/位置を scale_factor で論理座標に正規化する。

> 変更理由: 旧仕様は「付箋を飛ばさない」優先でモニターごとに整列したが、ユーザーの使い方では
> 「同じタグが画面ごとにバラける」方が困る。**全部を1画面に集めて俯瞰できる**ことを優先する（ユーザー確定）。

> 補足（確認済み）: タグと色を結びつける仕組みはコード上に存在しない
> （色変更=`backgroundColor` を書くだけ、タグ操作=`tags` を付け外すだけ。両者は独立）。
> よって「タグのかたまりの中を色で分ける」2.1 の構造で、両方の情報をそのまま使う。

---

## 5 実装方針（状態管理の鉄則に沿う）

**ブランチ運用（確定）**: 整列本体は新機能で規模が中〜大のため、**専用ブランチで開発し `develop` へ PR**する
（リリース手順書 `docs/010_RELEASE.md` の「アプリに影響する大きい変更」に該当）。
前提タスク（第2章）が `develop` に入った後に着手する。

唯一の状態は Rust `AppState`。整列の位置計算は **Rust 側で行い**、各ウィンドウへ反映する。
フロントは state を持たず、ボタン/ショートカットで invoke するだけ。

| 手順 | 場所 | 内容 |
|------|------|------|
| 1 | Rust | `AppState` の全付箋から `tags`・`backgroundColor`・`window`(w,h) を取得 |
| 2 | Rust | 全付箋のタグ一覧を作る。各付箋は `tags[0]` のかたまりに振り分ける（タグなしは右端ブロック 4.3） |
| 3 | Rust | タグごとに（タグ順は 4.2）、中を色の固定順で列に分け、各付箋の新しい x,y を計算 |
| 4 | Rust | 各付箋ウィンドウの位置を反映し、frontmatter の `window` も更新 |
| 5 | フロント | ボタン/ショートカットで整列コマンドを invoke するだけ |

**参照する既存コード**
- 位置: frontmatter `window: { x, y, width, height }`（`src-tauri/src/logic.rs` の `re_window`）
- 色・タグ: `extract_meta_from_content`（`src-tauri/src/logic.rs`）が `(x,y,w,h,color,aot,tags,folded)` を返す
- 色定義: `app/hooks/useStickyNoteContextMenu.ts`（黄/赤/青。白/黒は前提タスクで追加）
- トレイUI: `src-tauri/src/tray.rs`
- 位置反映の注意: `fusen_show_at_position`（`src-tauri/src/lib.rs`）は表示/フォーカス用途で副作用が強い。
  整列にはそのまま使わず、**位置変更のみ・フォーカス変更なし**の専用経路を用意する。

### 5.1 座標系（重要・Codex 指摘）
- 既存の `app/api/window.ts` は OS の物理座標を scaleFactor で割り、frontmatter には**論理座標**で保存している。
- 一方 `fusen_show_at_position` は**物理座標**を受ける。座標系が混在しているので明示する。
- 方針: **計算・AppState・frontmatter は論理座標で統一**。OS へ移動する瞬間だけ物理座標へ変換する。
  monitor / workArea のサイズも同じ座標系（論理）に正規化してから計算する。

### 5.2 はみ出し・あふれの扱い（Codex 指摘）
- **横あふれ**: タグ数や色列が多く workArea 幅を超える場合、超えたタグブロックを次の段（下）へ折り返す。
- **巨大な付箋**（`width > workArea幅` / `height > workArea高さ`）: サイズは変えず、**左上だけ workArea 内に入れる**（はみ出しは許容）。

#### 5.2.1 カンバン方式での横の収め方（旧・横折り返しは廃止・確定 26-06-21）

**経緯**: 旧レイアウト（タグを横の大ブロックとして左右に並べる）は、付箋が workArea 幅を超え横一列に伸びて画面外に飛ぶ不具合があり、横あふれ折り返し（タグブロックを次段へ）を実装した（コミット 53bdc20）。しかしタグが4つあると 1タグ≈900px で2タグ目以降が画面外になり破綻。**カンバン方式（タグ=縦レーン）に変更して根本解決**（実機モックで確定）。

- **カンバンでは横は破綻しない**：横方向は「5色の列＋同色の右下重ね」だけで、1レーンの幅は**色数（最大5列）＋重なり分**に収まる。タグが何個あっても**横幅は増えない**（タグは下のレーンへ伸びる）。
- よって**旧・横あふれ折り返し（タグブロックを次段へ）は不要＝廃止**。arrange.rs の改段ロジック（`tag_block_width` での横折り返し）はカンバン配置への作り直しで置き換える。
- 1レーン内で5色＋重なりが workArea 幅を超える稀なケース（巨大付箋が横に重なる等）は、右方向のはみ出しは許容（サイズ不変）。レーンの左端は必ず画面内。

#### 5.2.2 重なり量を動的計算して必ず画面内に収める（確定 26-06-21）

**方針: 重なり量（縦）を固定にせず、全付箋が workArea の高さに収まるよう逆算して決める。下限なし・必ず全付箋の上端が画面内。**
タグが多ければ重なり深く（step小）、少なければ浅く（step大＝見やすい）。

**2段構えのクランプ（縦を詰める。横の右ずれは固定でよい＝ユーザー確定）:**

**重要な見直し（26-06-22）**: 当初「下限なし＝必ず画面内」にしたが、実機で stepY が 0 に潰れ、同色付箋が完全に縦重なりして**下のタイトルが全く見えない**不具合が出た。
→ **stepY に最小値 `STACK_STEP_Y_MIN`（タイトルが見える高さ ≈50px）を設ける**。優先を「必ず画面内」より「**重なっても下のタイトルが見える**」に変更。

1. **同色の縦 step を動的計算（最小値あり）**（レーン内）:
   - `stepY = clamp((lane_h - effective_height)/max(nmax-1,1), STACK_STEP_Y_MIN, NOTE_H)`。
   - **下限 = STACK_STEP_Y_MIN（≈50px・タイトルが見える）。stepY=0 で潰すのは禁止。**
   - 枚数が多くて下限でも収まらないときは、下にはみ出すのを許容（各付箋の上端は画面内に保つ）。
2. **レーンの開始 y も動的計算 / レーン自体も重ねる（最小値あり）**:
   - レーン高の合計が workArea 高に収まるなら普通に縦に積む（LANE_GAP）。
   - 収まらない場合はレーン同士も上下で重ねるが、**レーン step にも最小値**（前レーンのタイトルが見える高さ）を設け、潰さない。

- 横（同色の右ずれ stepX）は固定（≈18px）。横ははみ出さない（5.2.1）。
- **優先順位（確定 26-06-22）**: ①各付箋の上端が画面内 → ②重なっても下のタイトルが見える（stepY ≥ 最小値）→ ③なるべく画面内に収める。完全に全部画面内へ収めることは保証しない（多すぎれば下ははみ出す）。
- 単体テスト: 「同色が複数でも stepY ≥ STACK_STEP_Y_MIN（縦に必ずズレる＝下のタイトルが見える）」を検証。

### 5.3 反映順序と途中失敗（Codex 指摘）
- 順序は「新配置を計算 → 各ウィンドウを移動 → **成功した分だけ** AppState と frontmatter を更新」。
- 途中で移動に失敗した付箋は、その付箋だけ元のまま据え置く（全体を巻き戻さない）。
- 位置だけの更新では frontmatter の **`updated` を変えない**（整列は内容変更ではないため。履歴・同期の意味を濁さない）。

### 5.4 一括移動のパフォーマンス・ちらつき（Codex 指摘）
- 逐次 `await set_position` は遅く、各ウィンドウを前面化するとちらつく。
- Rust 側で **フォーカスを奪わない一括移動**にする。必要なら小さなバッチで実行する。
- サイズ変更なし・位置変更のみ・フォーカス変更なしを前提にする。

### 5.5 実装の段分け（混在防止・1つずつ検証）

第1段は完了（`arrange.rs` の位置計算・純粋関数）。第2段を3つに分ける。

| 段 | 内容 | 検証 |
|----|------|------|
| 第1段 | 位置計算ロジック `arrange.rs`（純粋関数） | ✅ cargo test 7件 pass（feature/arrange-clean） |
| 2-a | 整列コマンド `fusen_arrange_by_tag`：付箋列挙→メタ取得→seqソート→arrange計算→位置反映→frontmatter更新 | ✅ 実装完了（部品①〜⑤）。実機で配置確認済み（横/縦あふれの課題が判明） |
| 2-d | カンバン配置（タグ=縦レーン×色=列×同色=右下重ね）＋ folded=40px ＋ タグなしレーン深く詰める | ✅ 実装完了（arrange.rs 刷新・lib.rs で folded 伝搬・cargo test 9件 pass）。実機確認待ち |
| 2-e | 黄赤青の列Xを全レーンで揃える（白黒は青の右に左詰め）＋ レーン間（行間）を広げる | ✅ 実装完了（arrange.rs：黄赤青を固定列X・全レーン共通・0枚でも空ける／白黒は青の右の共通Xから左詰め／LANE_GAP 40→80／テスト11件 pass）。実機確認待ち |
| 2-b | トレイメニューに「タグで整列」追加 | ✅ 実装完了（tray.rs にメニュー項目＋on_menu_event 分岐、lib.rs に run_fusen_arrange_by_tag 切り出し・cargo build/test 緑）。仮ショートカット Ctrl+Shift+L も併存。実機でメニュー表示確認待ち |
| 2-f | 同色の縦ズレ stepY に最小値（潰れて下が見えない不具合の修正） | ✅ 実装完了。当初28pxは付箋(高さ300超)に対し小さすぎ下が隠れたため **STACK_STEP_Y_MIN/LANE_STEP_Y_MIN=50** に。folded=40<50 の clamp panic を上限補正で回避。同色は必ず縦≥50pxズレ下のタイトルが見える。テスト12件 pass。実機確認 NG（重なりで下が見えない／3枚目おかしい）→ 2-g で新ルールに全面作り直し |
| 2-g | **★新ルール全面作り直し（3.0）**: 振り分け＝タグありのみカンバン／タグなしは色問わず青の右／白黒は行内の青の右／黄赤青は列詰め／2行目以降の黄赤無し行は青を上げ目／右下階段で左上が見える | 次。Codex 実装 → cargo test → 実機 |
| 2-c | undo（整列前スナップショット1段＋「整列前に戻す」） | 2-g 後・実機 |

> 旧・部品⑥（横あふれ折り返し・コミット 53bdc20）は 2-d のカンバン化で置き換え（横折り返しは廃止・5.2.1）。

### 5.6 2-a 実装指示（Codex 再開時にそのまま渡す）

**作るもの**: Rust コマンド `fusen_arrange_by_tag`（Rust 主導・状態の真実は AppState）。

**手順**:
1. 付箋ウィンドウを列挙: `app.webview_windows()` の `(label, window)` で `label != "main"` が付箋。
2. 各付箋について:
   - label から path を引く（既存の `get_window_label(path)` の逆引き、または AppState.notes と突き合わせ）。
   - 現在位置を取得: `window.outer_position()`（物理）を `window.scale_factor()` で割り**論理座標**にする
     （JS の `getWindowGeometry`＝`app/api/window.ts` と同じ換算）。※モニター判定には使わない。位置反映の参考用。
   - **モニター判定はしない**（4.4 確定＝全付箋をメインモニターに集約）。
3. **メインモニター1枚の作業領域(workArea)** を `primary_monitor()` から取得し scale_factor で論理座標に正規化 →
   全付箋を `ArrangeNote` に変換 →
   `arrange::calculate_arrange_by_tag_positions(notes, work_area)` で新座標（論理）を得る。
4. 各付箋を新座標へ移動: フォーカスを奪わない位置変更のみ（5.4）。論理→OS は内部で変換。
   `fusen_show_at_position` は副作用が強いので使わない（5.1/位置反映の注意）。
5. 移動に成功した付箋だけ frontmatter の `window` を更新（`updated` は変えない・5.3）。
   既存の window 正規表現フォーマット `window: { x, y, width, height }` を維持。

**制約**:
- トレイUI（2-b）・undo（2-c）は今回やらない。
- arrange.rs は完成済み・変更しない。`NoteMeta` の構造も変えない。
- StickyNote.tsx 等フロントの表示ロジックは触らない（編集高速化は別タスク）。

### 5.7 既存コードの接続点（2026-06-20 下調べ済み・Codex に渡す）

実装前に確認した既存コードの位置と使い方。Codex 依頼時はこれを添えれば再調査不要。

| 用途 | 既存コード | 使い方 |
|------|-----------|--------|
| 付箋ウィンドウ列挙 | `app.webview_windows()` を回す（実例 lib.rs:1241 `fusen_is_sticky_note_focused`） | `for (label, window) in app.webview_windows()` で `label != "main"` が付箋 |
| path→label | `get_window_label(path)`（lib.rs:943） | path から label を作る |
| **label→path（逆引き）** | 専用関数は無い | `app_state.notes`（各 note に path あり・lib.rs:845/851）の各 path を `get_window_label` に通して一致する label を探す |
| メタ抽出 | `extract_meta_from_content(content)`（logic.rs:154） | 返り値 `(x,y,w,h,color,aot,tags,folded)`。tags/color/w/h をここから取る |
| 現在位置→論理座標 | `window.outer_position()`（物理）÷ `window.scale_factor()` | JS の `getWindowGeometry`（app/api/window.ts）と同じ換算。**Rust 側に既存の換算ヘルパは無い**ので、この割り算を Codex に書かせる |
| 位置反映 | `window.set_position(LogicalPosition)` 等 | フォーカスを奪わない移動。`fusen_show_at_position`（lib.rs:1453）は副作用強で使わない |
| handler 登録 | `generate_handler!`（lib.rs:3140） | `fusen_arrange_by_tag` をここに追加 |
| frontmatter window 更新 | `re_window`（logic.rs） | `window: { x, y, width, height }` フォーマットを維持・`updated` は変えない |

### 5.8 部品分割（1依頼=1部品・都度コミット／Codex トークン切れ事故対策）

2-a を以下の粒度で分け、1部品ずつ Codex に依頼 → cargo build/test 緑 → 即コミット。
大ファイル lib.rs は**全面書き換え禁止・追記/最小差分のみ**。各依頼前に Codex 残量を確認する。

| 部品 | 内容 | 検証 |
|------|------|------|
| ① 骨組み | `fusen_arrange_by_tag` の空実装＋handler 登録。付箋を列挙してログ出すだけ | cargo build |
| ② メタ取得 | 各付箋の path（label逆引き）・tags・color・w・h・現在位置（論理）を集める | cargo build |
| ③ 計算呼び出し | メインモニターの workArea（primary_monitor を論理正規化）で `calculate_arrange_by_tag_positions` を呼び新座標を得る | cargo build/test |
| ④ 位置反映 | フォーカスを奪わず移動。途中失敗は据え置き | 実機（仮トリガ） |
| ⑤ frontmatter更新 | 成功分だけ window 更新・updated は変えない | 実機 |

> 旧・部品⑥（マルチモニター用 monitor 判定）は **4.4 の確定（全付箋を1モニター集約）により不要となり削除**。
> 2-a は ③④⑤ で完成する。

---

## 6 関連する作業ルール

- **位置更新でデータを壊さない**：本文・1行目・タグを上書きしない。`window` だけ更新する。
- **テストとセット**：位置計算ロジック（色の並び・列割り当て・あふれ時の重ね）の単体テストを Rust 側に追加する。
- **設計書を現実と一致させる**：実装後、本計画書の「未決定」を消し、確定仕様に書き換える。

---

## 7 整列の取り消し（undo・確定）

ボタン1発で自由配置を上書きするので、**直前の1回だけ「整列前に戻す」**を入れる（1段undo）。

### 7.1 仕組み（整列前スナップショットを1段持つ）
- 整列する**直前に、全付箋の現在位置を1セット記録**する（スナップショット）。
- 記録先は Rust `AppState`（状態の真実は AppState の鉄則どおり）。**メモリのみ・ファイルに書かない**。
- 中身は `[(付箋のパス, 元の window=x/y/width/height), ...]` のリスト。
- スナップショットは**常に1枚だけ**。再度整列したら古いものを上書きする
  ＝戻せるのは「直前の整列前」だけ（複数段には**しない**。要望が出たら拡張）。
- アプリを閉じると消える（＝再起動後は戻せない）。これは許容。

### 7.2 3つの操作
| 操作 | やること |
|------|---------|
| 整列する | ①現在の全位置をスナップショットに保存 → ②計算して並べる |
| 戻す | スナップショットの位置を全付箋へ書き戻す → スナップショットを空にする |
| 戻せるか | スナップショットが空なら「整列前に戻す」を出さない／押せなくする |

- 「戻す」の位置書き戻しは、整列の位置反映（5.3）と**同じ経路を使い回す**（書き込む座標が違うだけ）。

### 7.3 スナップショット後に付箋が増減した場合
- **付箋のパスで照合**し、今あるものだけ戻す。
- 記録後に**消えた付箋**: 戻しようがないので無視。
- 記録後に**増えた付箋**: スナップショットに無いので動かさない（そのまま）。

> 補足: 7 以外の論点はすべて確定済み。5色以外の付箋（黒の右に「その他」列）は 3.3 を参照。

---

## 8 改版履歴

| No | 日付 | 変更内容 |
|----|------|----------|
| 1 | 26-06-17 | 初版。並べ方（タグのかたまりの中を色=列で分ける入れ子構造・トランプ風の重ね・ボタン1発）を確定。複数タグは `tags[0]` のかたまりにだけ置くと確定。タグと色は独立（結びつきなし）と確認。タグ順・出力モニター・固定色外の扱いを未決定論点として記載。 |
| 2 | 26-06-17 | タグ順=付箋が多い順、出力先=元あった場所に近い方、で確定。色の固定順に白・黒を追加（黄→赤→青→白→黒）し、白黒の付箋色追加を前提タスク（第2章）として明記。 |
| 3 | 26-06-17 | 実物プレビューで確認し、白=#fafaf0、黒=#cfd8dc（青み淡グレー）に確定。 |
| 4 | 26-06-17 | Codex レビュー前の整備：図を5色に更新、章順を通し番号に整理、タグなし付箋の扱い（4.3）と参照番号のズレを修正。 |
| 5 | 26-06-17 | Codex レビュー（`arrange-by-tag-review-codex.md`）を反映：UI=タスクトレイ起動（3.5）、出力先を `monitor→tag→color`（4.4）、座標系/あふれ/反映順序/一括移動の注意（5.1〜5.4）、7-1 は案A推奨、整列の取り消し（7-2）を追加。 |
| 6 | 26-06-17 | 未確認3点をコードで確認し確定：タグ枚数は `tags[0]` 基準で数える（4.2）、モニター判定は既存 `monitorFromPoint` を流用（4.4）、色メニューを整列順「黄→赤→青→白→黒」に統一（第2章）。 |
| 7 | 26-06-17 | アプリ上は色変更が5色の固定選択のみで自由色は作れないと確認。5色以外（手書き等の異常値）は黒の右に「その他」列で受けると確定（3.3）。論点 7-1 を「整列の取り消し（undo）」に繰り上げ。 |
| 8 | 26-06-17 | undo は1段（直前1回だけ）で確定。整列前スナップショットを AppState にメモリで1枚持つ仕組み・3操作・付箋増減時の照合を 7 に明記。全論点が確定し、状態を「設計確定」に更新。 |
| 9 | 26-06-17 | ブランチ運用を確定：前提タスク（白黒追加＋メニュー順統一）は小さいので develop 直接、整列本体は専用ブランチで develop へ PR。前提を先に入れてから整列に着手する順序を明記。 |
| 10 | 26-06-19 | 第1段（arrange.rs 位置計算・純粋関数）完了・レビュー合格（cargo test 7件）。ブランチを feature/arrange-clean に整理（旧 feature/arrange-by-tag の混在を排除）。第2段を 2-a/2-b/2-c に分割（5.5）し、2-a の実装指示（5.6）を Codex 再開用に明記。 |
| 11 | 26-06-20 | 2-a の既存コード接続点を下調べ（5.7）・部品分割（5.8）を追記。Codex トークン切れ事故（lib.rs 全面書き換え中断で src 17ファイル破損→checkout で復旧）を受け、1依頼=1部品＋都度コミット＋大ファイル追記のみの運用に確定。実装は Codex 残量回復後に着手。部品①骨組み・②メタ取得を実装＆コミット。 |
| 12 | 26-06-20 | **出力先を変更**: 旧 `monitor→tag→color`（付箋を元モニター内で整列）を破棄し、**全付箋をメインモニター1枚に集約**して `tag→color` で整列（4.4）。理由＝同じタグが画面ごとに分裂すると困る・1画面で俯瞰したい（ユーザー確定）。モニター判定 API が不要になり旧・部品⑥を削除、2-a は ③④⑤ で完成（5.6/5.8）。 |
| 13 | 26-06-20 | 縦の積み順を seq 降順（新しいのが上）で確定（3.2.1）。**2-a 完成**＝部品①骨組み/②メタ取得/③計算/seqソート/④位置反映＋仮トリガCtrlShiftL/⑤frontmatter更新を順に実装・各 cargo build/test 緑でコミット。並行開発のため worktree 構成へ移行（メイン=develop固定、整列=ore-no-fusen-arrange、endroll=別worktree）。**残＝実機確認**（Ctrl+Shift+L で主ディスプレイにタグ×色集約を確認）→ その後 2-b（トレイUI）・2-c（undo）。 |
| 14 | 26-06-21 | **レイアウトをカンバン方式に確定**（3.1）。実機確認で旧（タグ横並び）は横あふれ→折り返し実装（53bdc20）→それでも4タグで横破綻＆縦はみ出しと判明。HTMLモックで擦り合わせ、**タグ=横レーン（縦積み）× 色=列 × 同色=右下に階段重ね（左上=タイトル/書き出しが見える）**に決定。横折り返しは廃止（5.2.1）、縦はレーン積み＋上端クランプ（5.2.2）。同色重ねは新しいのが手前・左上。レーンの題（◆タグ名）は付箋外の表示物で複雑なため将来課題（題なしで先に作る）。次=2-d で arrange.rs をカンバン配置に作り直す。 |
| 15 | 26-06-22 | **新整列ルールに全面作り直し（3.0）**。実機（2-f）が NG＝「重なりで下が見えない／3枚目がおかしい」。ユーザー新ルールを全ケース確定：①カンバン本体は**タグありのみ**（旧は全付箋）／②**タグなしは色問わず「その他」＝一番上の行の青の右隣に上寄り右下階段で重ねる**／③タグありでも**白黒その他色は列を作らずそのタグ行の青の右**に置く／④黄赤青は固定3列だが**列幅は詰める**／⑤**行の上げ目＝2行目以降で上の行と色が1つも被らない行は、まるごと（上端そろえたまま）上の行の高さの半分だけ上に食い込ませて空白の無駄を消す**（全色対象・行ごと。列ごとにギザギザ上げる案は使いにくいので不採用）／⑥右下階段＋stepY最小値で下のタイトルが見える。次=2-g で arrange.rs を 3.0 に作り直す（Codex 実装→test→実機）。 |
| 16 | 26-06-23 | **HTMLモック（.planning/arrange-mock.html）で認識合わせ→3.0 を確定値に更新**。モック1回目で判明した誤りを修正し仕様確定：①**白・黒・5色外は、タグの有無に関わらず「タグなし置き場」へ集約**（No.15の「白黒は青の右」を撤回）。②**置き場の呼称を「その他」→「タグなし」に統一**（中身はタグなし＋白黒＋5色外）。③置き場所＝**1行目の右端の右**（青の右隣ではない）。④行は1タグのみ（別タグが同Yに混ざらない＝モックの最大バグを修正）。⑤**上げ目の食い込み量＝半分固定をやめ、上の行とぶつからない最大量＝空き分だけ詰める**＋**累積なし（直前1行のみ比較）**。⑥重ねるのは同タグ同色だけ（別タグは矩形衝突禁止）。Codex で部品1（classify 振り分け関数＋テスト5件 pass）実装済み。次=部品2でカンバン配置を arrange.rs に実装。 |
| 17 | 26-06-23 | **本質を明文化＝「高さだけがタグの区切り」（3.0冒頭★本質）**。モックにレーンの線・見出しを出していたため、別タグを同じ高さに並べる重大ミス（仕事と趣味が同じ横帯）に気づけなかった。本番には線も見出しも無く**タグの違いは付箋の高さ（Y）だけで表す**＝別タグを同じ高さに置くと混ざって見える。よって**タグごとに必ず高さを変えるのが最優先**、次が文字切れ対策の上げ目、両者はトレードオフ（詰めすぎると高さ差が消える）。**上げ目の数値基準を確定＝食い込みは「一つ上のレーンの黄色最下付箋の高さの半分まで」**。行の単調性チェック（各レーン最上端topが直前より必ず下）を必須化。モックからレーン線・見出しを削除する方針。 |
| 18 | 26-06-23 | **レーン順を「色の処理フロー」基準に変更＝黄枚数→同数なら赤枚数→同数なら青枚数の辞書順で多い方が上**（旧「総枚数が多い順」を撤回）。プロダクトストーリー＝黄(思いつき)→赤(TODO)→青(結果/済み)→保管庫、の処理フロー。黄を多く抱えるタグほど処理量が多く上に来る。「黄をどんどん処理していくゲーム」。 |
| 19 | 26-06-23 | **HTMLモックでの認識合わせ完了・確定パラメータ確定**。上げ目バグ2件修正（①intrusionを下限→到達上限に直しEが下に取り残される不具合解消／②上げ目を「色が被らないレーンだけ」に限定し LANE_GAP が効かない不具合解消）。確定値: STEP_X=18 / STEP_Y_MIN=50 / COL_GAP=16 / **LANE_GAP=48** / BUCKET_GAP=40 / 上げ目到達上限=一つ上レーンの黄色最下付箋 y+h/2。モック(.planning/arrange-mock.html)で基本・上げ目多発・その他大量の3パターンOK。次=この3.0を Rust(arrange.rs)に実装（部品2）。 |
