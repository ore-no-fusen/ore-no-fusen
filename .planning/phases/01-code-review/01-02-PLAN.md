---
phase: 01-code-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-code-review/01-02-frontend-review-notes.md
autonomous: true
requirements:
  - STAB-01
  - DATA-01
  - DATA-02
  - UI-01

must_haves:
  truths:
    - "StickyNote.tsx のすべての async listen() が unlisten を持ち、解除漏れがないことが確認されている"
    - "hasLoadedRef の二重ガードが空body上書きを完全にブロックすることが確認されている"
    - "autoSave の非同期フローに競合状態のリスクがないことが確認されている"
    - "isNewNote リセットが新規作成・再編集の両シナリオで正しく機能することが確認されている"
    - "RESEARCH.md の Open Questions 3件に対して調査結論が出ている"
  artifacts:
    - path: ".planning/phases/01-code-review/01-02-frontend-review-notes.md"
      provides: "フロントエンドレビュー中間ノート（Plan 03 の FINDINGS.md 作成に使用）"
  key_links:
    - from: "StickyNote.tsx"
      to: "async listen() 全呼び出し"
      via: "Grep + useEffect ブロック精読"
      pattern: "await listen|await.*\\.listen"
    - from: "useNoteFile.ts"
      to: "hasLoadedRef ガード"
      via: "autoSave useEffect 精読"
      pattern: "hasLoadedRef\\.current"
    - from: "StickyNote.tsx"
      to: "isNewNote state"
      via: "setIsNewNote 呼び出しパス精読"
      pattern: "setIsNewNote|isNewNote"
---

<objective>
フロントエンド（StickyNote.tsx, useNoteFile.ts, useEditMode.ts）を静的レビューし、Listenerリーク・空body上書き・競合状態・カーソル位置バグのリスクを洗い出す。

Purpose: STAB-01, DATA-01, DATA-02, UI-01 の充足状況を確認し、残存リスクと Open Questions への結論を記録する。
Output: `.planning/phases/01-code-review/01-02-frontend-review-notes.md`（Plan 03 の FINDINGS.md 作成に使用する中間ノート）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-code-review/01-RESEARCH.md

## このプランの作業方針

コードを変更しない。Grep + Read で精読し、発見事項を中間ノートに記録する。

### 調査する Open Questions（RESEARCH.md より）

1. **isPool リスナーの wrapUnlisten 未使用**
   - `fusen:promote_from_pool` リスナーが `u()` を直接呼ぶ（wrapUnlisten なし）
   - Tauri v2 で unlisten が Promise を返す場合、.catch() なしにエラーが出るリスクがあるか

2. **useEditMode の startEditing が initialContent に依存**
   - `startEditing` の deps が `[isEditing, initialContent]` で、初期空 content を参照するケースがあるか

3. **useEffect の deps に isHover を含む handleGlobalPointer**
   - isHover の更新頻度によるパフォーマンス影響の深刻度

### 既知修正済み事項（Pitfall 2: これらを「新規リスク」として記録しない）

- C-1: fusen_update_geometry デッドコード削除済み
- C-2: reload_note リスナーの空body上書き修正済み
- H-1: hasLoadedRef による空body ブロック修正済み
- isNewNote バグ: ダブルクリック時に setIsNewNote(false) 追加済み

### 注意（Pitfall 3: パターン混在の判定）

複数の unlisten パターン（cancelled / isMounted / mounted）は即バグではない。
各パターンが「async listen() の解決前後を正しくハンドリングしているか」を本質として確認する。
動作上問題なければ「要改善（低優先）」として記録する。
</context>

<tasks>

<task type="auto">
  <name>Task 1: Listener リーク・データ保護・競合状態の精読</name>
  <files>.planning/phases/01-code-review/01-02-frontend-review-notes.md</files>
  <action>
以下の手順でフロントエンドコードを調査する。コードは一切変更しない。

**STAB-01: Listener リーク確認（StickyNote.tsx）**

1. Grep で StickyNote.tsx の `await listen` / `await.*\.listen` を列挙する。
2. 各 listen() 呼び出しを含む useEffect ブロックを Read で精読し、以下を確認:
   - `cancelled` / `isMounted` / `mounted` フラグが存在するか
   - async setup 関数の resolve 後にフラグチェックがあるか
   - cleanup return で unlisten が呼ばれるか
3. isPool リスナー（`fusen:promote_from_pool`）の `u()` 直接呼び出しを確認し、Tauri v2 の unlisten 関数が同期か非同期かを判定して Open Question 1 に結論を出す。

**DATA-01: 空body上書き確認（useNoteFile.ts）**

4. useNoteFile.ts を Read で全体精読し、以下のガードが存在することを確認:
   - `hasLoadedRef.current` による autoSave ブロック
   - 空 body + 空 frontmatter の二重チェック
5. reload_note ハンドラ（StickyNote.tsx）の空 body スキップロジックを確認する（C-2 修正）。

**DATA-02: 競合状態確認（useNoteFile.ts + StickyNote.tsx）**

6. autoSave の useEffect deps を確認し、不必要な再実行トリガーがないかを確認する。
7. `cancelled` フラグが loadNote 非同期処理中のアンマウントを正しく処理するかを確認する。
8. useEditMode の startEditing deps（`[isEditing, initialContent]`）を確認し、Open Question 2 に結論を出す。

**UI-01: カーソル位置確認（StickyNote.tsx + useEditMode.ts）**

9. isNewNote の state フローを追う:
   - 新規作成時に `setIsNewNote(true)` が呼ばれるパス
   - ダブルクリック時に `setIsNewNote(false)` が呼ばれるパス（修正済み確認）
   - startEditing の RichTextEditor カーソル初期化ロジック（50/150/300ms setTimeout）との整合性
10. useEditMode.ts を Read で精読し、startEditing / endEditing の実装を確認する。

**パフォーマンス（低優先）**

11. StickyNote.tsx の handleGlobalPointer useEffect の deps を確認し（`[isHover]`）、Open Question 3 に深刻度判定を出す。

調査結果を `.planning/phases/01-code-review/01-02-frontend-review-notes.md` に以下の形式でまとめる:

```
# フロントエンド レビューノート（Plan 02）

## STAB-01: Listener リーク確認

### listen() 呼び出し一覧
| useEffect | イベント | フラグパターン | wrapUnlisten | 問題 |
|-----------|---------|--------------|--------------|------|
| ...（実際の発見内容）|

### Open Question 1: isPool の u() 直接呼び出し
[結論: 問題あり / 問題なし（理由）]

## DATA-01: 空body上書きガード確認
[hasLoadedRef ガード・二重チェックの確認結果]

## DATA-02: 競合状態確認
[autoSave deps・cancelled フラグ・Open Question 2 の結論]

## UI-01: isNewNote カーソル位置確認
[新規作成・再編集両シナリオの状態フロー確認結果]

## パフォーマンス（低優先）
[Open Question 3: isHover deps の深刻度判定]
```
  </action>
  <verify>
    <automated>test -f .planning/phases/01-code-review/01-02-frontend-review-notes.md && echo "OK: ノートファイル存在確認"</automated>
  </verify>
  <done>
    - `.planning/phases/01-code-review/01-02-frontend-review-notes.md` が存在する
    - StickyNote.tsx の全 listen() 呼び出しについてリーク有無が判定されている
    - Open Question 1, 2, 3 それぞれに結論が記録されている
    - hasLoadedRef ガードの完全性が確認されている
    - isNewNote フローが新規作成・再編集両方で正しいことが確認されている
  </done>
</task>

</tasks>

<verification>
`.planning/phases/01-code-review/01-02-frontend-review-notes.md` を確認し、以下が揃っていること:
- STAB-01, DATA-01, DATA-02, UI-01 の各セクションが存在する
- Open Questions 3件すべてに結論が記録されている
- 既修正済み項目（C-1, C-2, H-1, isNewNote バグ）が新規リスクとして混入していない
</verification>

<success_criteria>
- 4要件（STAB-01, DATA-01, DATA-02, UI-01）について「確認済み/残存/要確認」が明確
- RESEARCH.md の Open Questions 3件に結論が出ている
- Phase 2 の修正対象リストとして使える精度の情報が揃っている
</success_criteria>

<output>
完了後、`.planning/phases/01-code-review/01-02-SUMMARY.md` を作成する。
</output>
