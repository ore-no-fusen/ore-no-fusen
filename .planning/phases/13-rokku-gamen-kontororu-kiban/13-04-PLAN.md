---
phase: 13-rokku-gamen-kontororu-kiban
plan: 04
type: execute
wave: 4
depends_on:
  - "13-03"
files_modified: []
autonomous: false
requirements:
  - LOCK-01
  - LOCK-02
  - LOCK-03
  - LOCK-04
  - LOCK-05

must_haves:
  truths:
    - "一覧の任意のメモをタップひとつでロック画面に通知として表示される（iPhone 実機確認）"
    - "ロック中の🔔を再タップすると通知が消える（iPhone 実機確認）"
    - "ロック中メモの🔔が text-blue-500 で表示され、未ロックは text-gray-400（UI 確認）"
    - "2件以上のメモを同時にロック画面に表示できる（独立した通知として）"
    - "アプリを閉じて再度開くと🔔状態が復元されている"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "Phase 13 完全実装"
      contains: "lockedNoteIds"
  key_links: []
---

<objective>
iPhone 実機での Phase 13 全機能検証を行い、LOCK-01〜05 のすべてが動作することを確認する。

Purpose: Service Worker 通知はブラウザ自動テストで検証できないため、実機での手動検証が必要。
Output: Phase 13 完了の承認
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-rokku-gamen-kontororu-kiban/13-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: iPhone 実機での Phase 13 全機能検証</name>
  <action>iPhone 実機での手動検証。LOCK-01〜05 の全要件を以下の手順で確認する。Service Worker 通知はブラウザ自動テストで検証できないため、実機確認が必要。</action>
  <what-built>
Phase 13 の全機能:
- 一覧の全メモカードに🔔ボタン（常時表示）
- 🔔タップ → ロック画面通知表示（fusen-lock-&lt;id&gt; タグ）
- 🔔再タップ → 通知消去
- ロック中: text-blue-500、未ロック: text-gray-400
- 起動時ロック状態復元（IndexedDB locked フラグ）
- 削除時の自動ロック解除
  </what-built>
  <how-to-verify>
iPhone で俺の付箋 PWA を開き、以下を順番に確認してください。

**前提条件:**
- `npm run dev` を起動しておく（または本番デプロイ）
- iPhone Safari でホーム画面に追加済み（PWA モード）
- 通知権限が未設定または granted の状態

**LOCK-01: ロック画面への表示**
1. iPhone で一覧画面を開く
2. 任意のメモの🔔ボタンをタップ
3. 初回の場合は通知権限ダイアログが表示される → 「許可」をタップ
4. iPhoneのロック画面に通知が表示されることを確認
   - 通知タイトルがメモの先頭行と一致している
   - 通知に残りの本文（先頭40文字）が表示されている
5. 一覧で🔔が青色（text-blue-500）になっていることを確認（LOCK-03）

**LOCK-02: 通知の消去**
6. 🔔ボタン（青色）を再タップ
7. ロック画面の通知が消えることを確認
8. 一覧で🔔がグレー（text-gray-400）に戻ることを確認

**LOCK-04: 複数メモの同時表示**
9. 異なる2件のメモそれぞれの🔔をタップ
10. ロック画面に2件の独立した通知が表示されることを確認

**LOCK-05: 起動時の復元**
11. どれか1件のメモをロック中（🔔青色）の状態にする
12. PWA を完全終了（ホームボタン → アプリを上にスワイプして終了）
13. PWA を再度開く
14. 一覧でロック中だったメモの🔔が青色で表示されていることを確認
15. ロック画面にも通知が表示されていることを確認（permission granted の場合）

**削除時のロック解除（Pitfall 5 検証）:**
16. ロック中のメモを削除する
17. ロック画面の通知が消えることを確認
  </how-to-verify>
  <verify>
    <automated>MISSING — Service Worker 通知はブラウザ自動テストで検証不可。iPhone 実機確認が必要。</automated>
  </verify>
  <done>LOCK-01〜05 がすべて iPhone 実機で動作確認済み。ユーザーが "approved" を入力。</done>
  <resume-signal>すべて確認できたら "approved" と入力してください。問題があれば具体的な症状を教えてください。</resume-signal>
</task>

</tasks>

<verification>
iPhone 実機での全5要件の動作確認完了。
</verification>

<success_criteria>
- LOCK-01〜05 がすべて iPhone 実機で動作確認済み
- ユーザーが "approved" を入力
</success_criteria>

<output>
After completion, create `.planning/phases/13-rokku-gamen-kontororu-kiban/13-04-SUMMARY.md`
</output>
