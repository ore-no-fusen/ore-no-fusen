---
phase: 13-rokku-gamen-kontororu-kiban
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - app/viewer/page.tsx
autonomous: true
requirements:
  - LOCK-01
  - LOCK-02
  - LOCK-03
  - LOCK-04

must_haves:
  truths:
    - "一覧の全メモカード右カラムに🔔ボタンが表示される（draft/sent/received_pc を問わず）"
    - "🔔ボタンをタップするとロック画面通知が発火し、🔔が text-blue-500 になる（楽観的UI更新）"
    - "🔔を再タップすると通知が消え、🔔が text-gray-400 に戻る"
    - "2件のメモを独立して🔔タップすると、それぞれ fusen-lock-<id> タグの通知が独立して存在する"
    - "権限が denied の場合、エラーメッセージが表示される"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "lockedNoteIds state、isLockPermissionPending state、handleLockToggle 関数、🔔ボタン UI"
      contains: "lockedNoteIds"
  key_links:
    - from: "🔔ボタン onClick"
      to: "handleLockToggle"
      via: "React イベントハンドラ"
      pattern: "handleLockToggle"
    - from: "handleLockToggle"
      to: "registration.showNotification()"
      via: "navigator.serviceWorker.ready"
      pattern: "showNotification"
    - from: "handleLockToggle"
      to: "saveDraft"
      via: "locked: true フィールド"
      pattern: "saveDraft.*locked"
---

<objective>
DraftRecord 型の拡張と🔔ボタンのコア実装を行い、LOCK-01〜04 を充足する。

Purpose: ロック画面コントロール基盤の主要機能。一覧から通知を表示・消去し、ON/OFF 状態を視覚的に識別できるようにする。
Output: app/viewer/page.tsx への差分（型追加・state 追加・ハンドラ追加・UI 追加）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-rokku-gamen-kontororu-kiban/13-CONTEXT.md
@.planning/phases/13-rokku-gamen-kontororu-kiban/13-RESEARCH.md

<interfaces>
<!-- 既存コードの主要型・state。executor はこれをそのまま参照する。 -->

From app/viewer/page.tsx (line 389):
```typescript
type DraftRecord = {
  id: string;
  title: string;
  body: string;
  created_at: string;
  images: { fileName: string; blob: Blob }[];
  tags?: string[];
  received_pc?: true;
  sent_at?: string;
  // ← ここに locked?: true を追加する
};
```

From app/viewer/page.tsx (line 668):
```typescript
const [activeNotifIds, setActiveNotifIds] = useState<string[]>([]);
// ロック通知管理には activeNotifIds を使わない。別 state を追加する。
```

From app/viewer/page.tsx (line 1725-1743) — 既存の🔕ボタンパターン:
```tsx
<div className="flex items-center gap-0">
  {note.status === 'received_pc' && activeNotifIds.includes(note.id) && (
    <button
      className="p-2 text-gray-400 hover:text-blue-500"
      aria-label="通知を削除"
      onClick={async (e) => {
        e.stopPropagation();
        // ...CLOSE_NOTIFICATION...
      }}
    >
      🔕
    </button>
  )}
  {/* 🗑️ ボタン line 1744〜 */}
</div>
```

From app/viewer/page.tsx — 既存の SW メッセージパターン:
```typescript
// GET_NOTIFICATIONS（既存）
navigator.serviceWorker.ready.then((reg) => {
  const channel = new MessageChannel();
  channel.port1.onmessage = (e) => setActiveNotifIds(e.data.ids ?? []);
  reg.active?.postMessage({ type: 'GET_NOTIFICATIONS' }, [channel.port2]);
}).catch(() => {});

// CLOSE_NOTIFICATION（既存）
reg.active?.postMessage({ type: 'CLOSE_NOTIFICATION', tag: 'fusen-' + note.id });
```

From app/viewer/page.tsx — errorMessage state（Claude's Discretion: 既存を流用）:
```typescript
// 既存の errorMessage state を流用する（新規 state 追加しない）
setErrorMessage('通知権限が必要です。設定から有効にしてください');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DraftRecord 型拡張と lockedNoteIds state 追加</name>
  <files>app/viewer/page.tsx</files>
  <action>
以下の最小変更を `app/viewer/page.tsx` に加える。

**1. DraftRecord 型に `locked?: true` を追加（line 389 付近）:**
```typescript
type DraftRecord = {
  id: string;
  title: string;
  body: string;
  created_at: string;
  images: { fileName: string; blob: Blob }[];
  tags?: string[];
  received_pc?: true;
  sent_at?: string;
  locked?: true;  // ← 追加
};
```
DB バージョンアップは不要（既存スキーマへのフィールド追加のみ）。

**2. `lockedNoteIds` と `isLockPermissionPending` state を追加:**
既存の `activeNotifIds` state（line 668）の直後に追加する:
```typescript
const [lockedNoteIds, setLockedNoteIds] = useState<string[]>([]);
const [isLockPermissionPending, setIsLockPermissionPending] = useState(false);
```

**3. `handleLockToggle` 関数を追加:**
既存の一覧カードに関するイベントハンドラ群の近くに配置する（コンポーネント内）。
RESEARCH.md の「Pattern 5: 楽観的 UI 更新」のコードをそのまま実装する:

```typescript
const handleLockToggle = async (e: React.MouseEvent, note: IphoneNote) => {
  e.stopPropagation();
  const isLocked = lockedNoteIds.includes(note.id);

  // 楽観的更新
  if (isLocked) {
    setLockedNoteIds(prev => prev.filter(id => id !== note.id));
  } else {
    setLockedNoteIds(prev => [...prev, note.id]);
  }

  try {
    if (isLocked) {
      // ロック解除: 通知を閉じてDB更新
      const reg = await navigator.serviceWorker.ready;
      reg.active?.postMessage({ type: 'CLOSE_NOTIFICATION', tag: `fusen-lock-${note.id}` });
      const draft = await loadDraft(note.id);
      if (draft) {
        const { locked, ...rest } = draft;
        await saveDraft(rest as DraftRecord);
      }
    } else {
      // ロック ON: 権限確認→通知表示→DB更新
      if (Notification.permission === 'default') {
        setIsLockPermissionPending(true);
        const result = await Notification.requestPermission();
        setIsLockPermissionPending(false);
        if (result !== 'granted') {
          setLockedNoteIds(prev => prev.filter(id => id !== note.id));
          setErrorMessage('通知権限が必要です。設定から有効にしてください');
          return;
        }
      } else if (Notification.permission === 'denied') {
        setLockedNoteIds(prev => prev.filter(id => id !== note.id));
        setErrorMessage('通知権限が必要です。設定から有効にしてください');
        return;
      }

      // 通知タイトル・body の生成
      const rawTitle = note.title || '';
      const rawBody = note.body || '';
      const notifTitle = rawTitle
        ? rawTitle.replace(/^#\s*/, '')
        : rawBody.slice(0, 20) || '（無題）';
      const notifBody = rawTitle
        ? rawBody.slice(0, 40)
        : rawBody.slice(20, 60);

      // SW 経由で通知表示（new Notification() はモバイルで動かない）
      const reg = await navigator.serviceWorker.ready;
      await reg.showNotification(notifTitle, {
        body: notifBody,
        tag: `fusen-lock-${note.id}`,
        icon: '/icon-192.png',
        badge: '/icon-192.png',
      });

      // DB に locked: true を保存
      const draft = await loadDraft(note.id);
      if (draft) {
        await saveDraft({ ...draft, locked: true });
      } else {
        // received_pc 等で draft が null の場合: 最小レコードを生成して保存
        await saveDraft({
          id: note.id,
          title: note.title || '',
          body: note.body || '',
          created_at: note.created_at || new Date().toISOString(),
          images: [],
          tags: note.tags,
          locked: true,
        });
      }
    }
  } catch {
    // 失敗時はロールバック
    if (isLocked) {
      setLockedNoteIds(prev => [...prev, note.id]);
    } else {
      setLockedNoteIds(prev => prev.filter(id => id !== note.id));
    }
  }
};
```

**重要な制約:**
- `new Notification()` を使ってはいけない（モバイル Safari で動作しない）
- `activeNotifIds` にロック通知 ID を混在させてはいけない（タグ変換の不整合が起きる）
- SW に `SHOW_NOTIFICATION` メッセージを送ってはいけない（ハンドラが存在しない）
- `isStandalone` が false の場合でも🔔ボタンは表示する（ただし permission エラーになるだけ）
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript がエラーなくコンパイルされる。DraftRecord に locked フィールドが存在し、lockedNoteIds state と handleLockToggle 関数がコンポーネント内に定義されている。</done>
</task>

<task type="auto">
  <name>Task 2: 一覧カードへの🔔ボタン UI 追加</name>
  <files>app/viewer/page.tsx</files>
  <action>
一覧カード右カラムのボタン群（line 1725 付近の `<div className="flex items-center gap-0">`）に🔔ボタンを追加する。

**追加位置:** 既存の🔕ボタン（line 1726〜）の直前に挿入する。

**追加するボタン:**
```tsx
<button
  className={`p-2 ${lockedNoteIds.includes(note.id) ? 'text-blue-500' : 'text-gray-400'} hover:text-blue-500`}
  aria-label={lockedNoteIds.includes(note.id) ? 'ロック解除' : 'ロック画面に表示'}
  disabled={isLockPermissionPending}
  onClick={(e) => handleLockToggle(e, note)}
>
  🔔
</button>
```

**配置ルール（CONTEXT.md より）:**
- 全メモに対して常時表示（draft / sent / received_pc を問わず条件分岐なし）
- 🗑️ボタンの左隣、🔕ボタンの左隣に配置（順序: 🔔 🔕 🗑️）

その後、Task 1 で追加した実装を LOCK-03 テストに対して GREEN にする。

`npx playwright test --grep "LOCK-03"` を実行し、テストが PASS するよう必要であれば `tests/lock-notification.spec.ts` のスタブを実際のテストに書き換える。

LOCK-03 テスト内容:
- viewer ページを開く
- list ステップの一覧に🔔ボタンが存在することを確認
- 🔔ボタンのクラスに `text-gray-400` があること（初期は OFF 状態）
- （SW モックが必要なため、UI クラスの静的確認のみとする）
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen && npx playwright test --grep "LOCK-03" 2>&1 | tail -10</automated>
  </verify>
  <done>🔔ボタンが一覧の全メモカードに表示される。lockedNoteIds に ID が含まれるメモは text-blue-500、含まれないメモは text-gray-400 で描画される。LOCK-03 テストが PASS。</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` がエラーなし
2. `npx playwright test --grep "LOCK-03"` が PASS
3. `npx playwright test --grep "LOCK-04"` はまだ FAIL（Wave 2 で対応）
</verification>

<success_criteria>
- DraftRecord 型に `locked?: true` フィールドが存在する
- `lockedNoteIds: string[]` state が存在する
- `handleLockToggle` 関数が存在し、楽観的 UI 更新・permission チェック・通知表示・DB 保存が実装されている
- 一覧の全メモカードに🔔ボタンが表示される
- ロック中は text-blue-500、未ロックは text-gray-400
- TypeScript コンパイルエラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/13-rokku-gamen-kontororu-kiban/13-02-SUMMARY.md`
</output>
