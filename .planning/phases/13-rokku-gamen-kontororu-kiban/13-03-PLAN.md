---
phase: 13-rokku-gamen-kontororu-kiban
plan: 03
type: execute
wave: 3
depends_on:
  - "13-02"
files_modified:
  - app/viewer/page.tsx
  - tests/lock-notification.spec.ts
autonomous: true
requirements:
  - LOCK-05
  - LOCK-02

must_haves:
  truths:
    - "アプリを閉じて再度開くと、ロック中メモの🔔が text-blue-500 で復元されている"
    - "メモを削除するとロック画面の通知も同時に消える"
    - "起動時復元は Notification.permission === 'granted' の場合のみ通知を再発火する（permission が default/denied の場合は UI 復元のみ）"
    - "LOCK-04/05 の Playwright テストが PASS する"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "起動時ロック復元ロジック（list useEffect 拡張）、削除時ロック解除処理"
      contains: "lockedNoteIds"
  key_links:
    - from: "list useEffect (line 864)"
      to: "loadAllDrafts()"
      via: "locked フラグ抽出 → setLockedNoteIds"
      pattern: "locked.*true.*lockedNoteIds"
    - from: "削除ハンドラ (line 1748)"
      to: "CLOSE_NOTIFICATION"
      via: "lockedNoteIds.includes(note.id) チェック"
      pattern: "fusen-lock-.*note\\.id"
---

<objective>
起動時ロック状態復元（LOCK-05）と削除時ロック解除（LOCK-02 補完）を実装し、LOCK-04/05 テストを GREEN にする。

Purpose: アプリ再起動後もロック状態が失われず、削除したメモが通知に残り続けるバグを防ぐ。
Output: app/viewer/page.tsx の list useEffect 拡張と削除ハンドラへの追加処理
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-rokku-gamen-kontororu-kiban/13-CONTEXT.md
@.planning/phases/13-rokku-gamen-kontororu-kiban/13-RESEARCH.md
@.planning/phases/13-rokku-gamen-kontororu-kiban/13-02-SUMMARY.md

<interfaces>
<!-- Wave 2 で追加された主要コード。13-02-SUMMARY.md から確認すること。 -->

From app/viewer/page.tsx (list useEffect, line 864〜899):
```typescript
useEffect(() => {
  if (step !== 'list') return;
  setIsHistoryLoading(true);
  const draftsPromise = loadAllDrafts().catch(() => [] as DraftRecord[]);
  // GET_NOTIFICATIONS...
  let thumbUrls: string[] = [];
  draftsPromise
    .then((drafts) => {
      const draftNotes: IphoneNote[] = drafts.map((d) => ({
        id: d.id, title: d.title, body: d.body,
        status: d.sent_at ? ('sent') : d.received_pc ? ('received_pc') : ('draft'),
        created_at: d.created_at, tags: d.tags,
      }));
      // ... setHistoryNotes, setThumbnailUrls ...
    })
    .finally(() => setIsHistoryLoading(false));
  return () => { thumbUrls.forEach((u) => URL.revokeObjectURL(u)); };
}, [step]);
```

From app/viewer/page.tsx (削除ハンドラ, line 1748〜):
```typescript
onClick={async (e) => {
  e.stopPropagation();
  setIsLoading(true);
  try {
    if (note.status === 'sent') {
      await deleteDraft(note.id);
      setHistoryNotes((prev) => prev.filter((n) => n.id !== note.id));
    } else {
      await deleteDraft(note.id);
      if (note.status === 'received_pc') {
        // Drive の notes_to_iphone.json からも削除...
        // CLOSE_NOTIFICATION (fusen-<id>)...
        setActiveNotifIds((prev) => prev.filter((id) => id !== note.id));
      }
      // loadAllDrafts して setHistoryNotes...
    }
  }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: list useEffect に起動時ロック復元を追加</name>
  <files>app/viewer/page.tsx</files>
  <action>
`step === 'list'` の useEffect（line 864〜899）内の `draftsPromise.then()` ブロックを拡張する。

`drafts` が取得できた後、以下を追加する（`setHistoryNotes(merged)` の後に挿入）:

```typescript
// ロック状態を復元: locked === true のメモIDを lockedNoteIds に反映
const lockedIds = drafts.filter(d => d.locked).map(d => d.id);
setLockedNoteIds(lockedIds);

// 通知を再発火（権限が granted の場合のみ）
// 注意: 起動時は自動で permission リクエストしてはいけない（iOS 制約）
if (lockedIds.length > 0 && Notification.permission === 'granted') {
  navigator.serviceWorker.ready.then(async (reg) => {
    for (const d of drafts.filter(d => d.locked)) {
      const rawTitle = d.title || '';
      const rawBody = d.body || '';
      const notifTitle = rawTitle
        ? rawTitle.replace(/^#\s*/, '')
        : rawBody.slice(0, 20) || '（無題）';
      const notifBody = rawTitle
        ? rawBody.slice(0, 40)
        : rawBody.slice(20, 60);
      await reg.showNotification(notifTitle, {
        body: notifBody,
        tag: `fusen-lock-${d.id}`,
        icon: '/icon-192.png',
        badge: '/icon-192.png',
      });
    }
  }).catch(() => {});
}
```

**RESEARCH.md Pitfall 4 への対応（必須）:**
- `Notification.permission === 'granted'` のチェックを必ず行う
- `'default'` または `'denied'` の場合は通知再発火をスキップし、`setLockedNoteIds(lockedIds)` だけ実行する
- 起動時に `Notification.requestPermission()` を呼んではいけない

**RESEARCH.md Pitfall 3 への対応（LOCK-05 永続化の確認）:**
- `received_pc` メモで `loadDraft` が null を返す場合、13-02 の `handleLockToggle` で最小レコードを生成・保存している
- 起動時復元では `loadAllDrafts()` を使うため、最小レコードが存在すれば `locked: true` が取得できる
- この実装で `received_pc` メモの起動時復元も対応できていることを確認する
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript コンパイルエラーなし。list useEffect 内に lockedIds 抽出と setLockedNoteIds 呼び出しが存在する。Notification.permission チェックが存在する。</done>
</task>

<task type="auto">
  <name>Task 2: 削除ハンドラへのロック解除追加 + LOCK-04/05 テスト GREEN 化</name>
  <files>app/viewer/page.tsx, tests/lock-notification.spec.ts</files>
  <action>
**Part A: 削除ハンドラへのロック解除追加**

削除ハンドラ（line 1748〜）の `deleteDraft(note.id)` 呼び出し直後に、ロック中メモの通知解除を追加する。

`await deleteDraft(note.id)` の直後（`if (note.status === 'sent')` ブランチと `else` ブランチの両方）に以下を追加する:

```typescript
// ロック中メモの通知を解除
if (lockedNoteIds.includes(note.id)) {
  try {
    const reg = await navigator.serviceWorker.ready;
    reg.active?.postMessage({ type: 'CLOSE_NOTIFICATION', tag: `fusen-lock-${note.id}` });
    setLockedNoteIds(prev => prev.filter(id => id !== note.id));
  } catch {
    // エラー無視（削除自体は成功している）
  }
}
```

RESEARCH.md の「Pitfall 5: 削除時のロック解除漏れ」への対応。

**Part B: LOCK-04/05 テストを GREEN に書き換える**

`tests/lock-notification.spec.ts` のスタブを実際のテストに書き換える:

**LOCK-04 テスト（通知タグ形式の検証）:**
- 2件の noteId に対して `fusen-lock-<id>` タグが生成されることを確認
- タグが異なることを確認（独立した通知であることの保証）
- 実際の通知発火は SW が必要なため、タグ生成ロジックのユニットテストとして実装
- `page.evaluate()` で `lockedNoteIds` state の内容を検証するか、ボタンの `aria-label` で状態を確認する

**LOCK-05 テスト（DB locked フラグの永続化）:**
- viewer ページを開いてモックの list ステップをシミュレートする
- `DraftRecord` に `locked` フィールドが型として存在することを TypeScript 型チェックで確認
- 実際の IndexedDB 操作はブラウザ環境が必要なため、UI 上で locked=true のメモが text-blue-500 で表示されることを確認する（e2e テストとして実装）

**テストが複雑すぎる場合の代替:**
- LOCK-04/05 が e2e で検証困難な場合は `test.todo()` に変更し、手動検証項目として扱う
- RESEARCH.md の「Validation Architecture」参照: LOCK-03〜05 は unit テスト想定だが、手動検証でも可
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen && npx playwright test --grep "LOCK" 2>&1 | tail -15</automated>
  </verify>
  <done>削除ハンドラに lockedNoteIds チェックと CLOSE_NOTIFICATION が追加されている。npx playwright test --grep "LOCK" が全件 PASS または SKIP（todo）で終わる。</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` がエラーなし
2. `npx playwright test --grep "LOCK"` が全件 PASS または SKIP（FAIL なし）
3. `npx playwright test` でフルスイートが既存テストを壊していないこと
</verification>

<success_criteria>
- list useEffect 内に起動時ロック復元ロジックが存在する（setLockedNoteIds + permission 条件付き showNotification）
- 削除ハンドラが lockedNoteIds をチェックし、ロック中なら CLOSE_NOTIFICATION と setLockedNoteIds 更新を実行する
- npx playwright test --grep "LOCK" が FAIL なし（PASS または TODO）
- npx playwright test でフルスイート合格（既存テスト 13 件が引き続き PASS）
</success_criteria>

<output>
After completion, create `.planning/phases/13-rokku-gamen-kontororu-kiban/13-03-SUMMARY.md`
</output>
