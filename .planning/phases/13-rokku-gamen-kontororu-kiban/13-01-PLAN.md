---
phase: 13-rokku-gamen-kontororu-kiban
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lock-notification.spec.ts
autonomous: true
requirements:
  - LOCK-03
  - LOCK-04
  - LOCK-05

must_haves:
  truths:
    - "LOCK-03/04/05 のテストスタブが存在し、npx playwright test --grep LOCK を実行すると FAIL で終わる（RED 状態）"
    - "テストスタブがビルドエラーなくパースされる"
  artifacts:
    - path: "tests/lock-notification.spec.ts"
      provides: "LOCK-03/04/05 の Playwright テストスタブ"
      exports: []
  key_links: []
---

<objective>
LOCK-03/04/05 の自動テストスタブを先行作成し、実装前に RED 状態を確立する。

Purpose: Nyquist ルール準拠。Wave 2〜3 の実装タスクが <verify> コマンドを持てるようにする。
Output: tests/lock-notification.spec.ts（3件の failing テスト）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-rokku-gamen-kontororu-kiban/13-CONTEXT.md
@.planning/phases/13-rokku-gamen-kontororu-kiban/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LOCK-03/04/05 テストスタブ作成</name>
  <files>tests/lock-notification.spec.ts</files>
  <action>
`tests/lock-notification.spec.ts` を新規作成する。以下の3件のテストをスタブとして実装する。

**LOCK-03 テスト（ロック中メモの視覚的識別）:**
- viewer/page.tsx がロードされた後、一覧ステップに遷移したと仮定したシナリオ
- `lockedNoteIds` に noteId が含まれる場合、🔔ボタンに `text-blue-500` クラスが付くことを検証
- 実装前はスタブのため `test.skip` または `expect(true).toBe(false)` で FAIL させる

**LOCK-04 テスト（複数メモの独立した通知タグ）:**
- 通知タグが `fusen-lock-<noteId>` 形式になることを検証
- 2件のメモIDが異なる場合にタグが衝突しないことを検証
- スタブのため FAIL させる

**LOCK-05 テスト（DB locked フラグの永続化）:**
- `DraftRecord` 型に `locked?: true` フィールドが存在することを型レベルで検証
- IndexedDB の `saveDraft`/`loadDraft` が `locked` フィールドを保持することを検証
- スタブのため FAIL させる

**スタブの実装方針:**
- `test.todo()` または `test('...', async () => { expect(false).toBe(true); })` でFAIL状態を作る
- テストのタイトルに `[LOCK-03]`、`[LOCK-04]`、`[LOCK-05]` を含める
- playwright.config.ts の設定（ポート3003）に合わせる

**注意:** LOCK-01・LOCK-02 は iPhone 実機の Service Worker 通知が必要なため manual-only（テストスタブ不要）。RESEARCH.md の「Validation Architecture」セクション参照。
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen && npx playwright test --grep "LOCK" 2>&1 | tail -5</automated>
  </verify>
  <done>tests/lock-notification.spec.ts が存在し、npx playwright test --grep "LOCK" を実行すると3件が FAIL（またはスキップ）で終わる。パースエラーなし。</done>
</task>

</tasks>

<verification>
`npx playwright test --grep "LOCK"` で3件のテストが RED（FAIL）で終わること。
</verification>

<success_criteria>
- tests/lock-notification.spec.ts が存在する
- LOCK-03/LOCK-04/LOCK-05 の各テストがスタブ状態で FAIL する
- ビルドエラー・パースエラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/13-rokku-gamen-kontororu-kiban/13-01-SUMMARY.md`
</output>
