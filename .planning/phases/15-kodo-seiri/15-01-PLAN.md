---
phase: 15-kodo-seiri
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - app/viewer/lib/indexeddb.test.ts
  - app/viewer/lib/drive.test.ts
autonomous: true
requirements:
  - CLEAN-02

must_haves:
  truths:
    - "lib/indexeddb.test.ts が存在し、openDraftsDB / saveDraft / loadDraft / loadAllDrafts / deleteDraft の各関数に対するテストが記述されている"
    - "lib/drive.test.ts が存在し、getAppFolderId / uploadToDrive / downloadFromDrive / uploadWithAutoRefresh の各関数に対するテストが記述されている（fetch モック使用）"
    - "`npm test` を実行するとこれらのテストは FAIL する（実装前なので期待通り）"
  artifacts:
    - path: "app/viewer/lib/indexeddb.test.ts"
      provides: "IndexedDB 純粋関数のテストスタブ"
    - path: "app/viewer/lib/drive.test.ts"
      provides: "Drive API 操作関数のテストスタブ（fetch モック付き）"
  key_links:
    - from: "app/viewer/lib/indexeddb.test.ts"
      to: "app/viewer/lib/indexeddb.ts"
      via: "import { openDraftsDB, saveDraft, loadDraft, loadAllDrafts, deleteDraft } from './indexeddb'"
      pattern: "from './indexeddb'"
    - from: "app/viewer/lib/drive.test.ts"
      to: "app/viewer/lib/drive.ts"
      via: "import { getAppFolderId, uploadToDrive, ... } from './drive'"
      pattern: "from './drive'"
---

<objective>
Wave 0: CLEAN-02 実装前のテストスタブを先行作成する。

Purpose: Nyquist ルール準拠。lib/indexeddb.ts と lib/drive.ts がまだ存在しないため、
テストファイルを先に作成し import エラー状態（RED）で確定させる。
Wave 1 で実装を行い、これらのテストが GREEN になることで CLEAN-02 の完成を検証する。

Output:
- app/viewer/lib/indexeddb.test.ts（テストスタブ、実装前は import エラーで FAIL）
- app/viewer/lib/drive.test.ts（テストスタブ、実装前は import エラーで FAIL）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-kodo-seiri/15-RESEARCH.md
@.planning/phases/15-kodo-seiri/15-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/indexeddb.test.ts — テストスタブ作成（RED）</name>
  <files>app/viewer/lib/indexeddb.test.ts</files>
  <behavior>
    - openDraftsDB() を呼ぶとPromiseが返ること
    - saveDraft(draft) で DraftRecord が保存されること
    - saveDraft 後に loadDraft(id) で同一レコードが返ること
    - loadAllDrafts() が DraftRecord[] を返すこと
    - deleteDraft(id) で削除後に loadDraft(id) が null を返すこと
  </behavior>
  <action>
    `app/viewer/lib/` ディレクトリを作成し、`app/viewer/lib/indexeddb.test.ts` を新規作成する。

    ファイル冒頭に `import { openDraftsDB, saveDraft, loadDraft, loadAllDrafts, deleteDraft } from './indexeddb';`
    を記述する（この時点では lib/indexeddb.ts が存在しないため import エラーになる。これが期待する RED 状態）。

    テストは vi.mock で IDBDatabase をモックして純粋関数の挙動を確認する構成にする。
    indexeddb.ts が存在しない状態で `npm test` を実行し、このファイルが import エラーで FAIL することを確認する。

    テスト観点:
    1. `saveDraft` に DraftRecord を渡すと DB に put が呼ばれること
    2. `loadDraft(id)` が対象 ID のレコードを返すこと（IDBObjectStore.get モック）
    3. `loadAllDrafts()` が getAll の結果を返すこと
    4. `deleteDraft(id)` が delete を呼ぶこと

    注意: lib/ ファイルに `'use client'` は書かない（純粋関数のため不要）。
  </action>
  <verify>
    <automated>MISSING — lib/indexeddb.ts が Wave 1 で作成される。このスタブ自体は import エラーで FAIL が期待動作。Wave 1 完了後に npm test がグリーンになること</automated>
  </verify>
  <done>app/viewer/lib/indexeddb.test.ts が存在し、5つのテスト関数が定義されている（import エラーで FAIL 状態が正しい）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/drive.test.ts — テストスタブ作成（RED）</name>
  <files>app/viewer/lib/drive.test.ts</files>
  <behavior>
    - getAppFolderId(token) が正常レスポンスで folderId を返すこと
    - getAppFolderId がフォルダ未存在時に新規作成リクエストを送ること
    - uploadToDrive(token, name, content) が multipart リクエストを送ること
    - downloadFromDrive(token, fileId) がファイル内容を返すこと
    - uploadWithAutoRefresh が token 期限切れ時に refreshAccessToken を呼ぶこと
  </behavior>
  <action>
    `app/viewer/lib/drive.test.ts` を新規作成する。

    ファイル冒頭に `import { getAppFolderId, uploadToDrive, downloadFromDrive, uploadWithAutoRefresh } from './drive';`
    を記述する（この時点では lib/drive.ts が存在しないため import エラーになる）。

    テストは `vi.stubGlobal('fetch', vi.fn())` で fetch をモックし、各関数のネットワーク呼び出しを確認する構成にする。

    テスト観点:
    1. `getAppFolderId` が Drive API の files.list を呼ぶこと
    2. フォルダ未存在時に files.create を呼ぶこと
    3. `uploadToDrive` が multipart/related で呼ぶこと
    4. `downloadFromDrive` が正しいファイルIDで GET を呼ぶこと

    注意: lib/ ファイルに `'use client'` は書かない。React は import しない。
  </action>
  <verify>
    <automated>MISSING — lib/drive.ts が Wave 1 で作成される。このスタブ自体は import エラーで FAIL が期待動作。Wave 1 完了後に npm test がグリーンになること</automated>
  </verify>
  <done>app/viewer/lib/drive.test.ts が存在し、5つのテスト関数が定義されている（import エラーで FAIL 状態が正しい）</done>
</task>

</tasks>

<verification>
- app/viewer/lib/ ディレクトリが作成されている
- app/viewer/lib/indexeddb.test.ts が存在する（import エラーで FAIL — 期待動作）
- app/viewer/lib/drive.test.ts が存在する（import エラーで FAIL — 期待動作）
- Wave 0 の役割はスタブ先行。テストが GREEN になるのは Wave 1 完了後。
</verification>

<success_criteria>
- app/viewer/lib/indexeddb.test.ts: 5テスト関数が定義されている
- app/viewer/lib/drive.test.ts: 5テスト関数が定義されている
- どちらのファイルも `'use client'` と `import React` を含まない
- Wave 1 完了後に npm test がグリーンになる前提でスタブが設計されている
</success_criteria>

<output>
After completion, create `.planning/phases/15-kodo-seiri/15-01-SUMMARY.md`
</output>
