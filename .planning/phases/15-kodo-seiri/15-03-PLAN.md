---
phase: 15-kodo-seiri
plan: "03"
type: execute
wave: 2
depends_on:
  - "15-02"
files_modified:
  - app/viewer/page.tsx
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "app/viewer/page.tsx に `noteData` state の定義が存在しない"
    - "app/viewer/page.tsx に `downloadWithAutoRefresh` 関数の定義が存在しない"
    - "app/viewer/page.tsx の step 型 Union から `'note'` が削除されている"
    - "E2E テスト 13件（npm run test:e2e）が全件パスする"
    - "`npm test` がグリーンで通過する"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "dead code 削除後の page.tsx"
      contains: "step 型に 'note' を含まない"
  key_links:
    - from: "app/viewer/page.tsx"
      to: "なし（削除のみ）"
      via: "grep -c で参照ゼロを確認"
      pattern: "noteData|downloadWithAutoRefresh|step.*note"
---

<objective>
Wave 2: page.tsx から dead code を削除する（CLEAN-01）。

Purpose: 使われていない `noteData` state・`step='note'`・`downloadWithAutoRefresh` 関数を
削除し、page.tsx のノイズを減らす。Phase 17 のコンポーネント分割をしやすくする土台。

Output:
- app/viewer/page.tsx（dead code 削除後）

スコープ:
- 削除するのは noteData state・step Union の 'note'・downloadWithAutoRefresh の3件のみ
- activeNotifIds は Phase 16（FIX-03）のスコープのため削除しない（RESEARCH.md Pitfall 5）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-kodo-seiri/15-RESEARCH.md
@.planning/phases/15-kodo-seiri/15-02-SUMMARY.md

<interfaces>
<!-- 削除対象の dead code。RESEARCH.md の詳細調査結果より。 -->

削除対象1: noteData state（616行付近）
```typescript
// この3行を削除する
const [noteData, setNoteData] = useState<{
  title: string;
  body: string;
} | null>(null);
```

削除対象2: step 型 Union から 'note' を削除（613行付近）
```typescript
// Before
const [step, setStep] = useState<
  'banner' | 'login' | 'push' | 'ready' | 'write' | 'list' | 'note'
>('banner');

// After（'note' を削除）
const [step, setStep] = useState<
  'banner' | 'login' | 'push' | 'ready' | 'write' | 'list'
>('banner');
```

削除対象3: downloadWithAutoRefresh 関数（345行付近）
```typescript
// この関数定義（8行程度）を削除する
async function downloadWithAutoRefresh(
  token: string,
  ...
): Promise<{ title: string; body: string }> {
  ...
}
```

Phase 15では削除しないもの（Phase 16 スコープ）:
- activeNotifIds state（639行付近）
- GET_NOTIFICATIONS 呼び出し（804行付近）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dead code の参照確認と削除</name>
  <files>app/viewer/page.tsx</files>
  <action>
    **削除前の確認（必須）:**
    以下の grep を実行し、削除対象が本当に「使われていない」ことを確認する。

    ```bash
    grep -n "setNoteData\|noteData" app/viewer/page.tsx
    grep -n "downloadWithAutoRefresh" app/viewer/page.tsx
    grep -n "setStep.*note\|step.*===.*note\|'note'" app/viewer/page.tsx
    ```

    - `setNoteData` の呼び出しが0件であること（state 定義と型定義のみ）
    - `downloadWithAutoRefresh` の呼び出しが0件であること（関数定義のみ）
    - `setStep('note')` の呼び出しが0件であること（Union型定義と `step === 'note'` の JSX 条件のみ）

    **削除作業:**
    1. `noteData` state 定義の3行を削除する
    2. step 型 Union から `| 'note'` を削除する
    3. `downloadWithAutoRefresh` 関数定義を削除する（関数全体を削除）
    4. JSX 内に `{step === 'note' && ...}` の条件がある場合はそのブロックも削除する
       （grep で確認してから判断する）

    **注意事項:**
    - `activeNotifIds` state と `GET_NOTIFICATIONS` の呼び出しは削除しない（Phase 16 スコープ）
    - 削除後に TypeScript コンパイルエラーが出ないことを `npm test` で確認する
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - grep で noteData / downloadWithAutoRefresh の参照が0件
    - step 型 Union に 'note' が含まれない
    - npm test がグリーン
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E テスト全件確認（フェーズゲート）</name>
  <files></files>
  <action>
    Phase 15 の完成確認として E2E テスト 13件を実行する。

    前提: port 3003 の Next.js dev サーバーが起動していること（MEMORY.md の注意事項より）。

    実行コマンド: `npm run test:e2e`

    全 13件がパスすることを確認する。
    失敗する場合は、lib/ 抽出（Plan 02）または dead code 削除（Plan 03 Task 1）による
    意図しない動作変更が原因の可能性がある。該当箇所を調査して修正する。

    注意: このタスクは「確認のみ」。E2E テストが GREEN であることがフェーズゲート条件。
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>E2E テスト 13件が全件グリーンで通過する</done>
</task>

</tasks>

<verification>
- `grep -c "noteData\|downloadWithAutoRefresh" app/viewer/page.tsx` が 0
- `grep -c "'note'" app/viewer/page.tsx` の結果確認（step Union と JSX 条件が削除されている）
- `npm test` グリーン
- `npm run test:e2e` 13件グリーン（devサーバー port 3003 が必要）
</verification>

<success_criteria>
- noteData state: page.tsx に定義が存在しない
- downloadWithAutoRefresh: page.tsx に定義が存在しない
- step 型: 'note' を含まない Union 型になっている
- npm test: グリーン
- npm run test:e2e: 13件全件グリーン
- activeNotifIds: Phase 16 スコープのため削除していない（存在したままで正しい）
</success_criteria>

<output>
After completion, create `.planning/phases/15-kodo-seiri/15-03-SUMMARY.md`
</output>
