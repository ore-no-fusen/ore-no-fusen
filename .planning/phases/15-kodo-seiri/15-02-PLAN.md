---
phase: 15-kodo-seiri
plan: "02"
type: execute
wave: 1
depends_on:
  - "15-01"
files_modified:
  - app/viewer/types.ts
  - app/viewer/lib/indexeddb.ts
  - app/viewer/lib/drive.ts
  - app/viewer/page.tsx
autonomous: true
requirements:
  - CLEAN-02

must_haves:
  truths:
    - "app/viewer/types.ts が存在し、IphoneNote / PendingHydrate / DraftRecord / CropModalProps が export されている"
    - "app/viewer/lib/indexeddb.ts が存在し、openDraftsDB / saveDraft / loadAllDrafts / loadDraft / deleteDraft が export されている"
    - "app/viewer/lib/drive.ts が存在し、getAppFolderId / uploadToDrive / downloadFromDrive / uploadWithAutoRefresh / uploadImageToDrive / uploadImageWithAutoRefresh が export されている"
    - "app/viewer/page.tsx が types.ts と lib/ からの import に切り替えられており、元の関数定義行が削除されている"
    - "`npm test` が全件グリーンで通過する（Wave 0 のスタブが GREEN になる）"
  artifacts:
    - path: "app/viewer/types.ts"
      provides: "型定義（IphoneNote, PendingHydrate, DraftRecord, CropModalProps）"
      exports:
        - IphoneNote
        - PendingHydrate
        - DraftRecord
        - CropModalProps
    - path: "app/viewer/lib/indexeddb.ts"
      provides: "IndexedDB CRUD 純粋関数"
      exports:
        - openDraftsDB
        - saveDraft
        - loadAllDrafts
        - loadDraft
        - deleteDraft
    - path: "app/viewer/lib/drive.ts"
      provides: "Drive API 操作関数"
      exports:
        - APP_FOLDER_NAME
        - getAppFolderId
        - uploadToDrive
        - downloadFromDrive
        - uploadWithAutoRefresh
        - uploadImageToDrive
        - uploadImageWithAutoRefresh
        - refreshAccessToken
  key_links:
    - from: "app/viewer/page.tsx"
      to: "app/viewer/types.ts"
      via: "import type { IphoneNote, PendingHydrate, DraftRecord } from './types'"
      pattern: "from './types'"
    - from: "app/viewer/page.tsx"
      to: "app/viewer/lib/indexeddb.ts"
      via: "import { openDraftsDB, saveDraft, ... } from './lib/indexeddb'"
      pattern: "from './lib/indexeddb'"
    - from: "app/viewer/page.tsx"
      to: "app/viewer/lib/drive.ts"
      via: "import { uploadToDrive, ... } from './lib/drive'"
      pattern: "from './lib/drive'"
---

<objective>
Wave 1: types.ts / lib/indexeddb.ts / lib/drive.ts を新規作成し、page.tsx の import を切り替える（CLEAN-02）。

Purpose: 1925行の page.tsx から型定義・IndexedDB操作・Drive操作を純粋関数として抽出し、
page.tsx の責務を UI ロジックのみに絞る土台を作る。
外から見た動作は一切変えない（ロジックのコピー移動のみ）。

Output:
- app/viewer/types.ts（新規）
- app/viewer/lib/indexeddb.ts（新規）
- app/viewer/lib/drive.ts（新規）
- app/viewer/page.tsx（import 付け替え + 元定義削除）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-kodo-seiri/15-RESEARCH.md
@.planning/phases/15-kodo-seiri/15-01-SUMMARY.md

<interfaces>
<!-- app/viewer/page.tsx から抽出する型・関数の一覧。行番号は調査時点のもの。 -->

抽出対象（types.ts へ）:
```typescript
// 266-283行
type IphoneNote = {
  title: string;
  body: string;
  tags: string[];
  images: { fileName: string; mimeType: string; base64: string }[];
}
type PendingHydrate = {
  draftId: string;
  blobMap: Map<string, Blob>;
}

// 359-370行
type DraftRecord = {
  id: string;
  title: string;
  body: string;
  tags: string[];
  images: { fileName: string; blob: Blob }[];
  locked?: boolean;
  sentAt?: string;
  createdAt: string;
  updatedAt: string;
}

// 424-428行
type CropModalProps = { ... }
```

抽出対象（lib/indexeddb.ts へ）:
```typescript
// 355-418行
function openDraftsDB(): Promise<IDBDatabase>
async function saveDraft(draft: DraftRecord): Promise<void>
async function loadAllDrafts(): Promise<DraftRecord[]>
async function loadDraft(id: string): Promise<DraftRecord | null>
async function deleteDraft(id: string): Promise<void>
```

抽出対象（lib/drive.ts へ）:
```typescript
// 105-352行（downloadWithAutoRefresh を除く）
const APP_FOLDER_NAME: string
let cachedFolderId: string | null = null
const LEGACY_FILE_NAMES: Record<string, string>
async function getAppFolderId(token: string): Promise<string>
async function uploadToDrive(token: string, fileName: string, content: string): Promise<void>
async function downloadFromDrive(token: string, fileName: string): Promise<string>
async function uploadWithAutoRefresh(token: string, refreshToken: string, fileName: string, content: string, onTokenRefresh: (t: string) => void): Promise<void>
async function uploadImageToDrive(token: string, file: File, fileName: string): Promise<string>
async function uploadImageWithAutoRefresh(...): Promise<string>
async function refreshAccessToken(refreshToken: string): Promise<string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts 作成 — 型定義を page.tsx から抽出</name>
  <files>app/viewer/types.ts, app/viewer/page.tsx</files>
  <action>
    1. `app/viewer/types.ts` を新規作成する。
       - ファイル先頭に `// app/viewer/types.ts` コメントを入れる
       - `'use client'` は書かない（型定義のみのファイルは不要）
       - page.tsx の 266-283行（IphoneNote, PendingHydrate）と 359-370行（DraftRecord）と 424-428行（CropModalProps）の型定義をそのままコピーし `export type` で export する
       - 行番号がズレている可能性があるため、実際に page.tsx を読んでこれらの型定義を探してコピーする

    2. `app/viewer/page.tsx` を修正する。
       - ファイル冒頭の import 群に `import type { IphoneNote, PendingHydrate, DraftRecord, CropModalProps } from './types';` を追加する
       - 元の型定義行（type IphoneNote = ..., type PendingHydrate = ..., type DraftRecord = ..., type CropModalProps = ...）を削除する

    注意:
    - コードの「移動」のみ。型の内容を変えない。
    - `export type` キーワードを使う（TypeScript の isolatedModules 対応）
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>app/viewer/types.ts に 4つの型が export されており、npm test がグリーンで通過する</done>
</task>

<task type="auto">
  <name>Task 2: lib/indexeddb.ts 作成 + lib/drive.ts 作成 + page.tsx import 切り替え</name>
  <files>app/viewer/lib/indexeddb.ts, app/viewer/lib/drive.ts, app/viewer/page.tsx</files>
  <action>
    **Step A: lib/indexeddb.ts 作成**
    - `app/viewer/lib/indexeddb.ts` を新規作成する
    - ファイル先頭に `// app/viewer/lib/indexeddb.ts` コメント。`'use client'` は書かない。`import React` は書かない。
    - types.ts からの import: `import type { DraftRecord } from '../types';`
    - page.tsx の `openDraftsDB`, `saveDraft`, `loadAllDrafts`, `loadDraft`, `deleteDraft` の関数定義をそのままコピーし、各関数に `export` キーワードを付ける
    - コードの内容は一切変えない（コピー＆export のみ）

    **Step B: lib/drive.ts 作成**
    - `app/viewer/lib/drive.ts` を新規作成する
    - ファイル先頭に `// app/viewer/lib/drive.ts` コメント。`'use client'` は書かない。`import React` は書かない。
    - page.tsx の以下をそのままコピーし export する:
      - `APP_FOLDER_NAME` 定数
      - `cachedFolderId` 変数（モジュールスコープのまま）
      - `LEGACY_FILE_NAMES` 定数（存在する場合）
      - `getAppFolderId`, `uploadToDrive`, `downloadFromDrive`, `uploadWithAutoRefresh`,
        `uploadImageToDrive`, `uploadImageWithAutoRefresh`, `refreshAccessToken`
    - `downloadWithAutoRefresh` は page.tsx に存在する場合でも lib/drive.ts には移動しない（CLEAN-01 で削除対象のため）
    - コードの内容は一切変えない

    **Step C: page.tsx の import 切り替え**
    - page.tsx の import 群の末尾付近に以下を追加する:
      ```typescript
      import { openDraftsDB, saveDraft, loadAllDrafts, loadDraft, deleteDraft } from './lib/indexeddb';
      import {
        APP_FOLDER_NAME, getAppFolderId, uploadToDrive, downloadFromDrive,
        uploadWithAutoRefresh, uploadImageToDrive, uploadImageWithAutoRefresh,
        refreshAccessToken
      } from './lib/drive';
      ```
    - page.tsx から Step A, Step B でコピーした元の関数定義を削除する（型定義は Task 1 で削除済み）
    - 削除した関数が使われている箇所はそのまま維持する（同名関数を import しているため動作変わらず）

    **確認手順（実行前に必ずやること）:**
    - `grep -n "cachedFolderId\|APP_FOLDER_NAME" app/viewer/page.tsx` で変数の実在を確認
    - TypeScript コンパイルエラーがないことを `npm test` で確認
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - app/viewer/lib/indexeddb.ts に 5関数が export されている
    - app/viewer/lib/drive.ts に Drive 操作関数群が export されている
    - app/viewer/page.tsx がこれらを import しており、元の関数定義が削除されている
    - npm test が全件グリーン（Wave 0 のテストスタブも含む）
  </done>
</task>

</tasks>

<verification>
- `ls app/viewer/types.ts app/viewer/lib/indexeddb.ts app/viewer/lib/drive.ts` が全て存在する
- `grep -n "from './types'" app/viewer/page.tsx` に IphoneNote / PendingHydrate / DraftRecord が含まれる
- `grep -n "from './lib/indexeddb'" app/viewer/page.tsx` に openDraftsDB 等が含まれる
- `grep -n "from './lib/drive'" app/viewer/page.tsx` に uploadToDrive 等が含まれる
- `npm test` がグリーン
</verification>

<success_criteria>
- types.ts: IphoneNote, PendingHydrate, DraftRecord, CropModalProps が export されている
- lib/indexeddb.ts: 5関数が export されており、'use client' を含まない
- lib/drive.ts: Drive 操作関数群が export されており、'use client' を含まない
- page.tsx: lib/ からの import に切り替わり、元の関数定義が削除されている
- npm test: 全件グリーン（Wave 0 スタブ含む）
</success_criteria>

<output>
After completion, create `.planning/phases/15-kodo-seiri/15-02-SUMMARY.md`
</output>
