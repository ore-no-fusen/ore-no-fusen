---
phase: 10-pc-notes-to-list
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/viewer/page.tsx
  - app/viewer/__tests__/page.test.tsx
autonomous: true
requirements:
  - REQ-TAG-SUGGEST
  - REQ-TAG-PERSIST

must_haves:
  truths:
    - "タグバーを開くと過去に使ったタグの候補が表示される"
    - "候補タグをタップするだけでタグが追加される（タイプ不要）"
    - "入力フィールドに文字を入れると候補が絞り込まれる"
    - "「iPhoneに置いておく」または「PCに送る」を押すと使用タグが fusen_known_tags に保存される"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "loadKnownTags / mergeKnownTags 関数・knownTags state・サジェストUI"
      contains: "fusen_known_tags"
    - path: "app/viewer/__tests__/page.test.tsx"
      provides: "REQ-TAG-SUGGEST / REQ-TAG-PERSIST テストスタブ"
  key_links:
    - from: "「iPhoneに置いておく」onClick"
      to: "mergeKnownTags(writeTags)"
      via: "saveDraft 呼び出し直前"
      pattern: "mergeKnownTags"
    - from: "「PCに送る」onClick"
      to: "mergeKnownTags(capturedTags)"
      via: "uploadWithAutoRefresh 呼び出し直前"
      pattern: "mergeKnownTags"
    - from: "tagInput onChange"
      to: "filteredSuggestions"
      via: "string.includes フィルタリング"
      pattern: "filteredSuggestions"
---

<objective>
タグ入力UIに既存タグのサジェスト機能を追加する。

Purpose: 過去に使ったタグを再入力なしに選択できるようにし、タグ付けの手間を削減する。
Output: loadKnownTags / mergeKnownTags 関数・knownTags state・サジェストバッジUI・タグ永続化
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-pc-notes-to-list/10-CONTEXT.md
@.planning/phases/10-pc-notes-to-list/10-RESEARCH.md

<interfaces>
<!-- app/viewer/page.tsx の既存タグ関連コード -->

行 1052–1088: タグバー JSX
  - writeTags: string[] — 現在のノートのタグ一覧
  - tagInput: string — 入力フィールドの値
  - setTagInput: Dispatch<SetStateAction<string>>
  - setWriteTags: Dispatch<SetStateAction<string[]>>
  - 入力フィールドは行 1070–1086（<input type="text">）

行 1117–1149: 「iPhoneに置いておく」onClick
  - saveDraft() の直前にタグ永続化を追加する箇所

行 1154–1230: 「PCに送る」onClick
  - uploadWithAutoRefresh() の直前にタグ永続化を追加する箇所
  - capturedTags = [...writeTags] が行 1158 で取得済み

<!-- localStorage スキーマ（新規） -->
キー: fusen_known_tags
値: JSON.stringify(string[]) — 重複なし配列
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: loadKnownTags / mergeKnownTags 実装 + テストスタブ追加</name>
  <files>app/viewer/page.tsx, app/viewer/__tests__/page.test.tsx</files>
  <behavior>
    - loadKnownTags(): string[] — localStorage から fusen_known_tags を読んで返す（parse失敗時は []）
    - mergeKnownTags(newTags: string[]): void — 既存タグと newTags をマージして重複なし配列を保存
    - テストスタブ: loadKnownTags が既存データを返す・mergeKnownTags が重複なくマージする
  </behavior>
  <action>
    **page.tsx: モジュールスコープ関数を追加**

    `getAppFolderId` 関数（行 191）の直前に以下を追加:

    ```typescript
    // タグ永続化ヘルパー
    export function loadKnownTags(): string[] {
      try {
        return JSON.parse(localStorage.getItem('fusen_known_tags') || '[]');
      } catch {
        return [];
      }
    }

    export function mergeKnownTags(newTags: string[]): void {
      const known = loadKnownTags();
      const merged = Array.from(new Set([...known, ...newTags]));
      localStorage.setItem('fusen_known_tags', JSON.stringify(merged));
    }
    ```

    **page.test.tsx: テストスタブ追加**

    既存のテストブロック末尾に追記:

    ```typescript
    import { loadKnownTags, mergeKnownTags } from '../page';

    describe('REQ-TAG-PERSIST: タグ永続化', () => {
      beforeEach(() => localStorage.clear());

      it('loadKnownTags は空のとき [] を返す', () => {
        expect(loadKnownTags()).toEqual([]);
      });
      it('loadKnownTags は保存済みタグを返す', () => {
        localStorage.setItem('fusen_known_tags', JSON.stringify(['仕事', '買い物']));
        expect(loadKnownTags()).toEqual(['仕事', '買い物']);
      });
      it('mergeKnownTags は重複なくマージする', () => {
        localStorage.setItem('fusen_known_tags', JSON.stringify(['仕事']));
        mergeKnownTags(['仕事', '買い物']);
        expect(loadKnownTags()).toEqual(['仕事', '買い物']);
      });
      it('mergeKnownTags は空配列でも既存タグを保持する', () => {
        localStorage.setItem('fusen_known_tags', JSON.stringify(['仕事']));
        mergeKnownTags([]);
        expect(loadKnownTags()).toEqual(['仕事']);
      });
    });

    describe('REQ-TAG-SUGGEST: タグサジェスト', () => {
      it.todo('tagInput が空のとき全 knownTags（最大10件）が候補として表示される');
      it.todo('tagInput に入力すると knownTags の includes フィルタリング結果が表示される');
      it.todo('候補タグをタップすると writeTags に追加され tagInput がクリアされる');
    });
    ```

    **注意:** page.tsx は 'use client' でクライアントコンポーネントだが、
    モジュールスコープの純粋関数は vitest の jsdom 環境で import・テスト可能。
    localStorage は vitest の jsdom に組み込まれているため、モック不要。
  </action>
  <verify>
    <automated>npx vitest run app/viewer/__tests__/page.test.tsx -t "タグ永続化"</automated>
  </verify>
  <done>
    - loadKnownTags / mergeKnownTags が export されている
    - タグ永続化テスト4件がグリーン
    - タグサジェストテストは todo のまま（Task 2 実装後に通る）
  </done>
</task>

<task type="auto">
  <name>Task 2: タグサジェストUI + 保存・送信時の永続化フック</name>
  <files>app/viewer/page.tsx</files>
  <action>
    **変更1: knownTags state の追加**

    ViewerPage コンポーネント内の state 宣言部（tagInput state の近く）に追加:

    ```typescript
    const [knownTags, setKnownTags] = useState<string[]>([]);
    ```

    **変更2: タグバー表示時に knownTags を読み込む**

    showTagBar の useEffect または 🏷️ボタンの onClick で knownTags をロードする。
    🏷️ボタンの onClick（行 1033 付近）を以下に変更:

    ```typescript
    onClick={() => {
      if (!showTagBar) {
        setKnownTags(loadKnownTags());
      }
      setShowTagBar((prev) => !prev);
    }}
    ```

    **変更3: タグバー JSX にサジェストバッジを追加**

    タグバー内の `<input>` タグ（行 1070）の直後に以下を追加:

    ```tsx
    {/* サジェスト候補 */}
    {(() => {
      const filtered = knownTags
        .filter((t) => !writeTags.includes(t) && t.includes(tagInput))
        .slice(0, 10);
      if (filtered.length === 0) return null;
      return (
        <div className="w-full flex flex-wrap gap-1 mt-1">
          {filtered.map((t) => (
            <button
              key={t}
              type="button"
              className="bg-gray-100 text-gray-700 text-xs rounded-full px-2 py-0.5 hover:bg-blue-100 hover:text-blue-700"
              onClick={() => {
                if (!writeTags.includes(t)) {
                  setWriteTags((prev) => [...prev, t]);
                }
                setTagInput('');
              }}
            >
              {t}
            </button>
          ))}
        </div>
      );
    })()}
    ```

    **変更4: 「iPhoneに置いておく」onClick に mergeKnownTags を追加**

    行 1126 の `await saveDraft({` の直前に追加:

    ```typescript
    mergeKnownTags(writeTags);
    ```

    **変更5: 「PCに送る」onClick に mergeKnownTags を追加**

    行 1179（`try {` の直後）の `const { title, body: extractedBody } = extractTitleBody(rawText);` の前に追加:

    ```typescript
    mergeKnownTags(capturedTags);
    ```
  </action>
  <verify>
    <automated>npx vitest run app/viewer/__tests__/page.test.tsx</automated>
  </verify>
  <done>
    - 🏷️ボタンをタップするたびに loadKnownTags() で候補が最新化される
    - タグバー内に入力フィールドの下にサジェスト候補バッジが表示される（knownTags - writeTags のフィルタ）
    - 入力に応じて候補が絞り込まれる（string.includes）
    - 候補バッジをタップすると writeTags に追加・tagInput がクリアされる
    - 「iPhoneに置いておく」と「PCに送る」の両方で mergeKnownTags が呼ばれる
    - vitest グリーン・E2E 13件グリーン
  </done>
</task>

</tasks>

<verification>
npx vitest run app/viewer/__tests__/page.test.tsx
npx playwright test
</verification>

<success_criteria>
- loadKnownTags / mergeKnownTags が export されており vitest でテスト可能
- タグ永続化テスト4件がグリーン
- タグバーに knownTags のサジェスト候補が表示される
- 「iPhoneに置いておく」と「PCに送る」の両方でタグが fusen_known_tags に保存される
- 既存 E2E テスト 13件がグリーンを維持する
</success_criteria>

<output>
After completion, create `.planning/phases/10-pc-notes-to-list/10-02-SUMMARY.md`
</output>
