---
phase: 10-pc-notes-to-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/viewer/page.tsx
  - app/viewer/__tests__/page.test.tsx
autonomous: true
requirements:
  - REQ-CB-LINE
  - REQ-CB-TOGGLE
  - REQ-CB-SERIALIZE
  - REQ-CB-HYDRATE

must_haves:
  truths:
    - "行の途中にカーソルがあっても☑ボタンを押すと行頭に `- [ ] ` が挿入される"
    - "hydrateEditor で `- [ ] text` / `- [x] text` が <input type='checkbox'> を含むspanに変換される"
    - "チェックボックスをタップするとON/OFFが切り替わる"
    - "serializeEditor がチェックボックスDOMを `- [ ] text` / `- [x] text` に正しく逆変換する"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "insertCheckboxAtLineStart関数・hydrateEditor拡張・serializeEditor拡張"
      contains: "insertCheckboxAtLineStart"
    - path: "app/viewer/__tests__/page.test.tsx"
      provides: "REQ-CB-LINE / REQ-CB-TOGGLE / REQ-CB-SERIALIZE / REQ-CB-HYDRATE テストスタブ"
  key_links:
    - from: "☑ボタン onClick"
      to: "insertCheckboxAtLineStart()"
      via: "直接呼び出し"
      pattern: "insertCheckboxAtLineStart"
    - from: "hydrateEditor"
      to: "<input type='checkbox'>"
      via: "checkMatch 分岐"
      pattern: "data-checkbox-line"
    - from: "serializeEditor walk"
      to: "- [x] / - [ ] 文字列"
      via: "data-checkbox-line 属性チェック"
      pattern: "data-checkbox-line"
---

<objective>
iPhone PWAエディタのチェックボックス機能を改善する。

Purpose: 行頭挿入とインタラクティブトグルにより、PC側と同等のチェックボックス操作感を実現する。
Output: insertCheckboxAtLineStart関数・hydrateEditor拡張・serializeEditor拡張・テストスタブ
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pc-notes-to-list/10-CONTEXT.md
@.planning/phases/10-pc-notes-to-list/10-RESEARCH.md

<interfaces>
<!-- app/viewer/page.tsx の既存ヘルパー（変更対象の周辺コード） -->

行 22–43: serializeEditor(el: HTMLDivElement): string
  - walk(node, isRoot) で TEXT_NODE / Element を再帰処理
  - 現在 INPUT タグは未処理（今回追加箇所）

行 56–67: insertTextAtCursor(text: string): void
  - window.getSelection() で Range 取得 → deleteContents → insertNode

行 86–148: hydrateEditor(el, markdown, blobMap): void
  - lines を 1行ずつ処理。通常テキスト行は span + br に変換（行 141–146）
  - チェックボックス行の分岐は未実装（今回追加箇所）

行 1021–1027: ☑ボタン
  - onClick={() => insertTextAtCursor('- [ ] ')}  ← insertCheckboxAtLineStart() に変更する
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: テストスタブ追加（Wave 0）</name>
  <files>app/viewer/__tests__/page.test.tsx</files>
  <behavior>
    - REQ-CB-LINE: serializeEditor が `- [ ] text` を正しく生成する行頭挿入テスト
    - REQ-CB-HYDRATE: hydrateEditor が `- [ ] text` を data-checkbox-line span + checkbox に変換するテスト
    - REQ-CB-SERIALIZE: serializeEditor が data-checkbox-line span を `- [ ] text` / `- [x] text` に逆変換するテスト
    - REQ-CB-TOGGLE: チェックボックスのチェック状態がシリアライズ結果に反映されるテスト
  </behavior>
  <action>
    既存の page.test.tsx（行 44 以降）にテストスタブを追記する。

    追記内容:

    ```typescript
    describe('REQ-CB-LINE: チェックボックス行頭挿入', () => {
      it.todo('行頭挿入: insertCheckboxAtLineStart が editorRef 直下の先頭ノードに - [ ]  を挿入する');
    });

    describe('REQ-CB-HYDRATE: hydrateEditor チェックボックス変換', () => {
      it('- [ ] text を data-checkbox-line span + unchecked input に変換する', () => {
        const el = document.createElement('div');
        hydrateEditorForTest(el, '- [ ] やること', new Map());
        const cb = el.querySelector('input[type="checkbox"]') as HTMLInputElement;
        expect(cb).not.toBeNull();
        expect(cb.checked).toBe(false);
        expect(el.querySelector('[data-checkbox-line]')).not.toBeNull();
      });
      it('- [x] text を data-checkbox-line span + checked input に変換する', () => {
        const el = document.createElement('div');
        hydrateEditorForTest(el, '- [x] 完了', new Map());
        const cb = el.querySelector('input[type="checkbox"]') as HTMLInputElement;
        expect(cb).not.toBeNull();
        expect(cb.checked).toBe(true);
      });
    });

    describe('REQ-CB-SERIALIZE: serializeEditor チェックボックス逆変換', () => {
      it('unchecked input を - [ ] text に逆変換する', () => {
        const el = document.createElement('div');
        hydrateEditorForTest(el, '- [ ] やること', new Map());
        const result = serializeEditorForTest(el);
        expect(result).toBe('- [ ] やること');
      });
      it('checked input を - [x] text に逆変換する', () => {
        const el = document.createElement('div');
        hydrateEditorForTest(el, '- [ ] やること', new Map());
        const cb = el.querySelector('input[type="checkbox"]') as HTMLInputElement;
        cb.checked = true;
        const result = serializeEditorForTest(el);
        expect(result).toBe('- [x] やること');
      });
    });

    describe('REQ-CB-TOGGLE: チェックボックストグル', () => {
      it.todo('iOS Safari での click イベントで checked 状態が変わる（実機確認）');
    });
    ```

    このテストで使う `hydrateEditorForTest` と `serializeEditorForTest` は Task 2 で export する関数を
    import する形で定義する（テストファイル冒頭に import 文を追加）。

    **現時点ではこれらの関数はまだ export されていないため、テストは MISSING — Wave 0 として
    `it.todo` で定義し、Task 2 実装後に具体的なテストに置き換える方針でよい。**

    実際の追記手順:
    1. ファイル末尾に上記 describe ブロックを追記する
    2. テストが todo のままで `npx vitest run app/viewer/__tests__/page.test.tsx` が0件失敗で通ることを確認
  </action>
  <verify>
    <automated>npx vitest run app/viewer/__tests__/page.test.tsx</automated>
  </verify>
  <done>テストスタブが追加され、既存の todo テストを含めてエラーなく実行できる</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: チェックボックス行頭挿入 + インタラクティブ変換の実装</name>
  <files>app/viewer/page.tsx</files>
  <behavior>
    - insertCheckboxAtLineStart() を追加: editorRef.current が Selection の祖先でない場合は focus() して処理
    - hydrateEditor の通常テキスト行処理（行 141–146）の前にチェックボックス行分岐を追加
    - serializeEditor の walk 関数内で data-checkbox-line 属性を持つ Element を `- [x] ` / `- [ ] ` に逆変換
    - ☑ボタンの onClick を insertCheckboxAtLineStart() に変更（行 1023）
  </behavior>
  <action>
    **変更1: serializeEditor の walk 関数に data-checkbox-line 分岐を追加（行 26 付近）**

    walk 関数の `if (node.nodeType === Node.TEXT_NODE)` の直後、`if (!(node instanceof Element))` の後、
    `mermaidCode` チェックの前に以下を追加:

    ```typescript
    // チェックボックス行（data-checkbox-line を持つ wrapper span）
    if (node instanceof Element && node.hasAttribute('data-checkbox-line')) {
      const cb = node.querySelector('input[type="checkbox"]') as HTMLInputElement | null;
      const text = Array.from(node.childNodes)
        .filter((c) => c.nodeType === Node.TEXT_NODE)
        .map((c) => c.textContent ?? '')
        .join('');
      return `- [${cb?.checked ? 'x' : ' '}] ${text}`;
    }
    ```

    **変更2: hydrateEditor の while ループ内、imgMatch 分岐の直後、通常テキスト行処理の前に追加（行 140 付近）**

    ```typescript
    // チェックボックス行 - [ ] / - [x]
    const checkMatch = line.match(/^- \[([ x])\] (.*)$/);
    if (checkMatch) {
      const checked = checkMatch[1] === 'x';
      const text = checkMatch[2];
      const wrapper = document.createElement('span');
      wrapper.setAttribute('data-checkbox-line', '');
      const cb = document.createElement('input');
      cb.type = 'checkbox';
      cb.checked = checked;
      cb.style.cssText = 'margin-right:4px;pointer-events:auto;vertical-align:middle;';
      cb.addEventListener('mousedown', (e) => e.preventDefault());
      const textNode = document.createTextNode(text);
      wrapper.appendChild(cb);
      wrapper.appendChild(textNode);
      el.appendChild(wrapper);
      el.appendChild(document.createElement('br'));
      i++;
      continue;
    }
    ```

    **変更3: insertCheckboxAtLineStart 関数を insertTextAtCursor の直後（行 67 の後）に追加**

    editorRef はコンポーネント内のため、関数はコンポーネント内のネスト関数として定義する。
    ただし viewer/page.tsx では editorRef はコンポーネント外のモジュールスコープではなく
    ViewerPage コンポーネント内の useRef であるため、既存の insertTextAtCursor の呼び出し箇所
    （行 1023）の onClick を直接 inline 関数に置き換える形で実装する。

    行 1021–1027 の ☑ボタン onClick を以下に変更:

    ```typescript
    onClick={() => {
      const sel = window.getSelection();
      if (!sel || sel.rangeCount === 0) {
        editorRef.current?.focus();
        insertTextAtCursor('- [ ] ');
        return;
      }
      const range = sel.getRangeAt(0);
      // カーソルが editorRef 外にある場合は focus してカーソルを確保
      if (!editorRef.current?.contains(range.commonAncestorContainer)) {
        editorRef.current?.focus();
        insertTextAtCursor('- [ ] ');
        return;
      }
      // カーソルのある行の親ノード（editorRef 直下の span/div）を特定
      let node: Node = range.startContainer;
      while (node.parentNode && !node.parentNode.isEqualNode(editorRef.current)) {
        node = node.parentNode;
      }
      // 行頭に Range をセットして挿入
      const headRange = document.createRange();
      headRange.setStart(node, 0);
      headRange.setEnd(node, 0);
      sel.removeAllRanges();
      sel.addRange(headRange);
      insertTextAtCursor('- [ ] ');
    }}
    ```

    **変更4: テストで使う関数を export する**

    page.tsx はクライアントコンポーネント（'use client'）のためモジュールスコープの関数のみ export 可能。
    `serializeEditor` と `hydrateEditor` はすでにモジュールスコープ関数のため、
    `export` キーワードを追加する（行 22 と 89）:

    ```typescript
    export function serializeEditor(el: HTMLDivElement): string {
    export function hydrateEditor(
    ```

    この export により、テストファイルから直接 import してテストを実行できる。
  </action>
  <verify>
    <automated>npx vitest run app/viewer/__tests__/page.test.tsx</automated>
  </verify>
  <done>
    - serializeEditor / hydrateEditor が export されている
    - hydrateEditor で `- [ ] text` → data-checkbox-line span + unchecked checkbox に変換される
    - hydrateEditor で `- [x] text` → data-checkbox-line span + checked checkbox に変換される
    - serializeEditor で unchecked checkbox span → `- [ ] text` に逆変換される
    - serializeEditor で checked checkbox span → `- [x] text` に逆変換される
    - ☑ボタンの onClick が行頭挿入ロジックに変更されている
    - vitest グリーン
  </done>
</task>

</tasks>

<verification>
npx vitest run app/viewer/__tests__/page.test.tsx
npx playwright test
</verification>

<success_criteria>
- hydrateEditor / serializeEditor が export されており、テストから import できる
- チェックボックス行の双方向変換（markdown ↔ DOM）が vitest でグリーン
- ☑ボタンが insertCheckboxAtLineStart ロジックに変更されている（行 1023）
- 既存 E2E テスト 13件がグリーンを維持する
</success_criteria>

<output>
After completion, create `.planning/phases/10-pc-notes-to-list/10-01-SUMMARY.md`
</output>
