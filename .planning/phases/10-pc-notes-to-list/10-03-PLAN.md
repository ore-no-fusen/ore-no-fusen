---
phase: 10-pc-notes-to-list
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - app/viewer/page.tsx
  - src-tauri/src/lib.rs
autonomous: false
requirements:
  - REQ-FOLDER-CACHE
  - REQ-SEND-PARALLEL
  - REQ-RUST-INTERVAL

must_haves:
  truths:
    - "2回目以降の送信で getAppFolderId が Drive API を呼ばない（キャッシュ使用）"
    - "複数画像がある場合、アップロードが並列実行される"
    - "fusen_from_iphone.json 書き込みと saveToHistory が並列実行される"
    - "送信後5秒以内にPC側に付箋が受信される（ポーリング5秒化）"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "cachedFolderId モジュール変数・Promise.all 並列送信"
      contains: "cachedFolderId"
    - path: "src-tauri/src/lib.rs"
      provides: "ポーリング間隔 5秒"
      contains: "from_secs(5)"
  key_links:
    - from: "getAppFolderId"
      to: "cachedFolderId"
      via: "if (cachedFolderId !== null) return cachedFolderId;"
      pattern: "cachedFolderId"
    - from: "「PCに送る」onClick"
      to: "Promise.all 画像アップロード"
      via: "Array.from(capturedBlobs.entries()).map(...)"
      pattern: "Promise.all"
    - from: "lib.rs ポーリングループ"
      to: "from_secs(5)"
      via: "tokio::time::interval"
      pattern: "from_secs\\(5\\)"
---

<objective>
PCへの送信フローを高速化し、送信からPC受信まで5秒以内を実現する。

Purpose: 逐次処理・キャッシュ欠如・30秒ポーリングの3つのボトルネックを同時に解消する。
Output: cachedFolderId キャッシュ・画像並列アップロード・JSON+履歴並列書き込み・ポーリング5秒化
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-pc-notes-to-list/10-CONTEXT.md
@.planning/phases/10-pc-notes-to-list/10-RESEARCH.md

<interfaces>
<!-- app/viewer/page.tsx の送信フロー（変更対象） -->

行 191–214: getAppFolderId(accessToken: string): Promise<string | null>
  - キャッシュなし（今回追加）

行 1183–1185: 画像アップロード（逐次）
  for (const [fileName, file] of capturedBlobs.entries()) {
    await uploadImageWithAutoRefresh(token, file, fileName);
  }
  → Promise.all に変更

行 1187–1203: fusen_from_iphone.json 書き込みと saveToHistory（逐次）
  await uploadWithAutoRefresh(token, 'fusen_from_iphone.json', { ... });
  const note: IphoneNote = { ... };
  await saveToHistory(token, note);
  → Promise.all に変更

<!-- src-tauri/src/lib.rs のポーリング（変更対象） -->
行 1826: tokio::time::interval(std::time::Duration::from_secs(30))
→ from_secs(5) に変更
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: viewer/page.tsx の送信高速化（キャッシュ + 並列化）</name>
  <files>app/viewer/page.tsx</files>
  <action>
    **変更1: cachedFolderId モジュール変数を追加（行 189 の APP_FOLDER_NAME 定数の直後）**

    ```typescript
    const APP_FOLDER_NAME = 'ore-no-fusen';
    let cachedFolderId: string | null = null;  // ← 追加
    ```

    **変更2: getAppFolderId 関数の先頭にキャッシュチェックを追加（行 191）**

    関数の最初の行（`const res = await fetch(` の直前）に追加:

    ```typescript
    async function getAppFolderId(accessToken: string): Promise<string | null> {
      if (cachedFolderId !== null) return cachedFolderId;  // ← 追加
      const res = await fetch(
        // ... 既存コード ...
    ```

    また既存の return 文を変更してキャッシュに保存する:
    - 行 197 `if (data.files?.[0]?.id) return data.files[0].id as string;`
      → `if (data.files?.[0]?.id) { cachedFolderId = data.files[0].id as string; return cachedFolderId; }`
    - 行 213 `return created.id as string;`
      → `cachedFolderId = created.id as string; return cachedFolderId;`

    **変更3: 画像アップロードを Promise.all で並列化（行 1183–1185）**

    既存の for...of ループを置き換える:

    ```typescript
    // 変更前:
    // for (const [fileName, file] of capturedBlobs.entries()) {
    //   await uploadImageWithAutoRefresh(token, file, fileName);
    // }

    // 変更後:
    await Promise.all(
      Array.from(capturedBlobs.entries()).map(([fileName, file]) =>
        uploadImageWithAutoRefresh(token, file, fileName)
      )
    );
    ```

    **変更4: fusen_from_iphone.json 書き込みと saveToHistory を Promise.all で並列化（行 1186–1203）**

    既存の逐次 await を Promise.all に変更:

    ```typescript
    // 変更前:
    // const fullBody = extractedBody;
    // await uploadWithAutoRefresh(token, 'fusen_from_iphone.json', { ... });
    // const note: IphoneNote = { ... };
    // await saveToHistory(token, note);

    // 変更後:
    const fullBody = extractedBody;
    const note: IphoneNote = {
      id: noteId,
      status: 'sent',
      title,
      body: fullBody,
      created_at: sentAt,
      sent_at: sentAt,
      tags: capturedTags,
    };
    await Promise.all([
      uploadWithAutoRefresh(token, 'fusen_from_iphone.json', {
        id: noteId,
        title,
        body: fullBody,
        sent_at: sentAt,
        tags: capturedTags,
      }),
      saveToHistory(token, note),
    ]);
    ```

    note オブジェクトを Promise.all の前に定義することで両並列タスクが同じ note を参照できる。
  </action>
  <verify>
    <automated>npx vitest run app/viewer/__tests__/page.test.tsx</automated>
  </verify>
  <done>
    - cachedFolderId がモジュール変数として定義されている
    - getAppFolderId 先頭に `if (cachedFolderId !== null) return cachedFolderId;` がある
    - getAppFolderId の両 return 文でキャッシュに保存している
    - 画像アップロードが Promise.all に変更されている
    - fusen_from_iphone.json と saveToHistory が Promise.all に変更されている
    - vitest グリーン
  </done>
</task>

<task type="auto">
  <name>Task 2: Rust ポーリング間隔を 30秒 → 5秒に変更</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
    src-tauri/src/lib.rs の行 1826 を変更する:

    変更前:
    ```rust
    tokio::time::interval(std::time::Duration::from_secs(30));
    ```

    変更後:
    ```rust
    tokio::time::interval(std::time::Duration::from_secs(5));
    ```

    この1行の変更のみ。周辺コードは変更しない。

    変更後、Rust コードがコンパイルできることを確認する:
    ```bash
    cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5
    ```

    コンパイル成功（warning はあってもよい、error がなければOK）を確認する。
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | grep -E "^error" | wc -l</automated>
  </verify>
  <done>
    - lib.rs 行 1826 が from_secs(5) になっている
    - cargo build がエラーなく完了する（0 errors）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    送信高速化の全変更:
    1. getAppFolderId のセッション内キャッシュ（cachedFolderId）
    2. 画像アップロードの並列化（Promise.all）
    3. JSON書き込みと履歴保存の並列化（Promise.all）
    4. Rustポーリング間隔 30秒 → 5秒（要 tauri build）
  </what-built>
  <how-to-verify>
    1. PC側アプリを `npm run tauri build` でリビルドしてインストールする
    2. iPhoneで viewer（PWA）を開き、テキストを入力して「PCに送る」をタップする
    3. タップ後、PCに付箋ウィンドウが開くまでの時間を計測する
    4. 5秒以内に開けば合格
    5. 画像1枚以上含むノートを送信して、送信が成功することも確認する
    6. 同一セッションで2回目の送信を行い、エラーなく送信できることを確認する（キャッシュ動作）
  </how-to-verify>
  <resume-signal>動作確認できたら "approved" と入力。問題があれば内容を説明してください。</resume-signal>
</task>

</tasks>

<verification>
npx vitest run app/viewer/__tests__/page.test.tsx
npx playwright test
cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | grep "^error" | wc -l
</verification>

<success_criteria>
- cachedFolderId がモジュール変数として定義され getAppFolderId でキャッシュされている
- 画像アップロードが Promise.all 並列に変更されている
- fusen_from_iphone.json と saveToHistory が Promise.all 並列に変更されている
- lib.rs 行 1826 が from_secs(5) になっている
- cargo build がエラーなし
- 実機でiPhoneから送信後5秒以内にPCに付箋が表示される（human-verify 承認）
</success_criteria>

<output>
After completion, create `.planning/phases/10-pc-notes-to-list/10-03-SUMMARY.md`
</output>
