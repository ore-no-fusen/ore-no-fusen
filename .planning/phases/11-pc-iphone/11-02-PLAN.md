---
phase: 11-pc-iphone
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - src-tauri/src/lib.rs
  - worker/index.js
autonomous: true
requirements: [P11-SCHEMA, P11-WORKER]

must_haves:
  truths:
    - "fusen_note.json に items 配列として書き込まれる（最新 20 件上限）"
    - "Web Push payload と Drive JSON の両方に id フィールドが含まれる"
    - "通知タグが 'fusen-<note_id>' になり、複数ノートが独立して表示される"
    - "旧形式の fusen_note.json が既に Drive にある場合でも動作が壊れない"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "fusen_send_to_iphone の read-modify-write 配列追加"
      contains: "items"
    - path: "worker/index.js"
      provides: "通知タグ fusen-<id>"
      contains: "fusen-"
  key_links:
    - from: "src-tauri/src/lib.rs fusen_send_to_iphone"
      to: "worker/index.js push handler"
      via: "note_json_push.id → data.id → tag"
      pattern: "fusen-"
---

<objective>
Rust 側の `fusen_send_to_iphone` を配列スキーマ（read-modify-write）に変更し、worker.js の通知タグを `'fusen-<id>'` に変更する。

Purpose: 複数ノートをロック画面に独立して表示できるようにするため、通知タグをノート固有 ID ベースにする。同時に Drive の fusen_note.json を複数件保持できる配列構造に変える。
Output: lib.rs（配列化）、worker/index.js（タグ変更）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-pc-iphone/11-CONTEXT.md
@.planning/phases/11-pc-iphone/11-RESEARCH.md

<interfaces>
<!-- src-tauri/src/lib.rs line 1240〜1391: fusen_send_to_iphone の現行実装 -->
<!-- 変更箇所は以下の 2 か所のみ -->

<!-- 1) note_json_drive / note_json_push に id を追加（line 1284-1295） -->
<!-- 現行: -->
let note_json_drive = serde_json::json!({
    "title": title,
    "body": body_rich,
    "tags": [],
    "sent_at": sent_at
});
let note_json_push = serde_json::json!({
    "title": title,
    "body": body_push,
    "tags": [],
    "sent_at": sent_at
});

<!-- 変更後: uuid クレートは Cargo.toml 既存依存 -->
let note_id = uuid::Uuid::new_v4().to_string();
let note_json_drive = serde_json::json!({
    "id": note_id,
    "title": title,
    "body": body_rich,
    "tags": [],
    "sent_at": sent_at,
    "received_at": null
});
let note_json_push = serde_json::json!({
    "id": note_id,
    "title": title,
    "body": body_push,
    "tags": [],
    "sent_at": sent_at
});

<!-- 2) Drive アップロード部分を read-modify-write に変更（line 1346-1354） -->
<!-- 現行: -->
tokio::spawn(async move {
    if let Err(e) = gdrive::upload_json(&bg_client, &bg_token, "fusen_note.json", &note_json_drive).await {
        eprintln!("[iphone] Drive upload error: {}", e);
    }
});

<!-- 変更後: -->
tokio::spawn(async move {
    let mut items: Vec<serde_json::Value> = match gdrive::download_json(&bg_client, &bg_token, "fusen_note.json").await {
        Ok(v) => v["items"].as_array().cloned().unwrap_or_default(),
        Err(_) => vec![],
    };
    items.push(note_json_drive);
    if items.len() > 20 {
        let start = items.len() - 20;
        items = items[start..].to_vec();
    }
    let data = serde_json::json!({ "items": items });
    if let Err(e) = gdrive::upload_json(&bg_client, &bg_token, "fusen_note.json", &data).await {
        eprintln!("[iphone] Drive upload error: {}", e);
    }
});

<!-- worker/index.js: tag 変更箇所 -->
<!-- push イベント: tag: 'fusen' → tag: 'fusen-' + (data.id ?? 'unknown') -->
<!-- data オブジェクトに id が含まれるよう showNotification の data も id を保持 -->
<!-- notificationclick: tag と targetUrl に id を使用 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib.rs — note_json に id 追加 + Drive read-modify-write 配列化</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
    fusen_send_to_iphone 関数（line 1240 付近）を以下の最小変更で修正する。

    **変更 1: note_json_drive / note_json_push に id フィールドを追加（line 1264 付近に note_id 生成を追加）**

    `let sent_at = chrono::Utc::now().to_rfc3339();` の直後に以下を追加:
    ```rust
    let note_id = uuid::Uuid::new_v4().to_string();
    ```

    note_json_drive に `"id": note_id.clone()` と `"received_at": null` を追加。
    note_json_push に `"id": note_id` を追加。

    **変更 2: Drive アップロード部分（line 1346-1354）を read-modify-write に変更**

    既存の `tokio::spawn` ブロックを RESEARCH.md の「Rust 側の read-modify-write パターン」で示したコードに置き換える:
    - `download_json` で既存 items を取得（失敗時は空 Vec）
    - `items.push(note_json_drive)` で末尾に追加
    - 20 件超過時は古いものから削除（`items = items[start..].to_vec()`）
    - `{ "items": items }` として upload_json

    Rust コンパイルエラーがないことを `cargo check` で確認する（テストは不要）。
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <done>cargo check が warning のみ（error なし）で通る。note_json_drive に "id" と "received_at": null が含まれ、Drive upload が read-modify-write になっている</done>
</task>

<task type="auto">
  <name>Task 2: worker/index.js — 通知タグを 'fusen-<id>' に変更</name>
  <files>worker/index.js</files>
  <action>
    worker/index.js を以下の最小変更で修正する。

    **push イベントハンドラ（line 5-18）:**
    - `data: { title, body }` を `data: { id: data.id ?? 'unknown', title, body }` に変更（notificationclick で id を参照できるよう）
    - `tag: 'fusen'` を `tag: 'fusen-' + (data.id ?? 'unknown')` に変更

    **notificationclick ハンドラ（line 20-46）:**
    - `const { title, body } = event.notification.data || {};` を `const { id, title, body } = event.notification.data || {};` に変更
    - `tag: 'fusen'` を `tag: 'fusen-' + (id ?? 'unknown')` に変更（再表示用 showNotification）
    - `const targetUrl = self.location.origin + '/viewer?note=1';` を `const targetUrl = self.location.origin + '/viewer?note=' + (id ?? 'unknown');` に変更

    変更後のファイルが構文エラーなしであることを確認（Node.js で parse が通ること）。
  </action>
  <verify>
    <automated>node --check /d/Users/uck/Documents/curry-project/ore-no-fusen/worker/index.js && echo "syntax OK"</automated>
  </verify>
  <done>worker/index.js が構文エラーなし。push タグが 'fusen-<id>'、notificationclick の targetUrl が '/viewer?note=<id>' になっている</done>
</task>

</tasks>

<verification>
- cargo check が error なしで通る
- worker/index.js が node --check で構文 OK
- npx vitest run app/viewer/viewer.test.tsx — 0 failures（既存テスト保護）
</verification>

<success_criteria>
- lib.rs: note_json_drive に "id"（UUID）と "received_at": null が含まれ、Drive アップロードが read-modify-write 配列追加になっている
- lib.rs: note_json_push に "id" が含まれ、Web Push payload に id が乗る
- worker/index.js: push 通知タグが 'fusen-<note_id>'、targetUrl が '/viewer?note=<id>'
- 既存ビルドが壊れていない（cargo check green）
</success_criteria>

<output>
After completion, create `.planning/phases/11-pc-iphone/11-02-SUMMARY.md`
</output>
