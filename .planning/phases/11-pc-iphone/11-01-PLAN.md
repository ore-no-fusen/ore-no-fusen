---
phase: 11-pc-iphone
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/viewer/viewer.test.tsx
autonomous: true
requirements: [P11-SCHEMA, P11-SAVE, P11-LIST, P11-WORKER, P11-DISMISS]

must_haves:
  truths:
    - "P11-01〜04 のテストスタブが viewer.test.tsx に存在し、vitest run が通る（スキップ扱い）"
    - "worker 通知タグに関するスタブが追加されている"
  artifacts:
    - path: "app/viewer/viewer.test.tsx"
      provides: "Wave 0 テストスタブ（P11-01〜04）"
      contains: "P11-01"
  key_links:
    - from: "app/viewer/viewer.test.tsx"
      to: "npx vitest run app/viewer/viewer.test.tsx"
      via: "describe/it.todo"
      pattern: "P11-0"
---

<objective>
Phase 11 の全要件に対するテストスタブを viewer.test.tsx に追加する（Nyquist ルール準拠）。

Purpose: Wave 1 実装が GREEN にすべきテストを RED 状態で先行定義し、実装順序を強制する。
Output: viewer.test.tsx に 4 つのスタブブロック（P11-01〜04）が追加された状態
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-pc-iphone/11-CONTEXT.md
@.planning/phases/11-pc-iphone/11-RESEARCH.md
@.planning/phases/11-pc-iphone/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: viewer.test.tsx に P11-01〜04 スタブを追加</name>
  <files>app/viewer/viewer.test.tsx</files>
  <behavior>
    - P11-01: DraftRecord に received_pc?: true フィールドを追加して saveDraft で保存できる（it.todo）
    - P11-02: loadAllDrafts が received_pc: true のレコードを返し、IphoneNote.status を 'received_pc' にマッピングできる（it.todo）
    - P11-03: fusen_note.json の配列スキーマ（items: [...]）を受け取り、received_at が null の件のみフィルタできる。旧スキーマ（単体オブジェクト）は互換処理して1件として扱う（it.todo）
    - P11-04: worker/index.js の push イベントで通知タグが 'fusen-<id>' になる（it.todo）
  </behavior>
  <action>
    viewer.test.tsx の末尾（line 275 以降）に以下の describe ブロックを追加する。既存コードは一切変更しない。

    追加する内容：

    ```typescript
    // ============================================================
    // Phase 11: PC→iPhone受信履歴保存
    // ============================================================

    describe('P11-01: DraftRecord received_pc フラグ', () => {
      it.todo('received_pc: true を持つ DraftRecord を saveDraft で保存できる');
      it.todo('保存した DraftRecord を loadAllDrafts で取得すると received_pc が true になっている');
    });

    describe('P11-02: IphoneNote.status received_pc マッピング', () => {
      it.todo('received_pc: true の DraftRecord は IphoneNote.status が received_pc になる');
      it.todo('received_pc が undefined の DraftRecord は IphoneNote.status が draft になる');
    });

    describe('P11-03: fusen_note.json 配列スキーマ互換', () => {
      it.todo('items 配列スキーマで received_at が null の件のみフィルタして返す');
      it.todo('旧スキーマ（title/body 直接）は 1 件の配列として互換処理する');
      it.todo('items が空配列の場合は空配列を返す');
    });

    describe('P11-04: worker 通知タグ fusen-<id>', () => {
      it.todo('push イベントで data.id が存在する場合 tag が fusen-<id> になる');
      it.todo('push イベントで data.id が undefined の場合 tag が fusen-unknown になる');
    });
    ```

    注意: `it.todo` はボディなしで記述する（Vitest は todo をスキップ扱いにするので現時点でテストは通る）。
  </action>
  <verify>
    <automated>npx vitest run app/viewer/viewer.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>npx vitest run app/viewer/viewer.test.tsx が 0 failures で完了し、P11-01〜04 の describe ブロックが存在する</done>
</task>

</tasks>

<verification>
npx vitest run app/viewer/viewer.test.tsx — 0 failures（todo はスキップ）
</verification>

<success_criteria>
- viewer.test.tsx に P11-01〜04 のスタブ describe ブロックが追加されている
- npx vitest run が失敗なしで通る
- 既存テストは変更・削除されていない
</success_criteria>

<output>
After completion, create `.planning/phases/11-pc-iphone/11-01-SUMMARY.md`
</output>
