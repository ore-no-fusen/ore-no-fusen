---
phase: 11-pc-iphone
plan: 03
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - app/viewer/page.tsx
autonomous: true
requirements: [P11-SAVE, P11-LIST, P11-DISMISS]

must_haves:
  truths:
    - "通知タップ後（?note=<id> パラメータ受信後）に Drive から全未読 items を取得して IndexedDB に一括保存し、タップしたノートを write ステップに直接表示する"
    - "一覧に 'received_pc' バッジ（水色）が表示される"
    - "一覧から received_pc ノートをタップすると write ステップで編集できる（pendingHydrate パターン）"
    - "received_pc ノートも一覧から 🗑️ で削除できる"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "型拡張・note 受信フロー・一覧表示"
      contains: "received_pc"
  key_links:
    - from: "?note=<id> パラメータ受信 useEffect"
      to: "IndexedDB saveDraft → setPendingHydrate → setStep('write')"
      via: "downloadFusenNoteItems → for...of saveDraft → items.find(id)"
      pattern: "received_pc"
    - from: "list ステップの loadAllDrafts"
      to: "IphoneNote.status received_pc"
      via: "d.received_pc ? 'received_pc' : 'draft'"
      pattern: "received_pc"
---

<objective>
viewer/page.tsx に PC→iPhone 受信フローを実装する。通知タップ後に Drive から全未読ノートを IndexedDB に保存し、タップしたノートを write に直接表示する。一覧には 'received_pc' バッジで表示・編集・削除できる。

Purpose: 通知をタップしたらすぐそのノートの内容が見える。一覧を経由する必要がない。
Output: viewer/page.tsx の型・フロー・UI を変更
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-pc-iphone/11-CONTEXT.md
@.planning/phases/11-pc-iphone/11-RESEARCH.md

<interfaces>
<!-- 既存型定義（app/viewer/page.tsx line 239-254） -->
type IphoneNote = {
  id: string;
  status: 'sent' | 'draft';           // ← 'received_pc' を追加
  title: string;
  body: string;
  created_at: string;
  sent_at?: string;
  tags?: string[];
};

type DraftRecord = {
  id: string;
  title: string;
  body: string;
  created_at: string;
  images: { fileName: string; blob: Blob }[];
  tags?: string[];
  // ← received_pc?: true を追加
};

<!-- 既存関数（そのまま再利用） -->
saveDraft(draft: DraftRecord): Promise<void>
loadAllDrafts(): Promise<DraftRecord[]>
deleteDraft(id: string): Promise<void>
downloadWithAutoRefresh(token: string): Promise<{ title: string; body: string }>
uploadWithAutoRefresh(token: string, fileName: string, data: object): Promise<void>
downloadFromDrive(token: string, fileName: string): Promise<any>
refreshAccessToken(): Promise<string | null>

<!-- 通知タップフロー（line 698-742） — 2 か所変更が必要 -->
// 1) ?note= 直接アクセス（line 709-721）
downloadWithAutoRefresh(token)
  .then((data) => {
    setNoteData(data);
    setStep('note');  // ← downloadFusenNoteItems + saveDraft + setStep('list') に変更
  })

// 2) pending_note（OAuth 再リダイレクト後、line 730-742）— 同様に変更

<!-- 一覧ロード（list ステップの useEffect、line 768-771 付近） -->
const draftNotes: IphoneNote[] = drafts.map((d) => ({
  id: d.id, title: d.title, body: d.body,
  status: 'draft' as const, created_at: d.created_at, tags: d.tags,
}));
// ↑ status を d.received_pc ? 'received_pc' : 'draft' に変更

<!-- note ステップの「消す」ボタン（line 1588-1600） -->
// ← 「通知を消して一覧へ」に変更（received_at 書き戻し + list 遷移）

<!-- list ステップのバッジ表示（line 1530-1536） -->
// note.status === 'sent': bg-blue-500 text-white
// note.status === 'draft': bg-yellow-400 text-gray-900
// ← 'received_pc' ケースを追加: bg-blue-100 text-blue-700「PC受信」

<!-- 削除ボタン表示条件（line 1546） -->
// 現行: note.status === 'draft'
// ← note.status === 'draft' || note.status === 'received_pc' に変更

<!-- 一覧タップ（line 1505-1524） -->
// 現行: sent は setPendingHydrate draftId: null, draft は loadDraft して draftId: note.id
// ← received_pc も draft と同様: loadDraft して draftId: note.id（pendingHydrate パターン）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 型拡張 + 通知タップ受信フロー（IndexedDB 一括保存 → list 遷移）</name>
  <files>app/viewer/page.tsx</files>
  <behavior>
    - DraftRecord に `received_pc?: true` フィールドが追加されている
    - IphoneNote.status に `'received_pc'` が追加されている
    - `downloadFusenNoteItems(token)` ヘルパー関数が追加されている（配列スキーマ + 旧スキーマ互換）
    - 通知タップ時（?note=<id> パラメータ）に downloadFusenNoteItems → for...of saveDraft → タップした id のノートを pendingHydrate → setStep('write') で動作する
    - OAuth 再リダイレクト後の pending_note フローも同様に変更されている
    - FusenNoteItem 型（id, title, body, sent_at, received_at）が定義されている
  </behavior>
  <action>
    1. DraftRecord 型に `received_pc?: true` を追加（line 339-346 付近）。

    2. IphoneNote 型の status を `'sent' | 'draft' | 'received_pc'` に変更（line 241）。

    3. downloadWithAutoRefresh の下（line 333 付近）に FusenNoteItem 型と downloadFusenNoteItems 関数を追加:

    ```typescript
    type FusenNoteItem = {
      id: string;
      title: string;
      body: string;
      sent_at: string;
      received_at: string | null;
    };

    async function downloadFusenNoteItems(token: string): Promise<FusenNoteItem[]> {
      const data = await downloadFromDrive(token, 'fusen_note.json').catch(() =>
        refreshAccessToken().then((t) => {
          if (!t) throw new Error('session expired');
          return downloadFromDrive(t, 'fusen_note.json');
        })
      );
      if (Array.isArray(data?.items)) {
        return data.items.filter((item: FusenNoteItem) => item.received_at == null);
      }
      // 旧スキーマ（単体オブジェクト）互換
      if (data?.title || data?.body) {
        return [{
          id: data.sent_at ?? 'legacy',
          title: data.title ?? '',
          body: data.body ?? '',
          sent_at: data.sent_at ?? '',
          received_at: null,
        }];
      }
      return [];
    }
    ```

    4. ?note= パラメータ処理（line 709-721）の `downloadWithAutoRefresh` 呼び出しを以下に変更:
    ```typescript
    downloadFusenNoteItems(token)
      .then(async (items) => {
        // 全未読を IndexedDB に一括保存
        for (const item of items) {
          await saveDraft({
            id: item.id,
            title: item.title,
            body: item.body,
            created_at: item.sent_at,
            images: [],
            tags: [],
            received_pc: true,
          });
        }
        // タップされた note_id のノートを write に直接表示
        const tappedId = new URLSearchParams(window.location.search).get('note');
        const tapped = items.find((item) => item.id === tappedId) ?? items[0];
        if (tapped) {
          const titleLine = tapped.title ? `# ${tapped.title}\n\n` : '';
          setPendingHydrate({
            markdown: titleLine + tapped.body,
            blobMap: new Map(),
            draftId: tapped.id,
            tags: [],
          });
        }
        setStep('write');
      })
      .catch(() => {
        localStorage.removeItem('viewer_access_token');
        setErrorMessage('セッションが切れました。再度ログインしてください。');
        setStep('login');
      })
      .finally(() => setIsLoading(false));
    ```

    5. pending_note フロー（line 730-742）も同様に downloadFusenNoteItems + pendingHydrate に変更する。pending_note には note_id が含まれているため、items.find で同様に特定する。

    6. `setNoteData` / `noteData` state は note ステップのコードに残っているが、通知タップフローからは参照されなくなる。既存の note ステップ UI はそのまま残す（削除不要）。
  </action>
  <verify>
    <automated>npx vitest run app/viewer/viewer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>型コンパイルエラーなし（npx tsc --noEmit が通る）。vitest 0 failures。DraftRecord に received_pc フィールドあり。downloadFusenNoteItems が旧・新スキーマ両対応</done>
</task>

<task type="auto">
  <name>Task 2: 一覧表示（received_pc バッジ・削除・タップ）+ 「通知を消して一覧へ」ボタン</name>
  <files>app/viewer/page.tsx</files>
  <action>
    **1. list ステップの drafts → IphoneNote マッピング変更（line 768-771 付近）:**

    ```typescript
    const draftNotes: IphoneNote[] = drafts.map((d) => ({
      id: d.id, title: d.title, body: d.body,
      status: d.received_pc ? ('received_pc' as const) : ('draft' as const),
      created_at: d.created_at, tags: d.tags,
    }));
    ```

    **2. 一覧バッジ表示（line 1530-1536 付近）に received_pc ケースを追加:**

    既存の三項演算子を以下に変更:
    ```typescript
    className={`text-sm font-semibold px-2 py-0.5 rounded ${
      note.status === 'sent'
        ? 'bg-blue-500 text-white'
        : note.status === 'received_pc'
        ? 'bg-blue-100 text-blue-700'
        : 'bg-yellow-400 text-gray-900'
    }`}
    ```

    バッジラベルも変更:
    ```typescript
    {note.status === 'sent'
      ? t('pwa.statusSent')
      : note.status === 'received_pc'
      ? 'PC受信'
      : t('pwa.statusDraft')}
    ```

    **3. 削除ボタン表示条件（line 1546）を拡張:**
    ```typescript
    {(note.status === 'draft' || note.status === 'received_pc') && (
      // 削除ボタン（既存コードのまま）
    )}
    ```

    また、削除後の historyNotes 再ロードで received_pc も正しくマッピングされるよう、削除ハンドラ内の `setHistoryNotes` も上記の received_pc マッピングを適用する（`d.received_pc ? 'received_pc' : 'draft'`）。

    **4. 一覧タップハンドラ（line 1505-1524）に received_pc ケースを追加:**

    現行は `note.status === 'draft'` の分岐のみ。received_pc は draft と同じ扱い（loadDraft + blobMap + draftId: note.id）:
    ```typescript
    if (note.status === 'draft' || note.status === 'received_pc') {
      const draft = await loadDraft(note.id).catch(() => null);
      // ...以下は既存コードのまま
      setPendingHydrate({ markdown: fullText, blobMap, draftId: note.id, tags: note.tags ?? [] });
    } else {
      // sent の処理（既存のまま）
      setPendingHydrate({ markdown: fullText, blobMap: new Map(), draftId: null, tags: note.tags ?? [] });
    }
    setStep('write');
    ```

    **5. note ステップの「消す」ボタン（line 1588-1600）を「通知を消して一覧へ」に変更:**

    ```typescript
    onClick={async () => {
      // 1. 全通知クローズ（タグ指定なし → 全件）
      navigator.serviceWorker.ready.then((reg) => {
        reg.getNotifications().then((ns) => ns.forEach((n) => n.close()));
      });
      // 2. Drive の fusen_note.json 全 items に received_at を付けて書き戻す
      if (accessToken) {
        try {
          const data = await downloadFromDrive(accessToken, 'fusen_note.json');
          const items = (Array.isArray(data?.items) ? data.items : []).map(
            (item: FusenNoteItem) => ({
              ...item,
              received_at: item.received_at ?? new Date().toISOString(),
            })
          );
          await uploadWithAutoRefresh(accessToken, 'fusen_note.json', { items });
        } catch { /* エラーは無視 */ }
      }
      // 3. list へ
      setStep('list');
    }}
    ```

    ボタンラベルを `{t('pwa.deleteNote')}` から `通知を消して一覧へ` に変更。
    ボタン下にサブテキストを追加:
    ```tsx
    <p className="text-sm text-gray-400 mt-1">→ 一覧に履歴として残ります</p>
    ```

    **注意:** accessToken state がこのスコープで参照できることを確認すること（既存コードでは参照可能なはず）。
  </action>
  <verify>
    <automated>npx vitest run app/viewer/viewer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>vitest 0 failures。一覧に received_pc バッジの JSX が存在する。「通知を消して一覧へ」ボタンと received_at 書き戻しロジックが実装されている。npx tsc --noEmit が通る</done>
</task>

</tasks>

<verification>
- npx vitest run — 0 failures
- npx tsc --noEmit — 0 errors（TypeScript 型チェック）
- viewer/page.tsx に received_pc, downloadFusenNoteItems, 「通知を消して一覧へ」が含まれる
</verification>

<success_criteria>
- 通知タップ（?note=<id>）で Drive から全未読 items を取得 → IndexedDB に received_pc: true で保存 → タップした id のノートを write に直接表示
- 一覧に received_pc（水色バッジ「PC受信」）が表示される
- received_pc ノートをタップすると write で編集できる（draftId 維持）
- received_pc ノートを削除できる
- 旧スキーマ（単体 title/body）も互換処理される
</success_criteria>

<output>
After completion, create `.planning/phases/11-pc-iphone/11-03-SUMMARY.md`
</output>
