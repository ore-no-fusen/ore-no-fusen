---
phase: 11-pc-iphone
plan: 04
type: execute
wave: 2
depends_on: [11-02, 11-03]
files_modified: []
autonomous: false
requirements: [P11-SCHEMA, P11-WORKER, P11-SAVE, P11-LIST, P11-DISMISS]

must_haves:
  truths:
    - "PCから「iPhoneに送る」で送信すると、iPhoneロック画面に通知が届く"
    - "通知をタップすると一覧に「PC受信」バッジのノートが表示される"
    - "複数回送信すると、それぞれ独立した通知がロック画面に表示される"
    - "「通知を消して一覧へ」ボタンが機能する"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "全実装済み"
      contains: "received_pc"
    - path: "worker/index.js"
      provides: "通知タグ変更済み"
      contains: "fusen-"
    - path: "src-tauri/src/lib.rs"
      provides: "配列スキーマ送信"
      contains: "items"
  key_links:
    - from: "PC「iPhoneに送る」"
      to: "iPhone ロック画面通知（fusen-<id> タグ）"
      via: "lib.rs → WebPush → worker.js"
      pattern: "fusen-"
    - from: "通知タップ"
      to: "IndexedDB received_pc ノート → 一覧"
      via: "downloadFusenNoteItems → saveDraft → list"
      pattern: "received_pc"
---

<objective>
Phase 11 の全実装を実機（iPhone + PC Tauri アプリ）で統合検証する。

Purpose: 自動テストでは確認できない実機 Push 通知・複数通知独立表示・UI 表示を人間が確認する。
Output: 動作確認済み（手動 approve）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-pc-iphone/11-CONTEXT.md
@.planning/phases/11-pc-iphone/11-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全自動テストとビルドチェック</name>
  <files></files>
  <action>
    以下を順に実行してすべて通ることを確認する:
    1. `npx vitest run` — 全テストスイート
    2. `npx tsc --noEmit` — TypeScript 型エラーなし
    3. `cargo check --manifest-path src-tauri/Cargo.toml` — Rust コンパイルエラーなし

    いずれかが失敗した場合は修正してから次のタスクへ進む。
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen && npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <done>vitest 0 failures、tsc 0 errors、cargo check error なし</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 11 の全実装:
    - Rust: fusen_send_to_iphone が fusen_note.json を配列スキーマ（{ items: [...] }）で書き込む
    - worker.js: 通知タグが 'fusen-<note_id>' に変更済み
    - viewer/page.tsx: 通知タップ → IndexedDB 保存 → 一覧表示（「PC受信」水色バッジ）
    - viewer/page.tsx: 「通知を消して一覧へ」ボタンが received_at を書き戻して list へ遷移
  </what-built>
  <how-to-verify>
    **前提: Tauri アプリを再ビルド（`npm run tauri build` または dev 起動）し、iPhone PWA を最新の Vercel デプロイで開く**

    **テスト 1: 基本フロー（1 件送信）**
    1. PC の付箋ウィンドウで「iPhoneに送る」を実行
    2. iPhone のロック画面に通知が届くことを確認（タグが 'fusen-xxx' になっていることは直接確認不要）
    3. 通知をタップ → viewer が開く
    4. 一覧（list ステップ）に「PC受信」の水色バッジのノートが表示されることを確認
    5. ノートをタップ → write ステップで内容が読み込まれることを確認
    6. 「iPhoneに置いておく」を押す → 保存されて一覧に戻ることを確認

    **テスト 2: 複数ノート送信**
    1. PC から 2 件別々のノートを「iPhoneに送る」
    2. iPhone のロック画面に 2 件の通知が独立して表示されることを確認（1 件に集約されない）
    3. それぞれの通知をタップ → 一覧に 2 件の「PC受信」ノートが表示されることを確認

    **テスト 3: 「通知を消して一覧へ」**
    1. 通知をタップして viewer を開く（note ステップが表示される場合）
    2. 「通知を消して一覧へ」ボタンを押す
    3. 一覧（list ステップ）に遷移し、「→ 一覧に履歴として残ります」サブテキストが消えることを確認
    4. ロック画面の通知がすべて消えることを確認

    **テスト 4: 削除**
    1. 一覧の「PC受信」ノートの 🗑️ を押す
    2. 一覧から消えることを確認
  </how-to-verify>
  <resume-signal>テスト結果を報告してください（「approve」または問題の詳細）</resume-signal>
</task>

</tasks>

<verification>
- npx vitest run 0 failures
- npx tsc --noEmit 0 errors
- cargo check error なし
- 実機検証 approve
</verification>

<success_criteria>
- 全自動テストが通る
- 実機で PC → iPhone 通知・受信・一覧表示・編集・削除の全フローが動作する
- 複数ノードが独立した通知として表示される
</success_criteria>

<output>
After completion, create `.planning/phases/11-pc-iphone/11-04-SUMMARY.md`
</output>
