---
phase: 07-pc-receive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - src-tauri/src/lib.rs
  - app/page.tsx
  - components/ui/settings-page.tsx
autonomous: false
requirements:
  - POLL-01
  - POLL-02
  - POLL-03

must_haves:
  truths:
    - "PC起動中にiPhoneで「PCに送る」を押すと、30秒以内に新規付箋ウィンドウが画面右上に自動で開く"
    - "開いた付箋にiPhoneで入力したテキスト（画像・Mermaid含む）が正確に入っている"
    - "同じノートを2回受信しない（received_atマーク + LAST_IPHONE_NOTE_ID により、PC再起動後も重複が防止される）"
    - "Drive未接続時は設定画面の「iPhone連携」セクションに赤ドットが表示される"
    - "Drive接続回復後の次回ポーリング成功で赤ドットが自動消去される"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "tokio time feature + tauri-plugin-notification dependency"
      contains: "features = [\"rt\", \"time\"]"
    - path: "src-tauri/capabilities/default.json"
      provides: "notification:default 権限"
      contains: "notification:default"
    - path: "src-tauri/src/lib.rs"
      provides: "LAST_IPHONE_NOTE_ID static, build_context(), poll_iphone_note(), setup()内spawn"
      contains: "LAST_IPHONE_NOTE_ID"
    - path: "app/page.tsx"
      provides: "fusen:note_from_iphone listen と fusen:drive_disconnected/connected listen"
      contains: "note_from_iphone"
    - path: "components/ui/settings-page.tsx"
      provides: "iPhone連携サイドバーとh3横の赤ドットUI"
      contains: "iphoneDriveDisconnected"
  key_links:
    - from: "src-tauri/src/lib.rs poll_iphone_note()"
      to: "app/page.tsx fusen:note_from_iphone listen"
      via: "app_handle.emit(\"fusen:note_from_iphone\", IphoneNotePayload)"
      pattern: "fusen:note_from_iphone"
    - from: "app/page.tsx listen handler"
      to: "openNoteWindow()"
      via: "invoke fusen_create_note → invoke fusen_save_note → openNoteWindow"
    - from: "src-tauri/src/lib.rs poll_iphone_note()"
      to: "components/ui/settings-page.tsx 赤ドット"
      via: "emit fusen:drive_disconnected → page.tsx setIphoneDriveDisconnected → prop渡し"
      pattern: "fusen:drive_disconnected"
---

<objective>
iPhoneから送信された fusen_from_iphone.json をPC側Rustバックグラウンドループが30秒間隔で検出し、新規付箋ウィンドウとして画面右上に自動表示する。

Purpose: v3.0マイルストーン最終フェーズ。iPhone→PCの自動着信を完成させる。
Output:
- Rustポーリングループ（30秒間隔・Drive polling・重複防止・Windowsトースト通知）
- JS側受信リスナー（付箋ウィンドウ生成・音再生）
- Drive未接続フィードバック（設定画面の赤ドット）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-pc-receive/07-CONTEXT.md
@.planning/phases/07-pc-receive/07-RESEARCH.md

<interfaces>
<!-- 既存コードから抽出したシグネチャ。探索不要。 -->

<!-- src-tauri/src/gdrive.rs -->
```rust
pub async fn get_access_token(client: &Client) -> Result<String, String>
// Err時: "Googleアカウントが接続されていません..." → Drive未接続の検出点

pub async fn download_json(
    client: &Client,
    access_token: &str,
    filename: &str,
) -> Result<serde_json::Value, String>
// ファイル未存在時: Err("File not found: fusen_from_iphone.json")

pub async fn upload_json(
    client: &Client,
    access_token: &str,
    filename: &str,
    data: &serde_json::Value,
) -> Result<(), String>
// 既存ファイルがあれば PATCH（更新）
```

<!-- src-tauri/src/state.rs の型 -->
```rust
pub struct Note {
    pub body: String,
    pub frontmatter: String,
    pub meta: NoteMeta,  // { path: String, seq: i32, context: String, ... }
}
// fusen_create_note の戻り値型 = Result<Note, String>
```

<!-- Rust コマンドシグネチャ (lib.rs 132行目) -->
```rust
fn fusen_create_note(
    state: State<'_, Mutex<AppState>>,
    folder_path: String,  // camelCase でJS側から渡す: folderPath
    context: String,
) -> Result<Note, String>
```

<!-- app/page.tsx -->
```typescript
// state変数（145行目）
const [folderPath, setFolderPath] = useState<string>('');

// openNoteWindow シグネチャ（361行目）
const openNoteWindow = useCallback(async (
  path: string,
  meta?: { x?: number; y?: number; width?: number; height?: number; always_on_top?: boolean },
  isNew?: boolean
) => { ... }, [...]);
// isNew=false → 表示モード（編集開始しない）

// 既存 listen useEffect パターン
useEffect(() => {
  if (!isMainWindow) return;
  let unlisten: (() => void) | undefined;
  const promise = listen<PayloadType>('event:name', async (event) => { ... });
  promise.then((u) => { unlisten = u; });
  return () => {
    if (unlisten) unlisten();
    else promise.then((u) => u());
  };
}, [isMainWindow, ...deps]);
```

<!-- fusen_save_note の呼び出しパターン（page.tsx 1154-1158行目） -->
```typescript
await invoke('fusen_save_note', {
  path: newNote.meta.path,
  body: '...',
  frontmatterRaw: newNote.frontmatter || '',
  allowRename: false,
});
```

<!-- components/ui/settings-page.tsx の構造 -->
```typescript
// SettingsPageProps（28-31行目）
type SettingsPageProps = {
    onClose?: () => void;
    defaultTab?: string;
}

// SidebarItem（272行目） - label は string 型のみ
function SidebarItem({
  icon, label, isActive, onClick
}: { icon: React.ReactNode, label: string, isActive: boolean, onClick: () => void }) { ... }

// iPhone連携 SidebarItem（127-132行目）
<SidebarItem
    icon={<Smartphone className="mr-3 h-4 w-4" />}
    label="iPhone連携"
    isActive={activeSection === "iphone"}
    onClick={() => setActiveSection("iphone")}
/>

// IphoneSection の呼び出し（89-90行目）
case "iphone":
    return <IphoneSection t={t} />

// IphoneSection の定義（892行目）
function IphoneSection({ t }: { t: (key: any) => string }) { ... }

// 「Googleドライブ接続」h3（935-936行目）
<h3 className="font-semibold text-gray-800">Googleドライブ接続</h3>
```

<!-- fusen_from_iphone.json フォーマット（Phase 6確定済み） -->
```json
{
  "id": "uuid-v4-string",           // なければ sent_at を代替として使う
  "title": "ユーザー入力タイトル",  // 空あり
  "body": "Markdown形式の本文",      // base64画像含む可能性
  "sent_at": "2026-03-30T12:34:56Z",
  "received_at": null                // null または存在しない → 未受信
}
```
</interfaces>
</context>

<tasks>

<!-- ======================================================== -->
<!-- タスク1: Cargo.toml + capabilities + lib.rs              -->
<!-- 対応要件: POLL-01, POLL-03                               -->
<!-- ======================================================== -->
<task type="auto">
  <name>タスク1: Rust依存追加とポーリングループ実装</name>
  <files>src-tauri/Cargo.toml, src-tauri/capabilities/default.json, src-tauri/src/lib.rs</files>
  <action>
**変更A: src-tauri/Cargo.toml**

47行目の tokio 行を変更（"time" feature 追加）:
```toml
# 変更前
tokio = { version = "1", features = ["rt"] }
# 変更後
tokio = { version = "1", features = ["rt", "time"] }
```

rand_core の後（62行目付近）に追加:
```toml
tauri-plugin-notification = "2"
```

**変更B: src-tauri/capabilities/default.json**

permissions 配列の末尾に `"notification:default"` を追加する（既存の権限は変更しない）。

**変更C: src-tauri/src/lib.rs — 3箇所に追加**

**[C-1] static 変数の追加**
既存の `static LAST_VISIBILITY_MS` や `static NOTES_HIDDEN` の近傍に追加する:
```rust
static LAST_IPHONE_NOTE_ID: std::sync::Mutex<Option<String>> =
    std::sync::Mutex::new(None);
```

**[C-2] 関数3つの追加**
`// --- Entry Point ---` コメントの直前（1364行目付近）に追加する:

```rust
// --- iPhone受信 ---

fn build_context(title: &str, body: &str) -> String {
    if !title.is_empty() {
        return title.to_string();
    }
    let first_line = body.lines().next().unwrap_or("").trim();
    if !first_line.is_empty() {
        return first_line.chars().take(10).collect();
    }
    chrono::Local::now().format("%H:%M").to_string()
}

#[derive(Clone, serde::Serialize)]
struct IphoneNotePayload {
    title: String,
    body: String,
    context: String,
}

async fn poll_iphone_note(client: &reqwest::Client, app: &tauri::AppHandle) {
    // 1. access_token 取得（失敗 = Drive未接続）
    let token = match gdrive::get_access_token(client).await {
        Ok(t) => {
            let _ = app.emit("fusen:drive_connected", ());
            t
        }
        Err(_) => {
            let _ = app.emit("fusen:drive_disconnected", ());
            return;
        }
    };

    // 2. fusen_from_iphone.json をダウンロード
    let data = match gdrive::download_json(client, &token, "fusen_from_iphone.json").await {
        Err(e) if e.contains("File not found") => return, // 静かにスキップ
        Err(e) => {
            logger::log_info(&format!("[poll] Drive download error: {}", e));
            return;
        }
        Ok(d) => d,
    };

    // 3. received_at チェック（PC再起動後も有効）
    if data.get("received_at").and_then(|v| v.as_str()).is_some() {
        return;
    }

    // 4. id チェック（id がなければ sent_at を代替）
    let note_id = data
        .get("id")
        .and_then(|v| v.as_str())
        .or_else(|| data.get("sent_at").and_then(|v| v.as_str()))
        .unwrap_or("")
        .to_string();

    {
        let last = LAST_IPHONE_NOTE_ID
            .lock()
            .unwrap_or_else(|p| p.into_inner());
        if last.as_deref() == Some(&note_id) {
            return;
        }
    }

    // 5. 新着 → LAST_IPHONE_NOTE_ID 更新
    *LAST_IPHONE_NOTE_ID
        .lock()
        .unwrap_or_else(|p| p.into_inner()) = Some(note_id);

    // 6. コンテキスト名決定
    let title = data.get("title").and_then(|v| v.as_str()).unwrap_or("");
    let body  = data.get("body").and_then(|v| v.as_str()).unwrap_or("");
    let context = build_context(title, body);

    // 7. received_at を Drive に非同期で書き戻す
    let mut updated = data.clone();
    updated["received_at"] = serde_json::json!(chrono::Utc::now().to_rfc3339());
    {
        let client2 = client.clone();
        let token2  = token.clone();
        tauri::async_runtime::spawn(async move {
            let _ = gdrive::upload_json(
                &client2, &token2, "fusen_from_iphone.json", &updated,
            ).await;
        });
    }

    // 8. Windows トースト通知
    #[cfg(desktop)]
    {
        use tauri_plugin_notification::NotificationExt;
        let _ = app
            .notification()
            .builder()
            .title("iPhoneから付箋")
            .body(&context)
            .show();
    }

    // 9. JS にemit（JS側でfusen_create_note → openNoteWindow を実行）
    let _ = app.emit(
        "fusen:note_from_iphone",
        IphoneNotePayload {
            title: title.to_string(),
            body: body.to_string(),
            context,
        },
    );
}
```

**[C-3] setup() クロージャへの追加**

setup() クロージャ内の先頭付近（他の `.plugin(...)` が並んでいる箇所）に追加:
```rust
app.handle().plugin(tauri_plugin_notification::init())?;
```

setup() クロージャ末尾の `logger::log_info("アプリの初期化が完了しました");` の直前に追加:
```rust
// iPhone受信: バックグラウンドポーリングループ（30秒間隔）
{
    let app_handle = app.handle().clone();
    tauri::async_runtime::spawn(async move {
        let client = reqwest::Client::new();
        let mut interval =
            tokio::time::interval(std::time::Duration::from_secs(30));
        interval.tick().await; // 起動直後の即時tickを捨てる
        loop {
            interval.tick().await;
            poll_iphone_note(&client, &app_handle).await;
        }
    });
}
```

**注意事項:**
- polling loop は AppState Mutex に一切触れない（emit のみ）
- `tauri::async_runtime::spawn` を使う（`tokio::spawn` ではない）
- Lock毒防止: `.unwrap_or_else(|p| p.into_inner())` を使う
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen/src-tauri && cargo check 2>&1 | tail -10</automated>
  </verify>
  <done>
    `cargo check` がエラーなしで完了する。
    `LAST_IPHONE_NOTE_ID` / `build_context` / `poll_iphone_note` の3つが lib.rs に存在し、setup() 末尾に spawn が追加されている。
    Cargo.toml に `features = ["rt", "time"]` と `tauri-plugin-notification = "2"` が含まれる。
  </done>
</task>

<!-- ======================================================== -->
<!-- タスク2: JS受信ハンドラと赤ドットUI実装                   -->
<!-- 対応要件: POLL-02                                         -->
<!-- ======================================================== -->
<task type="auto">
  <name>タスク2: JS受信リスナーと赤ドットUI（page.tsx / settings-page.tsx）</name>
  <files>app/page.tsx, components/ui/settings-page.tsx</files>
  <action>
**app/page.tsx への変更**

**[A-1] iphoneDriveDisconnected state を追加**
145行目の `const [folderPath, setFolderPath]` の近く（既存 useState 定義の隣）に追加:
```typescript
const [iphoneDriveDisconnected, setIphoneDriveDisconnected] = useState(false);
```

**[A-2] fusen:note_from_iphone リスナー useEffect を追加**
既存の isMainWindow guard 付き useEffect 群の末尾（「音声再生イベントハンドラ」useEffect の後）に追加:

```typescript
// [iPhone受信] iPhoneから付箋受信 → 新規付箋ウィンドウを右上に開く (POLL-02)
useEffect(() => {
  if (!isMainWindow) return;
  let unlisten: (() => void) | undefined;
  const promise = listen<{ title: string; body: string; context: string }>(
    'fusen:note_from_iphone',
    async (event) => {
      const { body, context } = event.payload;
      try {
        const newNote = await invoke<{ body: string; frontmatter: string; meta: { path: string } }>(
          'fusen_create_note',
          { folderPath, context }
        );
        await invoke('fusen_save_note', {
          path: newNote.meta.path,
          body: body || '',
          frontmatterRaw: newNote.frontmatter || '',
          allowRename: false,
        });
        playCreateSound();
        const sw = window.screen.width;
        await openNoteWindow(
          newNote.meta.path,
          { x: sw - 430, y: 50, width: 400, height: 350 },
          false
        );
      } catch (e) {
        console.error('[iphone] 付箋作成失敗:', e);
      }
    }
  );
  promise.then((u) => { unlisten = u; });
  return () => {
    if (unlisten) unlisten();
    else promise.then((u) => u());
  };
}, [isMainWindow, openNoteWindow, folderPath]);
```

**[A-3] Drive接続状態リスナー useEffect を追加**（上の追加の直後）:

```typescript
// [iPhone受信] Drive接続状態 → 赤ドット制御
useEffect(() => {
  if (!isMainWindow) return;
  let unlistenDisconnected: (() => void) | undefined;
  let unlistenConnected: (() => void) | undefined;
  const p1 = listen('fusen:drive_disconnected', () => {
    setIphoneDriveDisconnected(true);
  });
  const p2 = listen('fusen:drive_connected', () => {
    setIphoneDriveDisconnected(false);
  });
  p1.then((u) => { unlistenDisconnected = u; });
  p2.then((u) => { unlistenConnected = u; });
  return () => {
    if (unlistenDisconnected) unlistenDisconnected();
    else p1.then((u) => u());
    if (unlistenConnected) unlistenConnected();
    else p2.then((u) => u());
  };
}, [isMainWindow]);
```

**[A-4] SettingsPage に prop を渡す**
page.tsx 内で `<SettingsPage` を使っている箇所を探し、`iphoneDriveDisconnected={iphoneDriveDisconnected}` を追加する:
```typescript
<SettingsPage
  onClose={...}
  defaultTab={...}
  iphoneDriveDisconnected={iphoneDriveDisconnected}
/>
```

---

**components/ui/settings-page.tsx への変更**

**[B-1] SettingsPageProps に追加**（28-31行目の型定義）:
```typescript
type SettingsPageProps = {
    onClose?: () => void;
    defaultTab?: string;
    iphoneDriveDisconnected?: boolean;  // 追加
}
```

**[B-2] SettingsPage 関数の引数に追加**（33行目）:
```typescript
export default function SettingsPage({ onClose, defaultTab, iphoneDriveDisconnected }: SettingsPageProps) {
```

**[B-3] IphoneSection に prop を渡す**（89-90行目の case "iphone"）:
```typescript
case "iphone":
    return <IphoneSection t={t} iphoneDriveDisconnected={iphoneDriveDisconnected ?? false} />
```

**[B-4] SidebarItem に badge prop を追加**（272行目の関数定義）:
```typescript
function SidebarItem({
  icon, label, isActive, onClick, badge
}: {
  icon: React.ReactNode;
  label: string;
  isActive: boolean;
  onClick: () => void;
  badge?: boolean;
}) {
    return (
        <Button
            variant={isActive ? "secondary" : "ghost"}
            className={`w-full justify-start ${isActive ? "bg-secondary font-medium" : ""}`}
            onClick={onClick}
        >
            {icon}
            <span className="flex-1 text-left">{label}</span>
            {badge && (
                <span className="ml-auto h-2 w-2 rounded-full bg-red-500 flex-shrink-0" />
            )}
        </Button>
    )
}
```

**[B-5] iPhone連携 SidebarItem に badge prop を追加**（127-132行目）:
```tsx
<SidebarItem
    icon={<Smartphone className="mr-3 h-4 w-4" />}
    label="iPhone連携"
    isActive={activeSection === "iphone"}
    onClick={() => setActiveSection("iphone")}
    badge={iphoneDriveDisconnected}
/>
```

**[B-6] IphoneSection の定義を変更**（892行目）:
```typescript
function IphoneSection({ t, iphoneDriveDisconnected }: {
  t: (key: any) => string;
  iphoneDriveDisconnected: boolean;
}) {
```

**[B-7] 「Googleドライブ接続」h3 横に赤ドットを追加**（935-936行目）:
```tsx
<div className="flex items-center gap-2">
    <h3 className="font-semibold text-gray-800">Googleドライブ接続</h3>
    {iphoneDriveDisconnected && (
        <span className="h-2 w-2 rounded-full bg-red-500 flex-shrink-0" title="Driveに接続されていません" />
    )}
</div>
```
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <done>
    `npx tsc --noEmit` がエラーゼロで完了する。
    page.tsx に `fusen:note_from_iphone` / `fusen:drive_disconnected` / `fusen:drive_connected` の3つのリスナーが存在する。
    settings-page.tsx の SidebarItem に badge prop が追加され、IphoneSection 内の h3 横に赤ドットのレンダリングロジックが存在する。
  </done>
</task>

<!-- ======================================================== -->
<!-- タスク3: 実機確認チェックポイント                          -->
<!-- ======================================================== -->
<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Rustポーリングループ（30秒間隔・Drive polling・重複防止・Windowsトースト通知）
    - JS受信リスナー（fusen_create_note → fusen_save_note → openNoteWindow 画面右上 isNew=false）
    - 赤ドットUI（Drive未接続時に「iPhone連携」サイドバー項目と h3 横に表示）
  </what-built>
  <how-to-verify>
    **Step 1: ビルド確認**
    ```
    cargo build  （エラーなしで完了すること）
    ```

    **Step 2: 正常動作確認（Drive接続済みの場合）**
    1. `npm run tauri dev` でアプリ起動
    2. 30秒以上待つ（コンソールに polling エラーが出ないことを確認）
    3. Drive上に fusen_from_iphone.json が存在しなければ何も起きない（正常）

    **Step 3: iPhone→PC E2E テスト**
    1. Google Drive の `ore-no-fusen` フォルダに以下の JSON を `fusen_from_iphone.json` という名前で作成する:
       ```json
       {
         "id": "test-001",
         "title": "テスト付箋",
         "body": "iPhoneからのテストメッセージ",
         "sent_at": "2026-03-30T12:00:00Z",
         "received_at": null
       }
       ```
    2. 30秒以内に画面右上に新規付箋ウィンドウが開くことを確認
    3. 付箋の内容が「iPhoneからのテストメッセージ」であることを確認
    4. Windowsトースト通知「iPhoneから付箋」が出ることを確認（環境による）

    **Step 4: 重複防止確認**
    1. 同じ fusen_from_iphone.json（received_at が書き込まれた後の状態）で再度30秒待つ
    2. 2回目の付箋ウィンドウが開かないことを確認
    3. Drive上の fusen_from_iphone.json に received_at フィールドが追記されていることを確認

    **Step 5: 赤ドット確認**
    1. gdrive_token.json を一時的にリネームしてアプリ再起動
    2. 設定画面 → iPhone連携 を開き、赤ドットが表示されることを確認
    3. gdrive_token.json を元に戻し、30秒後に赤ドットが消えることを確認
  </how-to-verify>
  <resume-signal>「確認完了」または発見した問題を記述してください</resume-signal>
</task>

</tasks>

<verification>
Phase 7 全体の完了確認:

1. `cargo check` エラーゼロ（タスク1の verify）
2. `npx tsc --noEmit` エラーゼロ（タスク2の verify）
3. POLL-01: 30秒間隔のポーリングループが setup() で起動される
4. POLL-02: iPhone送信後30秒以内に右上（x=screen.width-430, y=50, w=400, h=350）に付箋ウィンドウが開く
5. POLL-03: received_at マーク + LAST_IPHONE_NOTE_ID により同じノートが2回開かれない
6. Drive未接続時に設定画面の赤ドットが表示され、回復後に自動消去される
</verification>

<success_criteria>
1. PC起動中にiPhoneで「PCに送る」を押すと、30秒以内に新規付箋ウィンドウが右上に自動で開く
2. 開いた付箋にiPhoneで入力したテキスト（画像・Mermaid含む）が正確に入っている
3. 同じノートを2回受信しない（PC再起動後も received_at マークにより重複が防止される）
4. Drive未接続時は設定画面の「iPhone連携」サイドバーと「Googleドライブ接続」h3 横に赤ドットが表示される
5. `cargo check` + `npx tsc --noEmit` 両方エラーゼロ
</success_criteria>

<output>
完了後、`.planning/phases/07-pc-receive/07-01-SUMMARY.md` を作成する。
フォーマットは `@./.claude/get-shit-done/templates/summary.md` に従う。
</output>
