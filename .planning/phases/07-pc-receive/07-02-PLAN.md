---
phase: 07-pc-receive
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/viewer/page.tsx
  - src-tauri/src/gdrive.rs
  - src-tauri/src/lib.rs
  - src-tauri/Cargo.toml
autonomous: false
requirements:
  - POLL-02

must_haves:
  truths:
    - "iPhoneから画像を送ると、PC の付箋ウィンドウに画像が表示される"
    - "PC の OreNoFusen/images/ フォルダに fusen_img_TIMESTAMP.jpg が存在する"
    - "fusen_from_iphone.json の body は base64 データを含まず、ファイル名参照のみ含む（Drive の ore-no-fusen フォルダ内に fusen_img_TIMESTAMP.jpg として保存される）"
    - "画像なしのテキストのみ送信は影響を受けない（07-01 の動作が維持される）"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "画像を Drive にファイルとしてアップロードし、body にファイル名参照を埋め込む"
      contains: "uploadImageToDrive"
    - path: "src-tauri/src/gdrive.rs"
      provides: "Drive からバイナリファイルをダウンロードする関数"
      contains: "download_binary"
    - path: "src-tauri/src/lib.rs"
      provides: "画像をローカル保存し body のパスを書き換える Tauri コマンド"
      contains: "fusen_download_iphone_images"
  key_links:
    - from: "app/viewer/page.tsx 送信ボタン"
      to: "Drive の ore-no-fusen フォルダ内 fusen_img_TIMESTAMP.jpg"
      via: "uploadImageToDrive() — multipart POST"
      pattern: "fusen_img_"
    - from: "app/page.tsx fusen:note_from_iphone listen handler"
      to: "src-tauri/src/lib.rs fusen_download_iphone_images"
      via: "invoke('fusen_download_iphone_images', { body, folderPath })"
      pattern: "fusen_download_iphone_images"
    - from: "src-tauri/src/lib.rs fusen_download_iphone_images"
      to: "{folderPath}/images/fusen_img_XXX.jpg"
      via: "gdrive::download_binary() → std::fs::write()"
      pattern: "download_binary"
---

<objective>
iPhoneから送信される画像を base64 埋め込みではなく Drive 上の独立ファイルとして管理し、PC 受信時にローカルファイルとして保存してから付箋に表示する。

Purpose: base64 埋め込みにより fusen_from_iphone.json が数 MB になる問題を解消する。画像ファイルを Drive に分離することで JSON は小さく保ち、PC 側でも通常の Markdown 画像（ローカルパス）として表示できる。
Output:
- viewer/page.tsx: 画像添付時に Drive へバイナリアップロード、body には `![](fusen_img_TIMESTAMP.jpg)` のファイル名参照のみ格納
- gdrive.rs: `download_binary(client, token, filename) -> Result<Vec<u8>, String>` 関数追加
- lib.rs: `fusen_download_iphone_images(body, folder_path) -> Result<String, String>` コマンド追加（body 内の `fusen_img_*` 参照を検出 → Drive からダウンロード → ローカル保存 → 絶対パスに書き換えた body を返す）
- app/page.tsx: `fusen:note_from_iphone` listen handler で `fusen_download_iphone_images` を invoke してから `fusen_save_note` を呼ぶよう変更

**スコープ外**: Drive APIエラー時の自動リトライ3回（CONTEXT.md 決定事項）は 07-01（polling loop）で実装済みの範囲のみ対象。このプランの `fusen_download_iphone_images` コマンド内では実装しない。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-pc-receive/07-CONTEXT.md
@.planning/phases/07-pc-receive/07-RESEARCH.md
@.planning/phases/07-pc-receive/07-01-SUMMARY.md

<interfaces>
<!-- 既存コードから抽出したシグネチャ。探索不要。 -->

<!-- src-tauri/src/gdrive.rs — 既存関数（参考: download_json と同パターンで download_binary を追加） -->
```rust
pub async fn download_json(
    client: &Client,
    access_token: &str,
    filename: &str,
) -> Result<serde_json::Value, String>
// ファイル未存在時: Err("File not found: {filename}")
// 検索方法: Drive 全体から name='{filename}' and trashed=false で検索

// 追加する関数（下記タスクBで実装）
pub async fn download_binary(
    client: &Client,
    access_token: &str,
    filename: &str,
) -> Result<Vec<u8>, String>
// download_json と同じ検索ロジック、bytes() で取得するだけの違い
// 検索スコープ: Drive 全体（download_json と同じ）
// ファイル名衝突リスク: fusen_img_{timestamp}.jpg 形式はタイムスタンプで一意のため全体検索で問題なし
```

<!-- app/viewer/page.tsx — 既存のアップロード関数 -->
```typescript
// 既存: JSON アップロード（multipart）
async function uploadToDrive(accessToken: string, fileName: string, data: object): Promise<void>
// 既存: 自動リフレッシュラッパー
async function uploadWithAutoRefresh(token: string, fileName: string, data: object): Promise<void>
// 既存: フォルダID取得
async function getAppFolderId(accessToken: string): Promise<string | null>

// 既存の state
const [attachedImages, setAttachedImages] = useState<string[]>([]);
// 現在: base64 文字列の配列
// 変更後: ファイル名の配列（例: ['fusen_img_1234567890.jpg', ...]）

// 既存の送信処理（659-688行目）— fullBody の生成を変更する
const fullBody = writeBody + attachedImages.map((b64) => `\n![](${b64})`).join('');
// 変更後:
const fullBody = writeBody + attachedImages.map((fname) => `\n![](${fname})`).join('');
```

<!-- app/page.tsx — 07-01 で追加した listen handler（変更対象） -->
```typescript
// fusen:note_from_iphone listen handler 内（07-01 で実装済み）
const newNote = await invoke<...>('fusen_create_note', { folderPath, context });
await invoke('fusen_save_note', {
  path: newNote.meta.path,
  body: body || '',               // ← ここを変更: body をそのまま使う前に画像解決する
  frontmatterRaw: newNote.frontmatter || '',
  allowRename: false,
});
```

<!-- lib.rs — 追加するコマンド -->
```rust
#[tauri::command]
async fn fusen_download_iphone_images(
    folder_path: String,   // JS側の folderPath state の値
    body: String,          // fusen_from_iphone.json の body フィールド
) -> Result<String, String>
// 戻り値: ローカルパスに書き換えた body 文字列
// images ディレクトリ: {folder_path}/images/
// ファイル検出パターン: ![](fusen_img_*.*)
// 既存ファイルは再ダウンロードしない（冪等）
```
</interfaces>
</context>

<tasks>

<!-- ======================================================== -->
<!-- タスクA: viewer/page.tsx — 画像を Drive ファイルとしてアップロード -->
<!-- ======================================================== -->
<task type="auto">
  <name>タスクA: iPhone側の画像アップロード方式を Drive ファイルに変更</name>
  <files>app/viewer/page.tsx</files>
  <action>
**変更の目的**: 画像添付時に base64 文字列を state に持つのをやめ、Drive に画像ファイルをアップロードしてファイル名を state に持つ。

---

**[A-1] `uploadImageToDrive` 関数を追加**

`uploadWithAutoRefresh` 関数の直後（205行目付近）に追加する:

```typescript
// 画像ファイルを Drive にアップロードしてファイル名を返す
async function uploadImageToDrive(
  accessToken: string,
  file: File,
  fileName: string
): Promise<void> {
  const folderId = await getAppFolderId(accessToken);
  const parentId = folderId ?? 'root';
  const metadata = JSON.stringify({
    name: fileName,
    mimeType: file.type || 'image/jpeg',
    parents: [parentId],
  });
  const form = new FormData();
  form.append('metadata', new Blob([metadata], { type: 'application/json' }));
  form.append('file', file);
  const res = await fetch(
    'https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart',
    {
      method: 'POST',
      headers: { Authorization: `Bearer ${accessToken}` },
      body: form,
    }
  );
  if (!res.ok) throw new Error(`Drive image upload failed: ${res.status}`);
}

// uploadImageToDrive のトークン期限切れ対応ラッパー
async function uploadImageWithAutoRefresh(
  token: string,
  file: File,
  fileName: string
): Promise<void> {
  try {
    await uploadImageToDrive(token, file, fileName);
  } catch {
    const newToken = await refreshAccessToken();
    if (!newToken) throw new Error('session expired');
    await uploadImageToDrive(newToken, file, fileName);
  }
}
```

---

**[A-2] `attachedImages` state の型コメントを更新**

246行目の state 宣言はそのまま（型は `string[]` のまま）。ただし格納する値を base64 からファイル名に変える。
変更不要 — 型は変わらないため。

---

**[A-3] 画像選択ハンドラを変更（599-605行目付近）**

```typescript
// 変更前
const base64 = await resizeImageToBase64(file);
setAttachedImages((prev) => [...prev, base64]);

// 変更後
const fileName = `fusen_img_${Date.now()}.jpg`;
if (!accessToken) { setErrorMessage('ログインが必要です'); return; }
try {
  await uploadImageWithAutoRefresh(accessToken, file, fileName);
  setAttachedImages((prev) => [...prev, fileName]);
} catch (err) {
  setErrorMessage('画像のアップロードに失敗しました: ' + (err instanceof Error ? err.message : String(err)));
}
```

`resizeImageToBase64` の import は残す（他の箇所で使用しているか確認してから削除判断する。使われていなければ削除してよい）。

---

**[A-4] サムネイル表示を修正（564-575行目付近）**

現在 `attachedImages` の各要素を `src={b64}` で表示しているが、ファイル名に変わるため Drive URL での表示に変える。ただしファイルアップロード後に fileId を取得する手間を避けるため、アップロード成功後はファイル名テキストのみサムネイル表示する（簡易実装）:

```typescript
// 変更前
<img src={b64} className="w-16 h-16 object-cover rounded" alt="" />

// 変更後
<div className="w-16 h-16 object-cover rounded bg-gray-100 flex items-center justify-center text-xs text-gray-500 overflow-hidden text-center p-1">
  {fname.replace('fusen_img_', '').replace('.jpg', '')}
</div>
```

map の変数名も `b64` → `fname` に変える:
```typescript
// 変更前
{attachedImages.map((b64, i) => (
  <div key={i} className="relative">
    <img src={b64} className="w-16 h-16 object-cover rounded" alt="" />

// 変更後
{attachedImages.map((fname, i) => (
  <div key={i} className="relative">
    <div className="w-16 h-16 object-cover rounded bg-gray-100 flex items-center justify-center text-xs text-gray-500 overflow-hidden text-center p-1">
      {fname.replace('fusen_img_', '').replace('.jpg', '')}
    </div>
```

---

**[A-5] 送信処理の body 生成を修正**

「PCに送る」ボタン（659-688行目付近）と「iPhoneに置いておく」ボタン（628-650行目付近）の両方で `fullBody` 生成を変更する:

```typescript
// 変更前（両方）
const fullBody = writeBody + attachedImages.map((b64) => `\n![](${b64})`).join('');

// 変更後（両方）
const fullBody = writeBody + attachedImages.map((fname) => `\n![](${fname})`).join('');
```

履歴保存時の `stripBase64Images` は引き続き呼ぶ（テキストのみになるので実質 no-op になるが、既存の防御コードとして残す）。
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen && npx tsc --noEmit 2>&1 | tail -15 && grep -n "uploadImageWithAutoRefresh" app/viewer/page.tsx && grep -c "resizeImageToBase64" app/viewer/page.tsx || echo "resizeImageToBase64 removed"</automated>
  </verify>
  <done>
    `npx tsc --noEmit` がエラーゼロで完了する。
    viewer/page.tsx に `uploadImageToDrive` と `uploadImageWithAutoRefresh` 関数が存在する（grep で確認済み）。
    画像選択ハンドラが `resizeImageToBase64` ではなく `uploadImageWithAutoRefresh` を呼んでいる（送信フローから `resizeImageToBase64` が除去されているか、使用箇所がないことを確認）。
    `attachedImages.map((fname) => ...)` でファイル名参照を body に埋め込んでいる。
  </done>
</task>

<!-- ======================================================== -->
<!-- タスクB: gdrive.rs + lib.rs — PC側の画像ダウンロードと保存 -->
<!-- ======================================================== -->
<task type="auto">
  <name>タスクB: Rust 側の download_binary と fusen_download_iphone_images 実装</name>
  <files>src-tauri/src/gdrive.rs, src-tauri/src/lib.rs, src-tauri/Cargo.toml, app/page.tsx</files>
  <action>
**変更の目的**: PC が `fusen_from_iphone.json` を受信したとき、body 内の `![](fusen_img_*.*)` パターンを検出し、Drive からバイナリをダウンロードして `{folder_path}/images/` に保存し、body を絶対パスに書き換えて返す。

---

**[B-0] src-tauri/Cargo.toml に `regex` クレートを追加**

`[dependencies]` セクションに以下を追加する（既に存在する場合はスキップ）:

```toml
regex = "1"
```

追加場所: 他の依存クレートが並んでいる `[dependencies]` セクション末尾。

---

**[B-1] gdrive.rs に `download_binary` 関数を追加**

`download_json` 関数（365行目）の直後に追加する。

**検索スコープの方針**: `download_json` と同じく Drive 全体検索（`name='{filename}' and trashed=false`）を使用する。`fusen_img_{timestamp}.jpg` 形式はタイムスタンプで一意なため、他アプリ同名ファイルとの衝突リスクは実質ゼロ。`ore-no-fusen` フォルダ限定の parents フィルタは不要（download_json も使っていない）:

```rust
/// Drive からバイナリファイルをダウンロードして Vec<u8> で返す
/// ファイル未存在時: Err("File not found: {filename}")
/// 検索スコープ: Drive 全体（download_json と同じ実装方針）
pub async fn download_binary(
    client: &Client,
    access_token: &str,
    filename: &str,
) -> Result<Vec<u8>, String> {
    let q = format!("name='{}' and trashed=false", filename);
    let resp: DriveFileList = client
        .get("https://www.googleapis.com/drive/v3/files")
        .bearer_auth(access_token)
        .query(&[("q", q.as_str()), ("fields", "files(id)")])
        .send()
        .await
        .map_err(|e| e.to_string())?
        .json()
        .await
        .map_err(|e| e.to_string())?;
    let file_id = resp
        .files
        .into_iter()
        .next()
        .ok_or_else(|| format!("File not found: {}", filename))?
        .id;

    let bytes = client
        .get(format!(
            "https://www.googleapis.com/drive/v3/files/{}?alt=media",
            file_id
        ))
        .bearer_auth(access_token)
        .send()
        .await
        .map_err(|e| e.to_string())?
        .bytes()
        .await
        .map_err(|e| e.to_string())?;

    Ok(bytes.to_vec())
}
```

---

**[B-2] lib.rs に `fusen_download_iphone_images` コマンドを追加**

`// --- iPhone受信 ---` コメントブロック（build_context の前）の前に追加する:

```rust
/// iPhoneからの body 内の画像参照（fusen_img_*.* パターン）を
/// Drive からダウンロードしてローカル保存し、絶対パスに書き換えた body を返す
#[tauri::command]
async fn fusen_download_iphone_images(
    folder_path: String,
    body: String,
) -> Result<String, String> {
    use std::path::Path;

    // 画像ファイル名を抽出: ![](fusen_img_XXX.ext) → ["fusen_img_XXX.ext", ...]
    let re = regex::Regex::new(r"!\[\]\((fusen_img_[^)]+)\)")
        .map_err(|e| e.to_string())?;

    let filenames: Vec<String> = re
        .captures_iter(&body)
        .filter_map(|cap| cap.get(1))
        .map(|m| m.as_str().to_string())
        .collect::<std::collections::HashSet<_>>()
        .into_iter()
        .collect();

    if filenames.is_empty() {
        return Ok(body);
    }

    // images ディレクトリを作成
    let images_dir = Path::new(&folder_path).join("images");
    std::fs::create_dir_all(&images_dir).map_err(|e| e.to_string())?;

    // Drive接続・トークン取得
    let client = reqwest::Client::new();
    let token = gdrive::get_access_token(&client).await
        .map_err(|e| format!("Drive未接続: {}", e))?;

    // 各画像をダウンロードしてローカル保存
    let mut rewritten = body.clone();
    for filename in &filenames {
        let local_path = images_dir.join(filename);

        // 既存ファイルはスキップ（冪等）
        if local_path.exists() {
            let abs_windows = local_path.to_string_lossy().into_owned();
            rewritten = rewritten.replace(
                &format!("![]({filename})"),
                &format!("![]({abs_windows})"),
            );
            continue;
        }

        match gdrive::download_binary(&client, &token, filename).await {
            Ok(bytes) => {
                std::fs::write(&local_path, &bytes)
                    .map_err(|e| format!("画像保存失敗 {}: {}", filename, e))?;
                let abs_windows = local_path.to_string_lossy().into_owned();
                rewritten = rewritten.replace(
                    &format!("![]({filename})"),
                    &format!("![]({abs_windows})"),
                );
            }
            Err(e) => {
                logger::log_info(&format!("[images] download failed {}: {}", filename, e));
                // ダウンロード失敗した画像はそのまま残す（表示エラーになるが致命的ではない）
            }
        }
    }

    Ok(rewritten)
}
```

---

**[B-3] lib.rs の invoke_handler に `fusen_download_iphone_images` を登録**

`generate_handler![...]` マクロの末尾（他のコマンドが並んでいる箇所）に追加する:

```rust
fusen_download_iphone_images,
```

---

**[B-4] app/page.tsx の `fusen:note_from_iphone` listen handler を更新**

07-01 で追加した listen handler 内（`fusen_create_note` → `fusen_save_note` のフロー）を以下のように変更する。
`body` を `fusen_save_note` に渡す前に `fusen_download_iphone_images` を呼ぶ:

```typescript
// 変更前
const newNote = await invoke<{ body: string; frontmatter: string; meta: { path: string } }>(
  'fusen_create_note',
  { folderPath, context }
);
await invoke('fusen_save_note', {
  path: newNote.meta.path,
  body: body || '',
  frontmatterRaw: newNote.frontmatter || '',
  allowRename: false,
});

// 変更後
const newNote = await invoke<{ body: string; frontmatter: string; meta: { path: string } }>(
  'fusen_create_note',
  { folderPath, context }
);
// 画像参照をローカルパスに解決（画像なしの場合は body がそのまま返る）
const resolvedBody = await invoke<string>('fusen_download_iphone_images', {
  folderPath,
  body: body || '',
});
await invoke('fusen_save_note', {
  path: newNote.meta.path,
  body: resolvedBody,
  frontmatterRaw: newNote.frontmatter || '',
  allowRename: false,
});
```
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen/src-tauri && cargo check 2>&1 | tail -15</automated>
  </verify>
  <done>
    `cargo check` がエラーなしで完了する。
    src-tauri/Cargo.toml の `[dependencies]` に `regex = "1"` が存在する。
    gdrive.rs に `download_binary` 関数が追加されている。
    lib.rs に `fusen_download_iphone_images` コマンドが追加され、generate_handler に登録されている。
    app/page.tsx の listen handler が `fusen_download_iphone_images` を invoke してから `fusen_save_note` を呼んでいる。
  </done>
</task>

<!-- ======================================================== -->
<!-- タスクC: 実機確認チェックポイント                          -->
<!-- ======================================================== -->
<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - iPhone viewer: 画像選択時に Drive へ fusen_img_TIMESTAMP.jpg としてアップロード、body には `![](fusen_img_TIMESTAMP.jpg)` のファイル名参照を格納
    - PC Rust: `fusen_download_iphone_images` コマンドが body のファイル名参照を検出 → Drive からバイナリダウンロード → OreNoFusen/images/ に保存 → body の絶対パスに書き換え
    - PC JS: listen handler が resolvedBody（絶対パス）で fusen_save_note を呼ぶ
  </what-built>
  <how-to-verify>
    **Step 1: テキストのみ送信（回帰確認）**
    1. iPhone の viewer 画面でテキストのみ入力して「PCに送る」
    2. 30秒以内に PC で付箋ウィンドウが開くことを確認（07-01 の動作が維持されている）
    3. 付箋の内容が正しいことを確認

    **Step 2: 画像つき送信**
    1. iPhone の viewer 画面でテキストを入力し、カメラ/ライブラリから画像を1枚添付
    2. アップロード処理中（送信ボタンが「送信中...」になる）を確認
    3. 「PCに送る」を押す
    4. 30秒以内に PC で付箋ウィンドウが開くことを確認
    5. 付箋内に画像が表示されることを確認

    **Step 3: ファイル確認**
    1. Drive の ore-no-fusen フォルダに fusen_img_TIMESTAMP.jpg が存在することを確認
    2. PC の OreNoFusen/images/ フォルダに同名ファイルが保存されていることを確認
    3. fusen_from_iphone.json の body が base64 データを含まず `![](fusen_img_...)` 参照のみであることを確認

    **Step 4: 冪等確認（重複受信防止との連携）**
    1. 同じ付箋を受信しようとしても（received_at あり）スキップされることを確認（07-01 の重複防止が壊れていない）
  </how-to-verify>
  <resume-signal>「確認完了」または発見した問題を記述してください</resume-signal>
</task>

</tasks>

<verification>
Phase 07-02 完了確認:

1. `npx tsc --noEmit` エラーゼロ（タスクA の verify）
2. `cargo check` エラーゼロ（タスクB の verify）
3. viewer/page.tsx: `uploadImageToDrive` と `uploadImageWithAutoRefresh` 関数が存在する
4. viewer/page.tsx: 画像選択ハンドラが `uploadImageWithAutoRefresh` を呼んでいる（`resizeImageToBase64` は送信フローから除去）
5. src-tauri/Cargo.toml: `regex = "1"` が `[dependencies]` に存在する
6. gdrive.rs: `download_binary` が bytes() で返す（json() ではない）
7. lib.rs: `fusen_download_iphone_images` が `fusen_img_` パターンを regex で検出している
8. app/page.tsx listen handler: `fusen_download_iphone_images` invoke の後に `fusen_save_note` を呼んでいる
9. 実機: 画像つきノートが PC で正しく表示され、OreNoFusen/images/ にファイルが存在する（Step 2-3 確認済み）
</verification>

<success_criteria>
1. iPhone で画像を送ると Drive に fusen_img_TIMESTAMP.jpg として保存される（fusen_from_iphone.json に base64 は含まれない）
2. PC の付箋ウィンドウに画像が表示され、OreNoFusen/images/ に fusen_img_TIMESTAMP.jpg が存在する
3. テキストのみのノートは 07-01 と同じ動作を維持する
4. `cargo check` + `npx tsc --noEmit` 両方エラーゼロ
</success_criteria>

<output>
完了後、`.planning/phases/07-pc-receive/07-02-SUMMARY.md` を作成する。
フォーマットは `@./.claude/get-shit-done/templates/summary.md` に従う。
</output>
