---
phase: 06-iphone-send-ui
plan: "04"
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - app/viewer/page.tsx
  - app/viewer/SimpleNoteBody.tsx
  - app/viewer/viewer.test.tsx
autonomous: true
requirements:
  - SEND-04
  - REND-01

must_haves:
  truths:
    - "Mermaid ボタンタップでモーダルが開く"
    - "モーダルのプレビューボタンで mermaid.render() が呼ばれ SVG が表示される"
    - "挿入ボタンでカーソル位置に ```mermaid ブロックが挿入される"
    - "SimpleNoteBody が ```mermaid コードブロックを検出して SVG として描画する"
    - "複数の mermaid ブロックが各々ユニーク ID で描画される"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "MermaidModal コンポーネント（インライン）、showMermaidModal state の完全実装"
      contains: "showMermaidModal"
    - path: "app/viewer/SimpleNoteBody.tsx"
      provides: "Mermaid ブロック検出・SVG レンダリング"
      contains: "mermaid"
  key_links:
    - from: "Mermaid ボタン (page.tsx)"
      to: "MermaidModal コンポーネント"
      via: "showMermaidModal state"
      pattern: "showMermaidModal"
    - from: "SimpleNoteBody.tsx"
      to: "mermaid.render()"
      via: "useEffect 内 dynamic import"
      pattern: "import\\('mermaid'\\)"
---

<objective>
Mermaid モーダル（SEND-04）と SimpleNoteBody の Mermaid レンダリング（REND-01）を実装する。

Purpose: iPhoneでMermaid図を書いて挿入でき、viewer 上で SVG として表示される。

Output: page.tsx の MermaidModal、SimpleNoteBody.tsx の Mermaid 対応、SEND-04/REND-01 テスト GREEN
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-iphone-send-ui/06-CONTEXT.md
@.planning/phases/06-iphone-send-ui/06-RESEARCH.md
@.planning/phases/06-iphone-send-ui/06-02-SUMMARY.md

<interfaces>
<!-- SimpleNoteBody.tsx 現在の実装 -->
```typescript
// 現在: 画像のみ対応（35行）
export function SimpleNoteBody({ body }: { body: string }) {
  const imgRe = /!\[([^\]]*)\]\((data:[^)]+)\)/g;
  // ... imgRe のマッチ処理
}
```

<!-- page.tsx の Mermaid 関連 state（Plan 02 で追加済み）-->
```typescript
const [showMermaidModal, setShowMermaidModal] = useState(false);
const textareaRef = React.useRef<HTMLTextAreaElement>(null);
```

<!-- Mermaid dynamic import パターン（RESEARCH.md Pattern 2）-->
```typescript
useEffect(() => {
  import('mermaid').then(({ default: mermaid }) => {
    mermaid.initialize({ startOnLoad: false });
    mermaid.render(`mermaid-${index}`, code).then(({ svg }) => {
      containerRef.current!.innerHTML = svg;
    });
  });
}, [code]);
```

<!-- mermaid.render() の戻り値（mermaid@11.x）-->
// mermaid.render(id: string, code: string) → Promise<{ svg: string }>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mermaid モーダルを page.tsx に実装する（SEND-04）</name>
  <files>app/viewer/page.tsx, app/viewer/viewer.test.tsx</files>
  <behavior>
    - Mermaid ボタン → showMermaidModal=true でモーダルが表示される
    - モーダル内: コード入力テキストエリア + 「プレビュー」ボタン + 「挿入」ボタン + 「✕」閉じるボタン
    - 「プレビュー」: mermaid.render() を呼んでプレビュー SVG を表示（エラー時は赤テキスト）
    - 「挿入」: textareaRef 経由でカーソル位置に ```mermaid\n...\n``` ブロックを挿入 → モーダルを閉じる
    - mermaid import は useEffect 内の dynamic import（SSR 安全）
  </behavior>
  <action>
page.tsx を読み込む。

**1. MermaidModal に必要な state を ViewerPage に追加:**
```typescript
const [mermaidCode, setMermaidCode] = useState('');
const [mermaidPreviewSvg, setMermaidPreviewSvg] = useState<string | null>(null);
const [mermaidPreviewError, setMermaidPreviewError] = useState<string | null>(null);
const [isMermaidRendering, setIsMermaidRendering] = useState(false);
const mermaidPreviewRef = React.useRef<HTMLDivElement>(null);
```

**2. write ステップ内の Mermaid モーダルプレースホルダー部分を実際の実装で差し替える:**

`{showMermaidModal && <MermaidModal ... />}` のプレースホルダー（Plan 02 で作成）を、以下のインライン実装で置き換える:

```tsx
{showMermaidModal && (
  <div className="fixed inset-0 z-50 flex flex-col bg-white">
    {/* モーダルヘッダー */}
    <div className="flex items-center justify-between px-4 py-3 border-b border-gray-200">
      <button
        className="text-gray-500 text-lg font-medium"
        onClick={() => {
          setShowMermaidModal(false);
          setMermaidCode('');
          setMermaidPreviewSvg(null);
          setMermaidPreviewError(null);
        }}
      >
        ✕
      </button>
      <span className="font-semibold text-gray-900">Mermaid</span>
      <button
        className="text-blue-600 text-sm font-medium disabled:opacity-40"
        disabled={isMermaidRendering || !mermaidCode.trim()}
        onClick={async () => {
          setIsMermaidRendering(true);
          setMermaidPreviewError(null);
          try {
            const { default: mermaid } = await import('mermaid');
            mermaid.initialize({ startOnLoad: false });
            const id = `mermaid-preview-${Date.now()}`;
            const { svg } = await mermaid.render(id, mermaidCode);
            setMermaidPreviewSvg(svg);
          } catch (err: unknown) {
            setMermaidPreviewError('構文エラー: ' + (err instanceof Error ? err.message : String(err)));
            setMermaidPreviewSvg(null);
          } finally {
            setIsMermaidRendering(false);
          }
        }}
      >
        {isMermaidRendering ? '描画中...' : 'プレビュー'}
      </button>
    </div>

    {/* コード入力 */}
    <textarea
      className="flex-1 px-4 py-3 text-sm font-mono outline-none resize-none border-b border-gray-100"
      placeholder={'graph TD\n  A-->B'}
      value={mermaidCode}
      onChange={(e) => {
        setMermaidCode(e.target.value);
        setMermaidPreviewSvg(null);
      }}
    />

    {/* プレビュー領域 */}
    {mermaidPreviewSvg && (
      <div
        ref={mermaidPreviewRef}
        className="px-4 py-3 overflow-auto"
        dangerouslySetInnerHTML={{ __html: mermaidPreviewSvg }}
      />
    )}
    {mermaidPreviewError && (
      <p className="px-4 py-2 text-red-600 text-sm">{mermaidPreviewError}</p>
    )}

    {/* 挿入ボタン */}
    <div className="px-4 py-4 border-t border-gray-200">
      <button
        className="w-full py-3 rounded-lg bg-blue-600 text-white font-medium disabled:opacity-40"
        disabled={!mermaidCode.trim()}
        onClick={() => {
          if (!textareaRef.current) return;
          const block = `\`\`\`mermaid\n${mermaidCode}\n\`\`\``;
          const newBody = insertAtCursor(textareaRef.current, block);
          setWriteBody(newBody);
          setShowMermaidModal(false);
          setMermaidCode('');
          setMermaidPreviewSvg(null);
        }}
      >
        挿入
      </button>
    </div>
  </div>
)}
```

**3. viewer.test.tsx の SEND-04 describe ブロックを実装:**

```typescript
describe('SEND-04: Mermaid挿入', () => {
  it('insertAtCursor が ```mermaid ブロックを正しく挿入できる', () => {
    const textarea = document.createElement('textarea');
    textarea.value = '前のテキスト';
    textarea.selectionStart = 4;
    textarea.selectionEnd = 4;
    const block = '```mermaid\ngraph TD\n  A-->B\n```';
    const result = insertAtCursor(textarea, block);
    expect(result).toContain('```mermaid');
    expect(result).toContain('graph TD');
  });
});
```

**注意:** 実際のモーダル開閉テストは jsdom での React コンポーネントレンダリングが必要で複雑になる。最小限として `insertAtCursor` の動作を検証するテストを実装する。モーダルの E2E 動作は手動検証（VALIDATION.md の Manual-Only Verifications に記載済み）。
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npx vitest run app/viewer/viewer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>npx vitest run app/viewer/ が 0 failures。page.tsx に showMermaidModal && の全画面モーダルが実装されている。SEND-04 テストが GREEN。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SimpleNoteBody.tsx に Mermaid ブロック描画を追加する（REND-01）</name>
  <files>app/viewer/SimpleNoteBody.tsx, app/viewer/viewer.test.tsx</files>
  <behavior>
    - 本文テキスト内の ```mermaid\n...\n``` ブロックを検出する
    - 各 Mermaid ブロックを MermaidBlock コンポーネントで描画する（useEffect + dynamic import）
    - MermaidBlock は useEffect 内で mermaid.render(`mermaid-${index}`, code) を呼び、containerRef.current.innerHTML = svg で SVG を挿入する
    - 複数ブロックがある場合、index で ID を分ける（mermaid-0, mermaid-1...）
    - mermaid.render() エラー時は赤テキストで「Mermaid エラー: ...」を表示
    - 画像の imgRe 処理とMermaid ブロック処理を組み合わせる（両方対応）
    - 'use client' ディレクティブを追加する（useEffect 使用のため必須）
  </behavior>
  <action>
SimpleNoteBody.tsx の現在の実装（35行）を読み込んでから、以下のように書き換える:

```tsx
'use client';

import React, { useEffect, useRef } from 'react';

// Mermaid ブロック描画コンポーネント
function MermaidBlock({ code, index }: { code: string; index: number }) {
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (!containerRef.current) return;
    const id = `mermaid-${index}-${Date.now()}`;
    import('mermaid')
      .then(({ default: mermaid }) => {
        mermaid.initialize({ startOnLoad: false });
        return mermaid.render(id, code);
      })
      .then(({ svg }) => {
        if (containerRef.current) {
          containerRef.current.innerHTML = svg;
        }
      })
      .catch((err: unknown) => {
        if (containerRef.current) {
          containerRef.current.textContent =
            'Mermaid エラー: ' + (err instanceof Error ? err.message : String(err));
          containerRef.current.style.color = 'red';
        }
      });
  }, [code, index]);

  return <div ref={containerRef} className="my-4 overflow-auto" />;
}

export function SimpleNoteBody({ body }: { body: string }) {
  // テキストを「Mermaidブロック」と「その他」に分割する正規表現
  const mermaidRe = /```mermaid\n([\s\S]*?)```/g;
  const imgRe = /!\[([^\]]*)\]\((data:[^)]+)\)/g;

  const parts: React.ReactNode[] = [];
  let lastIndex = 0;
  let key = 0;
  let mermaidIndex = 0;

  // Mermaid ブロックを先に抽出して処理
  let match: RegExpExecArray | null;
  // mermaidRe と imgRe を同時に処理するため、全マッチを一旦収集してソート
  type Segment =
    | { type: 'mermaid'; start: number; end: number; code: string }
    | { type: 'img'; start: number; end: number; alt: string; src: string };

  const segments: Segment[] = [];

  mermaidRe.lastIndex = 0;
  while ((match = mermaidRe.exec(body)) !== null) {
    segments.push({
      type: 'mermaid',
      start: match.index,
      end: match.index + match[0].length,
      code: match[1],
    });
  }

  imgRe.lastIndex = 0;
  while ((match = imgRe.exec(body)) !== null) {
    segments.push({
      type: 'img',
      start: match.index,
      end: match.index + match[0].length,
      alt: match[1],
      src: match[2],
    });
  }

  // 位置順にソート
  segments.sort((a, b) => a.start - b.start);

  lastIndex = 0;
  for (const seg of segments) {
    if (seg.start > lastIndex) {
      parts.push(
        <span key={key++} style={{ whiteSpace: 'pre-wrap' }}>
          {body.slice(lastIndex, seg.start)}
        </span>
      );
    }
    if (seg.type === 'mermaid') {
      parts.push(<MermaidBlock key={key++} code={seg.code} index={mermaidIndex++} />);
    } else {
      // eslint-disable-next-line @next/next/no-img-element
      parts.push(
        <img
          key={key++}
          src={seg.src}
          alt={seg.alt}
          style={{ maxWidth: '100%', display: 'block', margin: '8px 0' }}
        />
      );
    }
    lastIndex = seg.end;
  }

  if (lastIndex < body.length) {
    parts.push(
      <span key={key++} style={{ whiteSpace: 'pre-wrap' }}>
        {body.slice(lastIndex)}
      </span>
    );
  }

  return <div className="mt-4">{parts}</div>;
}
```

**viewer.test.tsx の REND-01 describe ブロックを実装:**

既存の REND-01 it.todo を置き換え:

```typescript
describe('REND-01: Mermaidレンダリング', () => {
  it('SimpleNoteBody が通常テキストを pre-wrap で描画する（既存動作を保持）', () => {
    const { container } = render(<SimpleNoteBody body={'テスト\nテキスト'} />);
    expect(container.textContent).toContain('テスト');
  });

  it('SimpleNoteBody が ```mermaid ブロックをMermaidBlock要素として描画する', () => {
    const body = 'テキスト前\n```mermaid\ngraph TD\n  A-->B\n```\nテキスト後';
    const { container } = render(<SimpleNoteBody body={body} />);
    // MermaidBlock div が存在する（mermaid.render は vi.mock でモック済み）
    // テキスト前後が残っている
    expect(container.textContent).toContain('テキスト前');
    expect(container.textContent).toContain('テキスト後');
  });

  it('data: URI 画像と mermaid ブロックを同じ本文内で両方処理する', () => {
    const body = '![img](data:image/png;base64,abc)\n```mermaid\ngraph LR\n  X-->Y\n```';
    const { container } = render(<SimpleNoteBody body={body} />);
    const imgs = container.querySelectorAll('img');
    expect(imgs.length).toBe(1);
    // MermaidBlock の div も存在する
    expect(container.querySelectorAll('div').length).toBeGreaterThanOrEqual(1);
  });
});
```
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npx vitest run app/viewer/viewer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>npx vitest run app/viewer/ が 0 failures。SimpleNoteBody.tsx に MermaidBlock コンポーネントと mermaid dynamic import が実装されている。REND-01 テストが GREEN。</done>
</task>

</tasks>

<verification>
1. `npx vitest run app/viewer/` — 0 failures
2. `grep -n "import('mermaid')" app/viewer/SimpleNoteBody.tsx` — 1 行ヒット
3. `grep -n "use client" app/viewer/SimpleNoteBody.tsx` — 1 行ヒット（先頭行）
4. `grep -n "showMermaidModal" app/viewer/page.tsx` — 複数ヒット（state 宣言 + 表示ブロック）
</verification>

<success_criteria>
- Mermaid モーダルが page.tsx に実装されている（全画面、コード入力・プレビュー・挿入）
- SimpleNoteBody が ```mermaid ブロックを MermaidBlock コンポーネントで描画する
- SEND-04 と REND-01 のテストが GREEN
- 既存の SimpleNoteBody テスト（画像表示）が壊れていない
- 0 test failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-iphone-send-ui/06-04-SUMMARY.md`
</output>
