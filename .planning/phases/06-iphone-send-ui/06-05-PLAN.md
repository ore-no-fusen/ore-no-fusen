---
phase: 06-iphone-send-ui
plan: "05"
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - app/viewer/page.tsx
  - app/viewer/viewer.test.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02

must_haves:
  truths:
    - "list ステップで fusen_iphone_notes.json の最新10件が表示される"
    - "sent/draft バッジが正しく表示される"
    - "相対時刻（例: '3分前'）が表示される"
    - "draft タップで write ステップに遷移し、title と body が復元される"
    - "sent タップは何も起きない"
    - "履歴ファイルが存在しない（初回）場合は空リストが表示される"
    - "「← 戻る」で write ステップに戻る"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "list ステップ UI と履歴ロード・タップハンドラ"
      contains: "step === 'list'"
  key_links:
    - from: "list ステップ"
      to: "downloadFromDrive(token, 'fusen_iphone_notes.json')"
      via: "useEffect または入場時の async 処理"
      pattern: "fusen_iphone_notes"
    - from: "draft タップ"
      to: "setStep('write') + setWriteTitle + setWriteBody"
      via: "onClick ハンドラ"
      pattern: "setStep\\('write'\\)"
---

<objective>
list ステップ（履歴画面）を実装する: HIST-01（履歴表示）と HIST-02（下書き編集再開）。

Purpose: 送信済みと下書きの一覧を見せ、下書きを編集再開できる。

Output: page.tsx の list ステップ UI、HIST-01/HIST-02 テスト GREEN
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-iphone-send-ui/06-CONTEXT.md
@.planning/phases/06-iphone-send-ui/06-RESEARCH.md
@.planning/phases/06-iphone-send-ui/06-02-SUMMARY.md
@.planning/phases/06-iphone-send-ui/06-03-SUMMARY.md
@.planning/phases/06-iphone-send-ui/06-04-SUMMARY.md

<interfaces>
<!-- Plan 02 で確立済みの型と関数 -->

```typescript
type IphoneNote = {
  id: string;
  status: 'sent' | 'draft';
  title: string;
  body: string;
  created_at: string;
  sent_at?: string;
};

// downloadFromDrive(token, 'fusen_iphone_notes.json') → { notes: IphoneNote[] }
// 初回（ファイル未作成）: .catch(() => []) でフォールバック
```

<!-- State（Plan 02 で追加済み）-->
```typescript
const [writeTitle, setWriteTitle] = useState('');
const [writeBody, setWriteBody] = useState('');
const [step, setStep] = useState<...'write' | 'list'...>('banner');
const [accessToken, setAccessToken] = useState<string | null>(null);
```

<!-- 相対時刻フォーマット（RESEARCH.md Code Examples）-->
```typescript
function formatRelativeTime(isoString: string): string {
  const diff = Date.now() - new Date(isoString).getTime();
  const rtf = new Intl.RelativeTimeFormat('ja', { numeric: 'auto' });
  if (diff < 60_000) return rtf.format(-Math.floor(diff / 1000), 'seconds');
  if (diff < 3_600_000) return rtf.format(-Math.floor(diff / 60_000), 'minutes');
  if (diff < 86_400_000) return rtf.format(-Math.floor(diff / 3_600_000), 'hours');
  return rtf.format(-Math.floor(diff / 86_400_000), 'days');
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatRelativeTime ヘルパーを追加してエクスポートする</name>
  <files>app/viewer/page.tsx, app/viewer/viewer.test.tsx</files>
  <behavior>
    - formatRelativeTime('2026-03-29T00:00:00Z') が '〇〇日前' 形式の日本語を返す
    - 60秒未満: '〇秒前'
    - 1時間未満: '〇分前'
    - 1日未満: '〇時間前'
    - それ以上: '〇日前'
  </behavior>
  <action>
page.tsx の `resizeImageToBase64` 関数の後（または `insertAtCursor` の後）に `formatRelativeTime` を追加し、`export` する:

```typescript
export function formatRelativeTime(isoString: string): string {
  const diff = Date.now() - new Date(isoString).getTime();
  const rtf = new Intl.RelativeTimeFormat('ja', { numeric: 'auto' });
  if (diff < 60_000) return rtf.format(-Math.floor(diff / 1000), 'seconds');
  if (diff < 3_600_000) return rtf.format(-Math.floor(diff / 60_000), 'minutes');
  if (diff < 86_400_000) return rtf.format(-Math.floor(diff / 3_600_000), 'hours');
  return rtf.format(-Math.floor(diff / 86_400_000), 'days');
}
```

viewer.test.tsx に formatRelativeTime の基本テストを追加:

```typescript
import { formatRelativeTime } from './page';

describe('formatRelativeTime', () => {
  it('1時間前の ISO 文字列を「1時間前」として返す', () => {
    const oneHourAgo = new Date(Date.now() - 3_600_000).toISOString();
    const result = formatRelativeTime(oneHourAgo);
    expect(result).toContain('時間前');
  });

  it('2日前の ISO 文字列を「2日前」として返す', () => {
    const twoDaysAgo = new Date(Date.now() - 2 * 86_400_000).toISOString();
    const result = formatRelativeTime(twoDaysAgo);
    expect(result).toContain('日前');
  });
});
```
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npx vitest run app/viewer/viewer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>formatRelativeTime がエクスポートされ、2 テストが GREEN。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: list ステップ UI と HIST-01/HIST-02 テスト実装</name>
  <files>app/viewer/page.tsx, app/viewer/viewer.test.tsx</files>
  <behavior>
    - list ステップ入場時に downloadFromDrive で履歴をロードする（useEffect の step 依存で実行）
    - 最新10件のみ表示（.slice(0, 10)）
    - 各アイテム: [sent/draft バッジ] + タイムスタンプ + 本文冒頭20文字
    - draft タップ → setWriteTitle + setWriteBody → setStep('write')
    - sent タップ → 何もしない
    - ロード中: 「読み込み中...」表示
    - 空リスト: 「まだ履歴がありません」表示
    - ヘッダー左「← 戻る」→ setStep('write')
  </behavior>
  <action>
page.tsx を読み込む。

**1. 履歴ロード用 state を追加:**
```typescript
const [historyNotes, setHistoryNotes] = useState<IphoneNote[]>([]);
const [isHistoryLoading, setIsHistoryLoading] = useState(false);
```

**2. useEffect に step === 'list' の分岐を追加（既存の useEffect は `deps: []` のため、新しい useEffect を追加する）:**

```typescript
useEffect(() => {
  if (step !== 'list' || !accessToken) return;
  setIsHistoryLoading(true);
  downloadFromDrive(accessToken, 'fusen_iphone_notes.json')
    .then((data) => setHistoryNotes((data.notes ?? []).slice(0, 10)))
    .catch(() => setHistoryNotes([]))
    .finally(() => setIsHistoryLoading(false));
}, [step, accessToken]);
```

**3. list ステップのレンダリングを追加（write ステップの直後）:**

```tsx
{step === 'list' && (
  <div className="flex flex-col min-h-[100dvh] bg-white">
    {/* ヘッダー */}
    <div className="flex items-center justify-between px-4 py-3 border-b border-gray-200">
      <button
        className="text-blue-600 text-sm font-medium"
        onClick={() => setStep('write')}
      >
        ← 戻る
      </button>
      <span className="font-semibold text-gray-900">履歴</span>
      <div className="w-12" />
    </div>

    {/* コンテンツ */}
    <div className="flex-1 overflow-y-auto">
      {isHistoryLoading ? (
        <p className="text-center text-gray-400 py-8 text-sm">読み込み中...</p>
      ) : historyNotes.length === 0 ? (
        <p className="text-center text-gray-400 py-8 text-sm">まだ履歴がありません</p>
      ) : (
        <ul>
          {historyNotes.map((note) => (
            <li
              key={note.id}
              className={`px-4 py-3 border-b border-gray-100 ${
                note.status === 'draft'
                  ? 'cursor-pointer active:bg-gray-50'
                  : 'cursor-default'
              }`}
              onClick={() => {
                if (note.status !== 'draft') return;
                setWriteTitle(note.title);
                setWriteBody(note.body);
                setStep('write');
              }}
            >
              <div className="flex items-center gap-2 mb-1">
                <span
                  className={`text-xs font-medium px-1.5 py-0.5 rounded ${
                    note.status === 'sent'
                      ? 'bg-blue-100 text-blue-700'
                      : 'bg-yellow-100 text-yellow-700'
                  }`}
                >
                  {note.status === 'sent' ? '送信済み' : '下書き'}
                </span>
                <span className="text-xs text-gray-400">
                  {formatRelativeTime(note.created_at)}
                </span>
              </div>
              <p className="text-sm text-gray-700 truncate">
                {(note.title || note.body).slice(0, 20) || '（空のメモ）'}
              </p>
            </li>
          ))}
        </ul>
      )}
    </div>
  </div>
)}
```

**4. viewer.test.tsx の HIST-01 と HIST-02 describe ブロックを実装:**

```typescript
describe('HIST-01: 履歴表示', () => {
  it('fusen_iphone_notes.json の notes 配列から最新10件のみ取得する', () => {
    // スライスロジックのユニットテスト
    const notes: IphoneNote[] = Array.from({ length: 15 }, (_, i) => ({
      id: String(i),
      status: 'sent' as const,
      title: `note ${i}`,
      body: '',
      created_at: new Date().toISOString(),
    }));
    const displayed = notes.slice(0, 10);
    expect(displayed).toHaveLength(10);
  });

  it('notes が undefined の場合は空配列になる', () => {
    const data: { notes?: IphoneNote[] } = {};
    const notes = data.notes ?? [];
    expect(notes).toHaveLength(0);
  });
});

describe('HIST-02: 下書き編集', () => {
  it('draft ステータスのノートのみクリック時に title と body が返される', () => {
    const draftNote: IphoneNote = {
      id: 'draft-1',
      status: 'draft',
      title: 'テストタイトル',
      body: 'テスト本文',
      created_at: new Date().toISOString(),
    };
    // ハンドラのロジックをユニットテスト
    let title = '';
    let body = '';
    const handleTap = (note: IphoneNote) => {
      if (note.status !== 'draft') return;
      title = note.title;
      body = note.body;
    };
    handleTap(draftNote);
    expect(title).toBe('テストタイトル');
    expect(body).toBe('テスト本文');
  });

  it('sent ステータスのノートはタップしても何も起きない', () => {
    const sentNote: IphoneNote = {
      id: 'sent-1',
      status: 'sent',
      title: '送信済み',
      body: '本文',
      created_at: new Date().toISOString(),
      sent_at: new Date().toISOString(),
    };
    let triggered = false;
    const handleTap = (note: IphoneNote) => {
      if (note.status !== 'draft') return;
      triggered = true;
    };
    handleTap(sentNote);
    expect(triggered).toBe(false);
  });
});
```
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npx vitest run app/viewer/viewer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>npx vitest run app/viewer/ が 0 failures。page.tsx に step === 'list' のレンダリングブロックが存在する。HIST-01/HIST-02 テストが GREEN。</done>
</task>

</tasks>

<verification>
1. `npx vitest run app/viewer/` — 0 failures
2. `npx vitest run` — 全スイートが 0 failures
3. `grep -n "step === 'list'" app/viewer/page.tsx` — 1 行ヒット
4. `grep -n "fusen_iphone_notes" app/viewer/page.tsx` — 複数ヒット（saveToHistory + list ロード）
5. `grep -n "setStep.*write" app/viewer/page.tsx` — 複数ヒット（push完了・note消す・list戻る・draft編集）
</verification>

<success_criteria>
- list ステップが表示される（最新10件・sent/draft バッジ・相対時刻・冒頭テキスト）
- draft タップで write ステップに遷移し title/body が復元される
- sent タップは何も起きない
- 空リスト（初回）で「まだ履歴がありません」表示
- HIST-01/HIST-02 テストが GREEN
- 全テストスイートが 0 failures（npx vitest run）
</success_criteria>

<output>
After completion, create `.planning/phases/06-iphone-send-ui/06-05-SUMMARY.md`
</output>
