---
phase: 06-iphone-send-ui
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - app/viewer/viewer.test.tsx
autonomous: true
requirements:
  - SEND-01
  - SEND-02
  - SEND-03
  - SEND-04
  - HIST-01
  - HIST-02
  - REND-01

must_haves:
  truths:
    - "全テストスタブが RED（.todo）状態でファイルに存在し、npx vitest run app/viewer/ が 0 failures で終わる"
    - "mermaid と Canvas API の jsdom モックが設定されている"
    - "IphoneNote / IphoneNotesFile / FromIphoneFile 型定義が test ファイル内または types ファイルに存在する"
  artifacts:
    - path: "app/viewer/viewer.test.tsx"
      provides: "Phase 6 全要件のテストスタブ（it.todo ブロック）"
      contains: "SEND-01, SEND-02, SEND-03, SEND-04, HIST-01, HIST-02, REND-01"
  key_links:
    - from: "app/viewer/viewer.test.tsx"
      to: "app/viewer/page.tsx"
      via: "import ViewerPage (コメントアウト解除は Wave 1 以降)"
      pattern: "vi.mock.*uploadToDrive|downloadFromDrive"
---

<objective>
Wave 0: 全テストスタブを先行作成する。

Purpose: Nyquist ルール準拠。Wave 1 以降の実装タスクが automated verify を持てるよう、テストケースを先に定義する。RED 状態のスタブを作り、各 Wave の実装後に GREEN にする。

Output: 更新された app/viewer/viewer.test.tsx（既存テストは壊さず、Phase 6 の it.todo ブロックを追加）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-iphone-send-ui/06-CONTEXT.md
@.planning/phases/06-iphone-send-ui/06-RESEARCH.md
@.planning/phases/06-iphone-send-ui/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 6 テストスタブを viewer.test.tsx に追加する</name>
  <files>app/viewer/viewer.test.tsx</files>
  <action>
既存の viewer.test.tsx（44行）を読み込んでから、ファイル末尾に以下を追加する。既存テストは一切変更しない。

追加内容:

1. **モックセットアップ（ファイル先頭の beforeEach に追記、または新しい describe の beforeEach 内に追加）:**
   - `vi.mock('mermaid', () => ({ default: { initialize: vi.fn(), render: vi.fn().mockResolvedValue({ svg: '<svg>mock</svg>' }) } }))` をファイルの最上部（imports の直後）に追加
   - HTMLCanvasElement のモック: 既存 beforeEach（または新しい beforeEach）内に `HTMLCanvasElement.prototype.getContext = vi.fn().mockReturnValue({ drawImage: vi.fn(), canvas: { toDataURL: vi.fn().mockReturnValue('data:image/jpeg;base64,mock') } })` を追加

2. **型定義（テストファイル内に直接記述）:**
   ```typescript
   type IphoneNote = {
     id: string;
     status: 'sent' | 'draft';
     title: string;
     body: string;
     created_at: string;
     sent_at?: string;
   };
   ```

3. **SEND-01 スタブ:**
   ```
   describe('SEND-01: PCに送る', () => {
     it.todo('「PCに送る」を押すと uploadToDrive が fusen_from_iphone.json に正しいペイロードで呼ばれる');
     it.todo('送信成功後に body/title がクリアされる');
     it.todo('送信中はボタンが「送信中...」になり disabled になる');
   });
   ```

4. **SEND-02 スタブ:**
   ```
   describe('SEND-02: iPhoneに置いておく', () => {
     it.todo('「iPhoneに置いておく」を押すと uploadToDrive が fusen_iphone_notes.json に status:"draft" で呼ばれる');
     it.todo('下書き保存後に list ステップに遷移する');
   });
   ```

5. **SEND-03 スタブ:**
   ```
   describe('SEND-03: 画像添付', () => {
     it.todo('resizeImageToBase64 が Canvas API を使って base64 文字列を返す');
     it.todo('画像選択後にカーソル位置に ![](data:...) が挿入される');
   });
   ```

6. **SEND-04 スタブ:**
   ```
   describe('SEND-04: Mermaid挿入', () => {
     it.todo('Mermaidボタンでモーダルが開く');
     it.todo('プレビューボタンで mermaid.render() が呼ばれる');
     it.todo('挿入ボタンでカーソル位置に ```mermaid ブロックが挿入される');
   });
   ```

7. **HIST-01 スタブ:**
   ```
   describe('HIST-01: 履歴表示', () => {
     it.todo('listステップで fusen_iphone_notes.json の最新10件が表示される');
     it.todo('sent/draft バッジが正しく表示される');
     it.todo('履歴ファイルが存在しない（初回）場合は空リストが表示される');
   });
   ```

8. **HIST-02 スタブ:**
   ```
   describe('HIST-02: 下書き編集', () => {
     it.todo('draft アイテムをタップすると write ステップに遷移する');
     it.todo('write ステップに遷移後、下書きの title と body が復元される');
   });
   ```

9. **REND-01 スタブ:**
   ```
   describe('REND-01: Mermaidレンダリング', () => {
     it.todo('SimpleNoteBody が ```mermaid ブロックを検出して mermaid.render() を呼ぶ');
     it.todo('SVG が DOM に挿入される');
     it.todo('複数の mermaid ブロックがある場合、それぞれユニーク ID で描画される');
   });
   ```

**重要な注意事項:**
- `it.todo` のスタブは引数が1つ（説明文のみ）。実装は Wave 1 以降
- 既存の `describe('ViewerPage')`, `describe('ServiceWorker push handler')`, `describe('SimpleNoteBody')` は一切変更しない
- mermaid の vi.mock は ファイル内の describe ブロックより前（ファイルの最上部）に置く
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npx vitest run app/viewer/viewer.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>npx vitest run app/viewer/viewer.test.tsx が 0 failures で完了する。it.todo ブロックが全 7 describe グループに存在する。</done>
</task>

</tasks>

<verification>
npx vitest run app/viewer/viewer.test.tsx — 0 failures（既存テストが壊れていない + todo スタブが認識される）
</verification>

<success_criteria>
- viewer.test.tsx に SEND-01〜04, HIST-01〜02, REND-01 の it.todo スタブが存在する
- vi.mock('mermaid') と Canvas API モックのセットアップが追加されている
- IphoneNote 型定義がテストファイル内に存在する
- npx vitest run app/viewer/ が 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-iphone-send-ui/06-01-SUMMARY.md`
</output>
