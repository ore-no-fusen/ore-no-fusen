---
phase: 06-iphone-send-ui
plan: "02"
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - app/viewer/page.tsx
autonomous: true
requirements:
  - SEND-01
  - SEND-02

must_haves:
  truths:
    - "PWAアイコンタップ後（ログイン済み）に write ステップが表示される"
    - "push ステップ完了後に write ステップに遷移する（ready ではなく）"
    - "note「消す」後に write ステップに遷移する"
    - "write 画面にヘッダー・タイトル入力・本文テキストエリア・添付ツールバー・アクションボタンが表示される"
    - "「PCに送る」で fusen_from_iphone.json が Drive に書き込まれ、成功後に入力欄がクリアされる"
    - "「iPhoneに置いておく」で fusen_iphone_notes.json に draft が保存され、list ステップに遷移する"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "write/list ステップ型追加、write 画面 UI、送信/下書き保存ロジック"
      contains: "step === 'write'"
  key_links:
    - from: "push ステップの setStep('ready')"
      to: "setStep('write')"
      via: "コード変更（line 385 付近）"
      pattern: "setStep\\('write'\\)"
    - from: "note「消す」ボタンの setStep('ready')"
      to: "setStep('write')"
      via: "コード変更（line 426 付近）"
      pattern: "setStep\\('write'\\)"
    - from: "「PCに送る」ボタン"
      to: "uploadToDrive(token, 'fusen_from_iphone.json', {...})"
      via: "onClick ハンドラ内 async 処理"
      pattern: "uploadToDrive.*fusen_from_iphone"
    - from: "「iPhoneに置いておく」ボタン"
      to: "uploadToDrive(token, 'fusen_iphone_notes.json', {...})"
      via: "saveToHistory() ヘルパー呼び出し"
      pattern: "uploadToDrive.*fusen_iphone_notes"
---

<objective>
viewer/page.tsx にwrite ステップを追加し、PCへの送信と下書き保存を実装する。

Purpose: Phase 6 の中核。「書く」画面がホームになり、iPhoneからDriveへの送信フローが完成する。

Output: write ステップの UI とロジック、push/note の遷移修正、saveToHistory ヘルパー
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-iphone-send-ui/06-CONTEXT.md
@.planning/phases/06-iphone-send-ui/06-RESEARCH.md

<interfaces>
<!-- 既存 viewer/page.tsx から抽出した重要なコントラクト -->

既存の step 型（line 148-150）:
```typescript
const [step, setStep] = useState<
  'banner' | 'login' | 'push' | 'ready' | 'note'
>('banner');
```

変更後の step 型（'write' と 'list' を追加、'ready' は型に残すが遷移先から除去）:
```typescript
const [step, setStep] = useState<
  'banner' | 'login' | 'push' | 'ready' | 'write' | 'list' | 'note'
>('banner');
```

既存ヘルパー（再利用可能）:
```typescript
// line 48-83
async function uploadToDrive(accessToken: string, fileName: string, data: object): Promise<void>

// line 85-100
async function downloadFromDrive(accessToken: string, fileName: string): Promise<any>

// line 106-130
async function refreshAccessToken(): Promise<string | null>
```

修正が必要な既存コード:
- line 385: `setStep('ready')` → `setStep('write')` （push ステップ完了後）
- line 426: `setStep('ready')` → `setStep('write')` （note「消す」後）

Drive ファイルスキーマ（RESEARCH.md Pattern 4 & 5 より）:
```typescript
type IphoneNote = {
  id: string;           // crypto.randomUUID()
  status: 'sent' | 'draft';
  title: string;
  body: string;
  created_at: string;   // ISO 8601
  sent_at?: string;
};

type FromIphoneFile = {
  id: string;
  title: string;
  body: string;
  sent_at: string;     // ISO 8601
  received_at?: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: step 型拡張・遷移修正・ヘルパー追加</name>
  <files>app/viewer/page.tsx</files>
  <behavior>
    - push 完了後（line 385）: setStep('write') になる（'ready' ではない）
    - note「消す」後（line 426）: setStep('write') になる（'ready' ではない）
    - saveToHistory(token, note) で downloadFromDrive → unshift → slice(0,50) → uploadToDrive の順で処理
    - 初回（fusen_iphone_notes.json なし）: catch で空配列にフォールバック
    - uploadWithAutoRefresh(fileName, data) で uploadToDrive 失敗時に refreshAccessToken → retry
  </behavior>
  <action>
page.tsx を読み込み、以下の最小変更を行う:

**1. step 型に 'write' | 'list' を追加（'ready' は型に残す）:**
```typescript
const [step, setStep] = useState<
  'banner' | 'login' | 'push' | 'ready' | 'write' | 'list' | 'note'
>('banner');
```

**2. push ステップ完了後の遷移を修正（line 385 付近）:**
`setStep('ready')` → `setStep('write')`

**3. note「消す」後の遷移を修正（line 426 付近）:**
`setStep('ready')` → `setStep('write')`

**4. 通常フロー（token あり）の遷移を修正（line 282-283 付近）:**
現在: `setStep('push')` — push 完了ずみのユーザーは write に直接飛ばす。
ただし push 設定済みかどうかの判定は `localStorage.getItem('viewer_push_done')` フラグで行う:
- `viewer_push_done` が `'true'` → `setAccessToken(token); setStep('write')`
- なし → `setAccessToken(token); setStep('push')`（初回セットアップ）

push ステップ完了時に `localStorage.setItem('viewer_push_done', 'true')` を追加する。

**5. ヘルパー関数を追加（uploadToDrive の直後に）:**

```typescript
// Drive 書き込み（トークン期限切れ時に自動リフレッシュ）
async function uploadWithAutoRefresh(
  token: string,
  fileName: string,
  data: object
): Promise<void> {
  try {
    await uploadToDrive(token, fileName, data);
  } catch {
    const newToken = await refreshAccessToken();
    if (!newToken) throw new Error('session expired');
    await uploadToDrive(newToken, fileName, data);
  }
}

// iPhone 履歴への追記（最新50件上限、ID重複排除）
async function saveToHistory(token: string, note: IphoneNote): Promise<void> {
  const existing: IphoneNote[] = await downloadFromDrive(token, 'fusen_iphone_notes.json')
    .then((data) => data.notes ?? [])
    .catch(() => []);
  const filtered = existing.filter((n) => n.id !== note.id);
  const updated = [note, ...filtered].slice(0, 50);
  await uploadWithAutoRefresh(token, 'fusen_iphone_notes.json', { notes: updated });
}
```

**6. 型定義を追加（ヘルパー関数より前、コンポーネント定義より前）:**

```typescript
type IphoneNote = {
  id: string;
  status: 'sent' | 'draft';
  title: string;
  body: string;
  created_at: string;
  sent_at?: string;
};
```

**注意:** `saveToHistory` と `uploadWithAutoRefresh` は既存の `downloadFromDrive` / `uploadToDrive` / `refreshAccessToken` を使う。新規 fetch は書かない。
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npx vitest run app/viewer/viewer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>npx vitest run app/viewer/ が 0 failures。page.tsx に 'write' と 'list' が step 型に含まれている。push 完了後と note「消す」後の遷移が setStep('write') になっている。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write ステップ UI と送信/下書きロジック</name>
  <files>app/viewer/page.tsx</files>
  <behavior>
    - step === 'write' で write 画面が表示される
    - ヘッダー左に「📋 履歴」ボタン（setStep('list')）、中央に「書く」
    - タイトル入力（1行、任意）と本文テキストエリア（flex-1 で画面の大半）
    - 添付ツールバー: 📷ボタン（hidden input[type=file accept="image/*"] をトリガー）と「Mermaid」ボタン（モーダル開閉 — モーダル実装は Plan 04 に委ねるため、この時点では `setShowMermaidModal(true)` のみ）
    - 「PCに送る」: uploadWithAutoRefresh で fusen_from_iphone.json に送信 → saveToHistory で sent 記録 → 入力クリア → 成功メッセージ表示
    - 「iPhoneに置いておく」: saveToHistory で draft 保存 → setStep('list')
    - iOS キーボード対応: min-h-[100dvh] + flex flex-col でレイアウト
  </behavior>
  <action>
page.tsx を読み込み、`{step === 'ready' && (...)}` ブロックの直後に `{step === 'write' && (...)}` ブロックを追加する。

**必要な state 追加（ViewerPage コンポーネント内）:**
```typescript
const [writeTitle, setWriteTitle] = useState('');
const [writeBody, setWriteBody] = useState('');
const [sendSuccess, setSendSuccess] = useState(false);
const [showMermaidModal, setShowMermaidModal] = useState(false);
const textareaRef = React.useRef<HTMLTextAreaElement>(null);
const fileInputRef = React.useRef<HTMLInputElement>(null);
```

**write ステップのレンダリング（全体構成）:**
```tsx
{step === 'write' && (
  <div className="flex flex-col min-h-[100dvh] bg-white">
    {/* ヘッダー */}
    <div className="flex items-center justify-between px-4 py-3 border-b border-gray-200">
      <button
        className="text-blue-600 text-sm font-medium"
        onClick={() => setStep('list')}
      >
        📋 履歴
      </button>
      <span className="font-semibold text-gray-900">書く</span>
      <div className="w-12" /> {/* 右側スペーサー */}
    </div>

    {/* タイトル入力 */}
    <input
      type="text"
      placeholder="タイトル（任意）"
      value={writeTitle}
      onChange={(e) => setWriteTitle(e.target.value)}
      className="px-4 py-3 border-b border-gray-100 text-base outline-none"
    />

    {/* 本文テキストエリア */}
    <textarea
      ref={textareaRef}
      placeholder="メモを書く..."
      value={writeBody}
      onChange={(e) => setWriteBody(e.target.value)}
      className="flex-1 px-4 py-3 text-base outline-none resize-none"
    />

    {/* 添付ツールバー */}
    <div className="flex gap-3 px-4 py-2 border-t border-gray-100">
      <button
        className="text-2xl"
        onClick={() => fileInputRef.current?.click()}
        aria-label="画像を追加"
      >
        📷
      </button>
      <button
        className="text-sm font-medium text-blue-600 border border-blue-300 rounded px-3 py-1"
        onClick={() => setShowMermaidModal(true)}
      >
        Mermaid
      </button>
      {/* hidden file input */}
      <input
        ref={fileInputRef}
        type="file"
        accept="image/*"
        className="hidden"
        onChange={async (e) => {
          const file = e.target.files?.[0];
          if (!file || !textareaRef.current) return;
          try {
            const base64 = await resizeImageToBase64(file);
            const insertion = `![](${base64})`;
            const newBody = insertAtCursor(textareaRef.current, insertion);
            setWriteBody(newBody);
          } catch {
            setErrorMessage('画像の処理に失敗しました');
          } finally {
            e.target.value = '';
          }
        }}
      />
    </div>

    {/* 成功メッセージ */}
    {sendSuccess && (
      <p className="text-center text-green-600 text-sm py-1">送信しました！</p>
    )}
    {errorMessage && (
      <p className="text-center text-red-600 text-sm py-1">{errorMessage}</p>
    )}

    {/* アクションボタン */}
    <div className="flex gap-3 px-4 py-4 border-t border-gray-200">
      <button
        className="flex-1 py-3 rounded-lg bg-gray-100 text-gray-700 font-medium disabled:opacity-40"
        disabled={isLoading}
        onClick={async () => {
          if (!accessToken) return;
          setIsLoading(true);
          setErrorMessage(null);
          try {
            const note: IphoneNote = {
              id: crypto.randomUUID(),
              status: 'draft',
              title: writeTitle,
              body: writeBody,
              created_at: new Date().toISOString(),
            };
            await saveToHistory(accessToken, note);
            setStep('list');
          } catch (err: unknown) {
            setErrorMessage('保存に失敗しました: ' + (err instanceof Error ? err.message : String(err)));
          } finally {
            setIsLoading(false);
          }
        }}
      >
        iPhoneに置いておく
      </button>
      <button
        className="flex-1 py-3 rounded-lg bg-blue-600 text-white font-medium disabled:opacity-40"
        disabled={isLoading}
        onClick={async () => {
          if (!accessToken) return;
          setIsLoading(true);
          setErrorMessage(null);
          setSendSuccess(false);
          try {
            const noteId = crypto.randomUUID();
            const sentAt = new Date().toISOString();
            await uploadWithAutoRefresh(accessToken, 'fusen_from_iphone.json', {
              id: noteId,
              title: writeTitle,
              body: writeBody,
              sent_at: sentAt,
            });
            const note: IphoneNote = {
              id: noteId,
              status: 'sent',
              title: writeTitle,
              body: writeBody,
              created_at: sentAt,
              sent_at: sentAt,
            };
            await saveToHistory(accessToken, note);
            setWriteTitle('');
            setWriteBody('');
            setSendSuccess(true);
            setTimeout(() => setSendSuccess(false), 3000);
          } catch (err: unknown) {
            setErrorMessage('送信に失敗しました: ' + (err instanceof Error ? err.message : String(err)));
          } finally {
            setIsLoading(false);
          }
        }}
      >
        {isLoading ? '送信中...' : 'PCに送る'}
      </button>
    </div>
  </div>
)}
```

**ヘルパー関数を追加（型定義の後、コンポーネント定義の前）:**

```typescript
// 画像をリサイズして base64 文字列に変換（RESEARCH.md Pattern 3）
function resizeImageToBase64(file: File, maxWidth = 800): Promise<string> {
  return new Promise((resolve, reject) => {
    const img = new Image();
    const url = URL.createObjectURL(file);
    img.onload = () => {
      const scale = Math.min(1, maxWidth / img.width);
      const canvas = document.createElement('canvas');
      canvas.width = img.width * scale;
      canvas.height = img.height * scale;
      const ctx = canvas.getContext('2d')!;
      ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
      URL.revokeObjectURL(url);
      resolve(canvas.toDataURL('image/jpeg', 0.7));
    };
    img.onerror = reject;
    img.src = url;
  });
}

// テキストエリアのカーソル位置に文字列を挿入（RESEARCH.md Pattern 6）
function insertAtCursor(el: HTMLTextAreaElement, insertion: string): string {
  const { selectionStart, selectionEnd, value } = el;
  const newValue =
    value.slice(0, selectionStart) + insertion + value.slice(selectionEnd);
  requestAnimationFrame(() => {
    const pos = selectionStart + insertion.length;
    el.selectionStart = pos;
    el.selectionEnd = pos;
  });
  return newValue;
}
```

**注意:** Mermaid モーダル（`showMermaidModal` の実際の UI）は Plan 04 が実装する。この Plan では `setShowMermaidModal(true)` 呼び出しと state 宣言のみ。モーダル UI は `{showMermaidModal && <MermaidModal ... />}` のプレースホルダーで OK（Plan 04 が差し替える）。
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npx vitest run app/viewer/viewer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>npx vitest run app/viewer/ が 0 failures。page.tsx に step === 'write' のレンダリングブロックが存在し、「PCに送る」と「iPhoneに置いておく」ボタンが含まれている。</done>
</task>

</tasks>

<verification>
1. `npx vitest run app/viewer/` — 0 failures
2. page.tsx に 'write' が step 型に含まれている: `grep -n "write" app/viewer/page.tsx`
3. push 完了後の遷移が write: `grep -n "setStep('write')" app/viewer/page.tsx`
4. fusen_from_iphone.json への書き込み: `grep -n "fusen_from_iphone" app/viewer/page.tsx`
5. fusen_iphone_notes.json への書き込み: `grep -n "fusen_iphone_notes" app/viewer/page.tsx`
</verification>

<success_criteria>
- push ステップ完了後に write ステップに遷移する（ready ではない）
- note「消す」後に write ステップに遷移する
- write 画面がヘッダー・タイトル・本文・ツールバー・アクションボタンの構成で表示される
- 「PCに送る」が fusen_from_iphone.json に送信し、成功後に入力をクリアする
- 「iPhoneに置いておく」が fusen_iphone_notes.json に draft として保存し、list ステップに遷移する
- 0 test failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-iphone-send-ui/06-02-SUMMARY.md`
</output>
