---
phase: 09-iphone-fusen-kanri
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/viewer/__tests__/page.test.tsx
  - app/viewer/page.tsx
autonomous: true
requirements:
  - IPHONE-MGT-01

must_haves:
  truths:
    - "一覧からノートをタップすると、write 画面に遷移しエディタに内容が読み込まれる（現在のバグが修正される）"
    - "pendingHydrate state が非null のとき、write ステップ移行後 50ms 以内に hydrateEditor が呼ばれる"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "PendingHydrate 型定義 + pendingHydrate state + useEffect"
      contains: "PendingHydrate"
    - path: "app/viewer/__tests__/page.test.tsx"
      provides: "IndexedDB モック + pendingHydrate テストスタブ"
      contains: "pendingHydrate"
  key_links:
    - from: "list の li onClick"
      to: "setPendingHydrate → setStep('write')"
      via: "pendingHydrate state"
      pattern: "setPendingHydrate"
    - from: "useEffect([pendingHydrate])"
      to: "hydrateEditor(editorRef.current, ...)"
      via: "setTimeout 50ms"
      pattern: "hydrateEditor"
---

<objective>
一覧からノートをタップしてもエディタに内容が読み込まれないバグを修正する。

Purpose: `step === 'list'` の時点では write ブロックがアンマウントされており `editorRef.current` が null になる。`pendingHydrate` state を追加し、write ステップ移行後の useEffect で hydrateEditor を呼ぶことで確実に読み込む。
Output: `PendingHydrate` 型・`pendingHydrate` state・write 用 useEffect が app/viewer/page.tsx に追加される。テストスタブが app/viewer/__tests__/page.test.tsx に作成される。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-iphone-fusen-kanri/09-CONTEXT.md
@.planning/phases/09-iphone-fusen-kanri/09-RESEARCH.md

<interfaces>
<!-- app/viewer/page.tsx の現状（変更対象箇所）-->

既存 state（ViewerPage 内、行 647〜676 付近）:
```typescript
const [step, setStep] = useState<'banner'|'login'|'push'|'ready'|'write'|'list'|'note'>('banner');
const [currentDraftId, setCurrentDraftId] = useState<string | null>(null);
const [imageBlobs, setImageBlobs] = useState<Map<string, File>>(new Map());
const [writeTags, setWriteTags] = useState<string[]>([]);
const [showTagBar, setShowTagBar] = useState(false);
const editorRef = React.useRef<HTMLDivElement>(null);
```

既存ヘルパー関数:
```typescript
function hydrateEditor(el: HTMLDivElement, markdown: string, blobMap: Map<string, File>): void
async function loadDraft(id: string): Promise<DraftRecord | null>
```

既存 list の li onClick（行 1367〜1401 付近）— バグ箇所:
```typescript
onClick={async () => {
  if (!editorRef.current) return;  // ← list では常に null → 早期リターンしてバグ
  if (note.status === 'draft') {
    // ...
    hydrateEditor(editorRef.current, fullText, blobMap);  // ← null で呼ばれない
    setCurrentDraftId(note.id);
    // ...
  } else {
    hydrateEditor(editorRef.current, fullText, emptyBlobMap);  // ← null で呼ばれない
    setCurrentDraftId(null);
    // ...
  }
  setStep('write');
}}
```

既存 useEffect（step='list' のデータ取得、行 811〜832 付近）:
```typescript
useEffect(() => {
  if (step !== 'list') return;
  // ...historyNotes 取得...
}, [step, accessToken]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: テストスタブを作成する（Nyquist Wave 0）</name>
  <files>app/viewer/__tests__/page.test.tsx</files>
  <action>
`app/viewer/__tests__/` ディレクトリを作成し、以下の内容でテストスタブファイルを作成する。

このファイルは実際のテスト実装ではなく「テストが実行される足場」を作ることが目的。すべてのテストは `todo` で placeholder とする（RED フェーズ不要、スタブのみ）。

```typescript
/**
 * Phase 09 — IPHONE-MGT-01〜04 テストスタブ
 * 実装完了後に TODO を実際のアサーションに置き換える
 */

// IndexedDB モック（jest-environment-jsdom の IDBFactory をスタブ化）
const mockPut = jest.fn().mockResolvedValue(undefined);
const mockGet = jest.fn().mockResolvedValue(undefined);
const mockDelete = jest.fn().mockResolvedValue(undefined);
const mockGetAll = jest.fn().mockResolvedValue([]);

jest.mock('idb', () => ({
  openDB: jest.fn().mockResolvedValue({
    put: mockPut,
    get: mockGet,
    delete: mockDelete,
    getAll: mockGetAll,
  }),
}));

describe('IPHONE-MGT-01: pendingHydrate バグ修正', () => {
  it.todo('list の li onClick が setPendingHydrate を呼び、editorRef.current に依存しない');
  it.todo('pendingHydrate が非 null のとき useEffect が 50ms 後に hydrateEditor を呼ぶ');
  it.todo('pendingHydrate が null のとき useEffect は何もしない');
});

describe('IPHONE-MGT-02: 保存フロー（上書き vs 新規）', () => {
  it.todo('currentDraftId が存在する場合、同じ ID で saveDraft が呼ばれる（上書き）');
  it.todo('currentDraftId が null の場合、新しい UUID で saveDraft が呼ばれる（新規）');
});

describe('IPHONE-MGT-03: 一覧 ＋ボタン', () => {
  it.todo('＋ボタンクリック後、setPendingHydrate({ markdown: "", draftId: null }) が呼ばれる');
  it.todo('＋ボタンクリック後、write 画面がエディタ空状態で開く（前のノートの内容が残らない）');
});

describe('IPHONE-MGT-04: 下書き削除', () => {
  it.todo('削除ボタンクリックで deleteDraft(note.id) が呼ばれる');
  it.todo('削除後に loadAllDrafts() が呼ばれて historyNotes が更新される');
  it.todo('削除ボタンクリックで li の onClick（編集遷移）が発火しない（stopPropagation）');
});
```

`jest` / `it.todo` / `describe` は @jest/globals からではなくグローバル（vitest または jest）を使う。プロジェクトのテストフレームワークに合わせること（package.json の `jest` または `vitest` を確認して適切に調整）。
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=viewer/__tests__/page 2>&1 | tail -20</automated>
  </verify>
  <done>
- app/viewer/__tests__/page.test.tsx が存在する
- ファイルに IndexedDB モックと 4 describe ブロック（IPHONE-MGT-01〜04）が含まれる
- npm test -- --testPathPattern=viewer/__tests__/page が「todo」または「skipped」として通過し、FAIL にならない
  </done>
</task>

<task type="auto">
  <name>Task 1: PendingHydrate 型と state を追加し、write 用 useEffect を実装する</name>
  <files>app/viewer/page.tsx</files>
  <action>
以下の3箇所を変更する。最小変更のみ。

**変更1: 型定義を追加（行 314 の IphoneNote 型定義の直後に追加）**

```typescript
type PendingHydrate = {
  markdown: string;
  blobMap: Map<string, File>;
  draftId: string | null;
  tags: string[];
};
```

**変更2: state 宣言を追加（ViewerPage 内の state 宣言群の末尾、fileInputRef の直後あたりに追加）**

```typescript
const [pendingHydrate, setPendingHydrate] = useState<PendingHydrate | null>(null);
```

**変更3: write 用 useEffect を追加（既存の step==='list' の useEffect の直後に追加）**

```typescript
// pendingHydrate: list→write 遷移後に editorRef がマウントされてから hydrateEditor を呼ぶ
useEffect(() => {
  if (!pendingHydrate) return;
  const run = () => {
    if (!editorRef.current) return;
    hydrateEditor(editorRef.current, pendingHydrate.markdown, pendingHydrate.blobMap);
    setImageBlobs(pendingHydrate.blobMap);
    setCurrentDraftId(pendingHydrate.draftId);
    setWriteTags(pendingHydrate.tags);
    setShowTagBar(pendingHydrate.tags.length > 0);
    setPendingHydrate(null);
  };
  const t = setTimeout(run, 50);
  return () => clearTimeout(t);
}, [pendingHydrate]);
```

deps は `[pendingHydrate]` のみ（editorRef は ref なので deps に含めない）。

**変更4: list の li の onClick を修正（バグ修正）**

現在の onClick（行 1367〜1401 付近）を以下に置き換える:

```typescript
onClick={async () => {
  if (note.status === 'draft') {
    const draft = await loadDraft(note.id).catch(() => null);
    const blobMap = new Map<string, File>();
    if (draft?.images && draft.images.length > 0) {
      for (const { fileName, blob } of draft.images) {
        blobMap.set(fileName, new File([blob], fileName, { type: 'image/jpeg' }));
      }
    }
    const fullText = note.title
      ? `# ${note.title}\n\n${note.body ?? ''}`
      : (note.body ?? '');
    setPendingHydrate({ markdown: fullText, blobMap, draftId: note.id, tags: note.tags ?? [] });
  } else {
    const fullText = note.title
      ? `# ${note.title}\n\n${note.body ?? ''}`
      : (note.body ?? '');
    setPendingHydrate({ markdown: fullText, blobMap: new Map(), draftId: null, tags: note.tags ?? [] });
  }
  setStep('write');
}}
```

`if (!editorRef.current) return;` の早期リターンを削除し、代わりに setPendingHydrate を使う。
hydrateEditor / setImageBlobs / setCurrentDraftId / setWriteTags / setShowTagBar の直接呼び出しをすべて削除（useEffect 側に移動）。
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=viewer 2>&1 | tail -20</automated>
  </verify>
  <done>
- PendingHydrate 型が型定義セクションに存在する
- pendingHydrate state が ViewerPage 内に宣言されている
- useEffect([pendingHydrate]) が追加されており、50ms タイマーで hydrateEditor を呼ぶ
- list の li onClick が setPendingHydrate → setStep('write') の順に呼ぶ（直接 hydrateEditor を呼ばない）
- npm test が PASS（viewer/__tests__/page.test.tsx の todo が含まれる）
  </done>
</task>

</tasks>

<verification>
手動確認（実機または開発ブラウザ）:
1. list 画面で下書きノートをタップ → write 画面に遷移し、エディタに内容が表示される
2. list 画面で送信済みノートをタップ → write 画面に遷移し、エディタに内容が表示される
3. write 画面が空のまま開いてしまうバグが再現しないことを確認
</verification>

<success_criteria>
- `pendingHydrate` state が追加されている
- list の li onClick が setPendingHydrate を呼び、直接 hydrateEditor を呼ばない
- useEffect([pendingHydrate]) が write 画面マウント後にエディタをハイドレートする
- app/viewer/__tests__/page.test.tsx が存在し、IPHONE-MGT-01〜04 のスタブが含まれる
- npm test が PASS
</success_criteria>

<output>
完了後、`.planning/phases/09-iphone-fusen-kanri/09-01-SUMMARY.md` を作成する。
</output>
