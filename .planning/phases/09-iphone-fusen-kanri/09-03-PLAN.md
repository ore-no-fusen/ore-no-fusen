---
phase: 09-iphone-fusen-kanri
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - app/viewer/page.tsx
autonomous: false
requirements:
  - IPHONE-MGT-05

must_haves:
  truths:
    - "「PCに送る」ボタンが引き続き動作し、テキスト・画像・タグを Drive に送信できる"
    - "送信後に currentDraftId があれば IndexedDB の下書きが deleteDraft(currentDraftId) で削除される"
    - "「PCに送る」送信成功後は write 画面に留まる（setSendSuccess(true) のみで setStep を呼ばない）"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "送信フロー維持 + write ヘッダーの一覧ナビゲーション"
      contains: "PCに送る"
  key_links:
    - from: "「PCに送る」ボタン onClick"
      to: "uploadWithAutoRefresh → saveToHistory → deleteDraft"
      via: "currentDraftId チェック"
      pattern: "deleteDraft.*currentDraftId"
---

<objective>
「PCに送る」送信フローが引き続き正常動作することを確認し、write 画面から一覧に戻るナビゲーションを整備する。また全フロー統合動作を手動検証する。

Purpose: Phase 9 の全機能（バグ修正・新規作成・削除・保存・送信）が一体として動くことを確認する。
Output: write ヘッダーに「📋 一覧」ボタンが表示され、「PCに送る」後の下書き削除ロジックが currentDraftId を正しく参照する。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-iphone-fusen-kanri/09-CONTEXT.md
@.planning/phases/09-iphone-fusen-kanri/09-01-SUMMARY.md
@.planning/phases/09-iphone-fusen-kanri/09-02-SUMMARY.md

<interfaces>
<!-- Plan 01・02 完了後の状態 -->

write ヘッダー（行 970〜977 付近、Plan 02 で「📋 一覧」に変更済み）:
```tsx
<button className="text-blue-600 text-sm font-medium" onClick={() => setStep('list')}>
  📋 一覧
</button>
```

「PCに送る」ボタン onClick 内の下書き削除部分（行 1182〜1185 付近）:
```typescript
if (currentDraftId) {
  await deleteDraft(currentDraftId).catch(() => {});
  setCurrentDraftId(null);
}
```
※ この部分は currentDraftId を参照しており、Plan 01 の pendingHydrate 経由での setCurrentDraftId と整合する。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 送信後の下書き削除ロジックを currentDraftId ベースに明示的に修正する</name>
  <files>app/viewer/page.tsx</files>
  <action>
「PCに送る」ボタンの onClick 内の下書き削除ブロックを読み取り、以下の形になっているか確認する。

**期待する形:**
```typescript
if (currentDraftId) {
  await deleteDraft(currentDraftId).catch(() => {});
  setCurrentDraftId(null);
}
```

**確認1:** `if (currentDraftId)` のガードが存在することを確認する。Plan 01 で pendingHydrate 経由で `setCurrentDraftId` が設定されるため、送信時点では currentDraftId が正しく保持されている。

**確認2:** 送信成功後に `setStep` を呼んでいないことを確認する（write 画面に留まる）。`setSendSuccess(true)` のみが呼ばれているはずであり、`setStep('list')` や `setStep('write')` への遷移はしない。

**修正が必要な場合のみ変更する:**

もし削除ブロックが以下のいずれかになっている場合は修正する:
- `currentDraftId` ではなく `pendingHydrate?.draftId` を参照している → `currentDraftId` に戻す
- 削除ブロック自体が消えている → 上記の形で追加する
- 送信後に `setStep('list')` を呼んでいる → 削除して write 画面に留まる形にする

**write ヘッダーの「一覧」ボタン確認:**

Plan 02 Task 1 で変更済みのはずだが、もし変更されていなければここで対応する:
```tsx
<button
  className="text-blue-600 text-sm font-medium"
  onClick={() => setStep('list')}
>
  📋 一覧
</button>
```

上記2点に問題がなければファイルへの書き込みは行わない（最小修正の原則）。問題があった箇所のみ修正してコミットする。
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -30</automated>
  </verify>
  <done>
- 「PCに送る」onClick に `if (currentDraftId) { await deleteDraft(currentDraftId)...; setCurrentDraftId(null); }` が存在する
- 送信成功後に setSendSuccess(true) が呼ばれる（3秒後に false）、setStep は呼ばれない
- write ヘッダーに「📋 一覧」ボタンがある
- npm test が PASS（全テストスイート）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 9 の全機能:
- Plan 01: 一覧タップ→エディタ読み込みバグ修正（pendingHydrate）
- Plan 02: 一覧リニューアル（「＋」ボタン・削除ボタン・「一覧」名称）+ 保存フロー（上書き対応）+ 削除後 loadAllDrafts 再取得
- Plan 03: 送信フロー維持確認
  </what-built>
  <how-to-verify>
以下のフローをすべて確認する（実機または開発ブラウザで viewer/page.tsx が動作する環境）:

**フロー1: 新規作成→保存→一覧確認**
1. write 画面で「書く」にテキスト入力
2. 「iPhoneに置いておく」タップ → 一覧に遷移する
3. 一覧に「下書き」ノートが1件表示される

**フロー2: 一覧から下書きを開く（バグ修正確認）**
1. 一覧の下書きをタップ
2. write 画面が開き、エディタに内容が読み込まれている（空ではない）

**フロー3: 下書きを上書き保存**
1. エディタの内容を編集
2. 「iPhoneに置いておく」タップ → 一覧に戻る
3. 一覧のノート数が増えていない（同じIDで上書き）

**フロー4: 「＋」で新規作成**
1. 一覧ヘッダーの「＋」をタップ
2. write 画面が空で開く（前のノートの内容が残っていない）

**フロー5: 削除**
1. 一覧で下書きノートの右端の 🗑️ をタップ
2. 確認ダイアログなしで即削除
3. 一覧からノートが消える（loadAllDrafts 再取得で反映）
4. 別のノートをタップしても削除したノートが開かない

**フロー6: 送信（PCが起動していなくてもエラーが出ないことだけ確認）**
1. write 画面でテキスト入力
2. 「PCに送る」タップ
3. 送信処理が走る（エラーや成功は Drive 環境による）
  </how-to-verify>
  <resume-signal>"approved" と入力するか、問題があれば具体的な症状を説明してください</resume-signal>
</task>

</tasks>

<verification>
checkpoint:human-verify タスクで全フローを手動確認済みであること。
npm test が PASS（既存テスト含む全スイート）。
</verification>

<success_criteria>
- 全5フローが期待通りに動作する
- npm test PASS
- app/viewer/page.tsx のみが変更されている（Rust・app/page.tsx は変更なし）
</success_criteria>

<output>
完了後、`.planning/phases/09-iphone-fusen-kanri/09-03-SUMMARY.md` を作成する。
</output>
