---
phase: 04-hono-api-kiban
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - lib/webpush.ts
  - lib/webpush.test.ts
autonomous: true
requirements:
  - API-04
  - API-06

must_haves:
  truths:
    - "VAPID_PUBLIC_KEY/PRIVATE_KEY 未設定時に initVapid がエラーをスロー"
    - "sendNoteToIphone が web-push の sendNotification を正しい形式で呼ぶ"
    - "Push Subscription の保存形式（flat）と sendNotification 渡し形式（keys.p256dh）が正しく変換される"
    - "lib/webpush.test.ts が GREEN になる"
  artifacts:
    - path: "lib/webpush.ts"
      provides: "VAPID 設定 + Web Push 送信ラッパー"
      exports:
        - initVapid
        - sendNoteToIphone
  key_links:
    - from: "lib/webpush.ts"
      to: "web-push"
      via: "import webpush from 'web-push'"
      pattern: "from 'web-push'"
    - from: "app/api/v1/[[...route]]/route.ts (Plan 04)"
      to: "lib/webpush.ts"
      via: "import { sendNoteToIphone } from '../../../lib/webpush'"
      pattern: "sendNoteToIphone"
---

<objective>
Web Push ラッパー `lib/webpush.ts` を実装し、`lib/webpush.test.ts` を GREEN にする。

Purpose: Plan 04（APIハンドラ）が `POST /api/v1/notes/push` で iPhone に Push を送る基盤。
Output: `lib/webpush.ts`（VAPID 初期化 + sendNoteToIphone）、テスト GREEN
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-hono-api-kiban/04-CONTEXT.md
@.planning/phases/04-hono-api-kiban/04-RESEARCH.md
@.planning/phases/04-hono-api-kiban/04-01-SUMMARY.md

<interfaces>
<!-- lib/webpush.ts が export すべきインターフェース（APIハンドラが使用する） -->
<!-- APIハンドラは Google Drive から subscription を読み込んでからこの関数を呼ぶ -->
```typescript
// initVapid — VAPID 鍵をセットアップ（環境変数から読む）
// VAPID_PUBLIC_KEY または VAPID_PRIVATE_KEY が未設定なら Error をスロー
export function initVapid(): void

// sendNoteToIphone — Push Subscription に対して Web Push を送信
// subscription は Google Drive に保存されたフラット形式
// 内部で initVapid() を呼び出す
export async function sendNoteToIphone(
  subscription: { endpoint: string; p256dh: string; auth: string },
  note: { title: string; body: string }
): Promise<void>
```

<!-- web-push の sendNotification に渡す形式（フラット → keys 変換が必要） -->
```typescript
// webpush.sendNotification の第1引数形式（Pitfall 4 参照）
{
  endpoint: string,
  keys: { p256dh: string, auth: string }  // ← "keys" ネストが必要
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/webpush.ts 実装（RED→GREEN）</name>
  <files>lib/webpush.ts, lib/webpush.test.ts</files>
  <behavior>
    - initVapid: VAPID_PUBLIC_KEY または VAPID_PRIVATE_KEY が process.env にない場合 Error('VAPID_NOT_CONFIGURED') をスロー。両方揃っている場合は webpush.setVapidDetails('mailto:ore-no-fusen@example.com', PUBLIC, PRIVATE) を呼ぶ
    - sendNoteToIphone:
      1. initVapid() を呼ぶ
      2. subscription フラット形式を { endpoint, keys: { p256dh, auth } } に変換
      3. webpush.sendNotification(converted, JSON.stringify({ title, body })) を呼ぶ
      4. Push サービスが 410 Gone を返した場合は Error('SUBSCRIPTION_EXPIRED') をスロー
  </behavior>
  <action>
`lib/webpush.ts` を新規作成する。Research の Pattern 4 に従って実装。

実装上の注意:
1. `import webpush from 'web-push'` を使用（デフォルトインポート）
2. `setVapidDetails` の第1引数は必ず `mailto:` プレフィックスを含める（Pitfall 6 回避）
   - メールアドレスは `ore-no-fusen@example.com` を使用（CONTEXT.md に未指定のため Claude's Discretion）
3. sendNotification の第1引数は `{ endpoint, keys: { p256dh, auth } }` 形式（Pitfall 4 回避）
4. 410 エラー: `webpush.sendNotification` が statusCode 410 でスローする場合は Error('SUBSCRIPTION_EXPIRED') に変換
   - web-push は 410 を `statusCode: 410` プロパティを持つ Error オブジェクトでスロー

テスト修正が必要な場合（スキャフォールドの import パスが間違っている等）は lib/webpush.test.ts を修正してよい。
テストが GREEN になることを確認してからコミットする。
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npm run test -- lib/webpush.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
`npm run test -- lib/webpush.test.ts` が全テスト PASS（GREEN）。
lib/webpush.ts が initVapid と sendNoteToIphone を export している。
  </done>
</task>

</tasks>

<verification>
- `lib/webpush.ts` が存在し TypeScript コンパイルエラーなし
- `npm run test -- lib/webpush.test.ts` が GREEN
- initVapid / sendNoteToIphone の 2 関数が export されている
- sendNoteToIphone が subscription フラット形式を keys ネスト形式に変換している
</verification>

<success_criteria>
- Web Push ラッパーのテストがすべて GREEN
- VAPID 未設定エラー、410 Subscription 失効エラーが適切に処理される
- フラット subscription → web-push API 形式の変換が正しく実装されている
</success_criteria>

<output>
After completion, create `.planning/phases/04-hono-api-kiban/04-03-SUMMARY.md`
</output>
