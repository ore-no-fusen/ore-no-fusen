---
phase: 04-hono-api-kiban
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/gdrive.test.ts
  - lib/webpush.test.ts
  - app/api/v1/[[...route]]/route.test.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04

must_haves:
  truths:
    - "npm install が完了しhono/googleapis/web-pushが使用可能"
    - "テストスキャフォールドが存在しnpm run testが失敗（RED）している"
    - "lib/gdrive.test.ts がAPI-02/03/05/07のテストケースを定義している"
    - "lib/webpush.test.ts がAPI-04/06のテストケースを定義している"
    - "route.test.ts がAPI-01（Bearer認証）のテストケースを定義している"
  artifacts:
    - path: "lib/gdrive.test.ts"
      provides: "Google Drive ラッパーのテスト契約"
    - path: "lib/webpush.test.ts"
      provides: "Web Push ラッパーのテスト契約"
    - path: "app/api/v1/[[...route]]/route.test.ts"
      provides: "Hono エントリの Bearer 認証テスト契約"
  key_links:
    - from: "lib/gdrive.test.ts"
      to: "lib/gdrive.ts"
      via: "import"
      pattern: "from.*gdrive"
    - from: "lib/webpush.test.ts"
      to: "lib/webpush.ts"
      via: "import"
      pattern: "from.*webpush"
---

<objective>
npm パッケージ（hono, googleapis, web-push）を追加し、Wave 2/3 で実装するライブラリのテストスキャフォールドを RED 状態で作成する。

Purpose: Nyquist Rule — 実装前にテスト契約を定義。Wave 2/3 の実装タスクが RED→GREEN のサイクルで動けるようにする。
Output: package.json（hono/googleapis/web-push 追加）+ 3 つのテストファイル（失敗状態）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hono-api-kiban/04-CONTEXT.md
@.planning/phases/04-hono-api-kiban/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: npm パッケージ追加</name>
  <files>package.json, package-lock.json</files>
  <action>
以下のパッケージをインストールする:

```bash
npm install hono googleapis web-push
npm install --save-dev @types/web-push
```

インストール後、package.json に以下が含まれることを確認:
- `"hono": "^4.x.x"` (dependencies)
- `"googleapis": "^144.x.x"` (dependencies)
- `"web-push": "^3.6.x"` (dependencies)
- `"@types/web-push": "^3.6.x"` (devDependencies)

注意: `hono` は `hono/vercel` アダプター付きで導入される（hono パッケージに同梱）。
`@hono/node-server` は追加しない（TypeError の原因になる）。
  </action>
  <verify>
    <automated>node -e "require('./node_modules/hono'); require('./node_modules/googleapis'); require('./node_modules/web-push'); console.log('OK')" 2>&1 | grep OK</automated>
  </verify>
  <done>3 パッケージすべてが node_modules に存在し、import 可能</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: テストスキャフォールド作成（RED状態）</name>
  <files>
    lib/gdrive.test.ts,
    lib/webpush.test.ts,
    app/api/v1/[[...route]]/route.test.ts
  </files>
  <behavior>
    lib/gdrive.test.ts:
    - Test API-02a: GOOGLE_REFRESH_TOKEN 未設定時に getOAuth2Client が Error('OAUTH_NOT_CONFIGURED') をスロー
    - Test API-02b: invalid_grant エラーが OAUTH_REFRESH_TOKEN_EXPIRED エラーに変換される
    - Test API-03: upsertJsonFile が既存ファイルあり時は files.update を呼ぶ（モック）
    - Test API-03: upsertJsonFile が既存ファイルなし時は files.create を呼ぶ（モック）
    - Test API-05: savePushSubscription がモック Drive に対し upsert を呼ぶ
    - Test API-07: getLatestNote がモック Drive から JSON を返す

    lib/webpush.test.ts:
    - Test API-04: VAPID_PUBLIC_KEY 未設定時に initVapid が Error をスロー
    - Test API-06: sendNoteToIphone がモック sendNotification を呼ぶ

    app/api/v1/[[...route]]/route.test.ts:
    - Test API-01a: Authorization ヘッダーなしで POST /api/v1/subscribe が 401 を返す
    - Test API-01b: 正しい Bearer トークンで POST /api/v1/subscribe が 4xx 以外を返す（subscribe 未実装でも 500）
  </behavior>
  <action>
各テストファイルを作成する。テストはすべて RED（失敗）状態でよい。
実装ファイルがまだ存在しないため、import エラーで失敗することも許容する。

lib/gdrive.test.ts の作成方針:
- `vi.mock` で googleapis を完全モック
- `vi.stubEnv` で環境変数を制御（テスト後は `vi.unstubAllEnvs()`）
- 各 test の describe 構造: `describe('gdrive', () => { describe('getOAuth2Client', ...) })`

lib/webpush.test.ts の作成方針:
- `vi.mock('web-push', ...)` でモック
- `vi.stubEnv` で VAPID_PUBLIC_KEY/VAPID_PRIVATE_KEY を制御

app/api/v1/[[...route]]/route.test.ts の作成方針:
- Hono の `testClient` または `app.request()` を使用
- `process.env.API_SECRET = 'test-secret'` を設定してテスト

注意: vitest.config.ts の include に `'lib/**/*.test.ts'` と `'app/**/*.test.ts'` が含まれているか確認し、含まれていなければ追記する。
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npm run test -- --reporter=verbose 2>&1 | grep -E "FAIL|PASS|Error" | head -20</automated>
  </verify>
  <done>
3 つのテストファイルが存在する。
`npm run test` を実行するとテストが認識される（FAIL でよい、RED 状態が正常）。
import エラーを除き、テストケースの意図が明確に記述されている。
  </done>
</task>

</tasks>

<verification>
- `node_modules/hono`, `node_modules/googleapis`, `node_modules/web-push` が存在する
- `lib/gdrive.test.ts`, `lib/webpush.test.ts`, `app/api/v1/[[...route]]/route.test.ts` の 3 ファイルが存在する
- `npm run test` がテストを認識する（FAIL 許容）
</verification>

<success_criteria>
- npm install が完了し 3 パッケージが利用可能
- テストスキャフォールドが RED 状態で存在し、Wave 2/3 で GREEN にできる契約が定義されている
</success_criteria>

<output>
After completion, create `.planning/phases/04-hono-api-kiban/04-01-SUMMARY.md`
</output>
