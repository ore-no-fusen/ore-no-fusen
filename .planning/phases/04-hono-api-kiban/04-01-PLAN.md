---
phase: 04-hono-api-kiban
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/state.rs
autonomous: true
requirements:
  - API-03
  - API-07

must_haves:
  truths:
    - "Cargo.toml に oauth2, tauri-plugin-oauth, p256, jwt-simple, aes-gcm, hkdf の依存が追加されている"
    - "state.rs に ProConfig 構造体と AppState.pro_config フィールドが存在する"
    - "cargo check が通る"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "新規クレート依存関係"
      contains: "oauth2"
    - path: "src-tauri/src/state.rs"
      provides: "ProConfig 型 + AppState フィールド"
      contains: "pub struct ProConfig"
  key_links:
    - from: "src-tauri/src/state.rs"
      to: "AppState"
      via: "pro_config: Option<ProConfig> フィールド"
      pattern: "pro_config"
---

<objective>
Cargo.toml に Phase 4 で必要なすべての新規クレートを追加し、state.rs に ProConfig 構造体と AppState への pro_config フィールドを定義する。

Purpose: 後続の gdrive.rs (Plan 02) と webpush.rs (Plan 03) の実装が共通の型定義とクレートに依存できる基盤を確立する。
Output: コンパイルが通る Cargo.toml + ProConfig 型定義
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-hono-api-kiban/04-CONTEXT.md
@.planning/phases/04-hono-api-kiban/04-RESEARCH.md
</context>

<interfaces>
<!-- 既存 state.rs の型定義（executor が継承する） -->
From src-tauri/src/state.rs:
```rust
#[derive(serde::Serialize, serde::Deserialize, Default, Clone)]
pub struct AppState {
    pub base_path: Option<String>,
    pub folder_path: Option<String>,
    pub notes: Vec<NoteMeta>,
    pub selected_path: Option<String>,
    pub active_context_menu_path: Option<String>,
    pub active_world: Option<String>,
    pub active_tags: Vec<String>,
    pub last_alt_tab_window: Option<String>,
    // ← ここに pro_config: Option<ProConfig> を追加する
}
```

追加する ProConfig（CONTEXT.md より）:
```rust
pub struct ProConfig {
    pub push_endpoint: String,
    pub p256dh: String,
    pub auth: String,
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cargo.toml に新規クレートを追加する</name>
  <files>src-tauri/Cargo.toml</files>
  <action>
既存の Cargo.toml の [dependencies] セクションに以下を追加する。

```toml
reqwest = { version = "0.13", features = ["json", "rustls-tls", "multipart"] }
oauth2 = "5.0"
tauri-plugin-oauth = "2"
p256 = { version = "0.13", features = ["ecdh", "ecdsa"] }
jwt-simple = "0.12"
aes-gcm = "0.10"
hkdf = "0.12"
```

注意点:
- reqwest は CONTEXT.md の "0.12" ではなく "0.13" を使う（RESEARCH.md 推奨: Cargo.lock に 0.13.2 が既存のため二重化を防ぐ）
- sha2, base64, directories は既存のため追加不要
- tokio は既存のため追加不要
- tauri-plugin-oauth を lib.rs の plugin 登録に追加する必要があるが、それは Plan 04 で行う
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen/src-tauri && cargo check 2>&1 | tail -10</automated>
  </verify>
  <done>cargo check が error なしで通る。oauth2, tauri-plugin-oauth, p256, jwt-simple, aes-gcm, hkdf が Cargo.toml に存在する。</done>
</task>

<task type="auto">
  <name>Task 2: state.rs に ProConfig 構造体と AppState フィールドを追加する</name>
  <files>src-tauri/src/state.rs</files>
  <action>
state.rs の末尾付近（Settings の後）に ProConfig 構造体を追加し、AppState に pro_config フィールドを追加する。

追加する ProConfig:
```rust
#[derive(serde::Serialize, serde::Deserialize, Clone, Debug)]
pub struct ProConfig {
    pub push_endpoint: String,
    pub p256dh: String,
    pub auth: String,
}
```

AppState の変更（既存フィールドを保持したまま末尾に追加）:
```rust
pub struct AppState {
    // ...既存フィールドは変更しない...
    pub pro_config: Option<ProConfig>,
}
```

AppState の Default 実装は derive(Default) を使っているため、Option<ProConfig> は自動的に None になる。追加の変更は不要。

注意: 既存フィールドを変更しない。AppState::Default を破壊しない。
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen/src-tauri && cargo check 2>&1 | tail -10</automated>
  </verify>
  <done>ProConfig が state.rs に定義され、AppState.pro_config: Option&lt;ProConfig&gt; が存在する。cargo check が通る。</done>
</task>

</tasks>

<verification>
cargo check が error 0 で通ること。
state.rs に ProConfig と AppState.pro_config が存在すること。
Cargo.toml に 7 つの新規クレートが追加されていること。
</verification>

<success_criteria>
- cargo check エラーなし
- ProConfig 構造体が pub で export されている
- AppState.pro_config: Option&lt;ProConfig&gt; フィールドが存在する
- 後続 Plan 02/03 が gdrive.rs / webpush.rs を新規作成できる状態
</success_criteria>

<output>
完了後、`.planning/phases/04-hono-api-kiban/04-01-SUMMARY.md` を作成する。
</output>
