---
phase: 04-hono-api-kiban
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - lib/gdrive.ts
  - lib/gdrive.test.ts
autonomous: true
requirements:
  - API-02
  - API-03

must_haves:
  truths:
    - "GOOGLE_REFRESH_TOKEN が未設定の場合 OAUTH_NOT_CONFIGURED エラーが発生する"
    - "invalid_grant エラーが OAUTH_REFRESH_TOKEN_EXPIRED エラーに変換される（サイレント障害なし）"
    - "fusen_push_config.json の upsert が動作する（存在: update、不在: create）"
    - "fusen_note.json の upsert が動作する"
    - "getLatestNote が Google Drive から JSON を返す"
    - "lib/gdrive.test.ts が GREEN になる"
  artifacts:
    - path: "lib/gdrive.ts"
      provides: "Google Drive OAuth2 + ファイル読み書きラッパー"
      exports:
        - savePushSubscription
        - saveNote
        - getLatestNote
  key_links:
    - from: "lib/gdrive.ts"
      to: "googleapis"
      via: "import { google } from 'googleapis'"
      pattern: "from 'googleapis'"
    - from: "lib/gdrive.ts"
      to: "invalid_grant handling"
      via: "withDriveAuth wrapper"
      pattern: "invalid_grant"
---

<objective>
Google Drive ラッパー `lib/gdrive.ts` を実装し、`lib/gdrive.test.ts` を GREEN にする。

Purpose: APIハンドラ（Plan 04）が依存する Google Drive 操作の基盤を確立する。
Output: `lib/gdrive.ts`（OAuth2 + upsert + read）、テスト GREEN
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-hono-api-kiban/04-CONTEXT.md
@.planning/phases/04-hono-api-kiban/04-RESEARCH.md
@.planning/phases/04-hono-api-kiban/04-01-SUMMARY.md

<interfaces>
<!-- lib/gdrive.ts が export すべきインターフェース（APIハンドラが使用する） -->
```typescript
// savePushSubscription — POST /subscribe から呼ばれる
export async function savePushSubscription(
  sub: { endpoint: string; p256dh: string; auth: string }
): Promise<void>

// saveNote — POST /notes/push から呼ばれる
export async function saveNote(note: object): Promise<void>

// getLatestNote — GET /notes/latest から呼ばれる
// ファイルが存在しない場合は null を返す
export async function getLatestNote(): Promise<object | null>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/gdrive.ts 実装（RED→GREEN）</name>
  <files>lib/gdrive.ts, lib/gdrive.test.ts</files>
  <behavior>
    - getOAuth2Client: GOOGLE_CLIENT_ID/SECRET/REFRESH_TOKEN のいずれかが未設定なら Error('OAUTH_NOT_CONFIGURED') をスロー
    - withDriveAuth: err.response.data.error === 'invalid_grant' または err.message に 'invalid_grant' が含まれる場合に Error('OAUTH_REFRESH_TOKEN_EXPIRED') をスロー
    - ensureFolder: 'ore-no-fusen' フォルダをマイドライブ直下に確保（存在すれば ID 返却、なければ作成）
    - upsertJsonFile: `name='${filename}' and '${folderId}' in parents and trashed=false` で検索、存在すれば files.update、なければ files.create
    - savePushSubscription: ensureFolder → upsertJsonFile('fusen_push_config.json', sub)
    - saveNote: ensureFolder → upsertJsonFile('fusen_note.json', note)
    - getLatestNote: ensureFolder → files.list で fusen_note.json を検索 → files.get(alt: 'media', responseType: 'text') → JSON.parse。ファイルなしなら null
  </behavior>
  <action>
`lib/gdrive.ts` を新規作成する。Research の Pattern 2/3/5/6 に従って実装。

実装上の注意:
1. `import { google } from 'googleapis'` を使用（googleapis v144.x は named export）
2. `export const runtime = 'nodejs'` は route.ts に書くもの。gdrive.ts 自体には不要
3. upsertJsonFile の media body は文字列として渡す: `{ mimeType: 'application/json', body: JSON.stringify(data) }`
4. files.list のクエリに必ず `and trashed=false` と `spaces: 'drive'` を含める（Pitfall 5 回避）
5. withDriveAuth の invalid_grant 検知は Research Pattern 2 の条件をそのまま使用する

テスト修正が必要な場合（スキャフォールドの import パスが間違っている等）は lib/gdrive.test.ts を修正してよい。
テストが GREEN になることを確認してからコミットする。
  </action>
  <verify>
    <automated>cd D:/Users/uck/Documents/curry-project/ore-no-fusen && npm run test -- lib/gdrive.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
`npm run test -- lib/gdrive.test.ts` が全テスト PASS（GREEN）。
lib/gdrive.ts が 3 つの public 関数（savePushSubscription, saveNote, getLatestNote）を export している。
  </done>
</task>

</tasks>

<verification>
- `lib/gdrive.ts` が存在し TypeScript コンパイルエラーなし（`npx tsc --noEmit` でエラーなし）
- `npm run test -- lib/gdrive.test.ts` が GREEN
- savePushSubscription / saveNote / getLatestNote の 3 関数が export されている
</verification>

<success_criteria>
- Google Drive ラッパーのテストがすべて GREEN
- invalid_grant を OAUTH_REFRESH_TOKEN_EXPIRED に変換するロジックが実装されている
- upsert が存在チェック→update/create の正しいフローで動作する
</success_criteria>

<output>
After completion, create `.planning/phases/04-hono-api-kiban/04-02-SUMMARY.md`
</output>
