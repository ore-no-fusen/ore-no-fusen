---
phase: 03-kakunin-kensho
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified: []
autonomous: false
requirements:
  - STAB-02
  - STAB-03

must_haves:
  truths:
    - "Tauri ビルドが成功する（Rust コンパイルエラーなし）"
    - "tray.rs の unwrap() 修正が Rust コンパイラによって確認される"
    - "手動テストでピンボタン押下後にウィンドウが消えないことが確認される"
    - "手動テストでトレイアイコン操作後にクラッシュしないことが確認される"
    - "手動テストで frontmatter なしノートの行ジャンプが正常動作することが確認される"
  artifacts:
    - path: "src-tauri/src/tray.rs"
      provides: "Mutex unwrap_or_else 修正済み（line 55, 131）"
      contains: "unwrap_or_else"
    - path: "src-tauri/src/logic.rs"
      provides: "content.find() unwrap_or(0) 修正済み（line 371）"
      contains: "unwrap_or"
  key_links:
    - from: "src-tauri/src/tray.rs"
      to: "Mutex::lock() 結果"
      via: "unwrap_or_else(|p| p.into_inner())"
      pattern: "unwrap_or_else"
    - from: "src-tauri/src/logic.rs"
      to: "content.find() 結果"
      via: "unwrap_or(0)"
      pattern: "unwrap_or"
---

<objective>
Tauri ビルドで Rust コンパイルを通して unwrap() 残存がないことを確認し、手動テストで STAB-02 と STAB-03 の動作を検証する。

Purpose: STAB-02（unwrap 残存なし）と STAB-03（Win32/Tauri 状態同期）を確認し、フェーズ全要件を達成する
Output: ビルド成功確認 + 手動テスト完了 + Phase 3 最終サマリー
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kakunin-kensho/03-RESEARCH.md
@.planning/phases/03-kakunin-kensho/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tauri ビルド実行（Rust unwrap 残存確認）</name>
  <files></files>
  <action>
以下のコマンドで Rust コンパイルを確認する。フルビルドが不要な場合は cargo check でよい。

```bash
cd src-tauri && cargo check
```

または時間を許容できる場合:

```bash
npm run tauri build
```

期待結果: コンパイルエラーなし。tray.rs:55,131 の `unwrap_or_else` および logic.rs:371 の `unwrap_or` が正しくコンパイルされることをコンパイラが確認する。

失敗した場合の対応方針（CONTEXT.md 決定事項）:
- コンパイルエラー等の小さな問題: その場で最小修正を施してから再実行する
- 大規模な問題: 修正せず記録して Phase 4 以降に移す

Pitfall 注意（RESEARCH.md より）:
- `npm run tauri build` 実行後、Next.js キャッシュが壊れることがある。Plan 01 のテストは既に完了しているため影響なし

STAB-02 確認ポイント:
- src-tauri/src/tray.rs: `unwrap_or_else(|p| p.into_inner())` が 2箇所（line 55, 131付近）存在すること
- src-tauri/src/logic.rs: `unwrap_or(0)` が line 371付近に存在すること
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>cargo check または tauri build がエラーなしで完了し、Rust コンパイラが修正済みコードを受け入れること</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 手動テスト（STAB-02 修正動作確認 + STAB-03 ピンボタン確認）</name>
  <files></files>
  <what-built>
Phase 2 で施した3箇所の修正に対して、Tauri アプリを実際に起動して手動で動作確認する。

修正済み箇所:
1. tray.rs:55,131 — Mutex ポイズン時に unwrap_or_else で継続動作（クラッシュしない）
2. logic.rs:371 — frontmatter なしノートで content.find() が None でも 0 にフォールバック
3. STAB-03 — fusen_set_always_on_top が生Win32 SetWindowPos で実装済み（ウィンドウ消失なし）
  </what-built>
  <how-to-verify>
Tauri アプリを起動してから以下の順で確認する。

**シナリオ1: STAB-03 — ピンボタン後ウィンドウ消失なし**
1. アプリを起動する
2. 新規付箋を作成する
3. 付箋のピンボタン（常に最前面ボタン）を押す
4. 期待: 付箋ウィンドウが消えずに表示され続ける
5. もう一度ピンボタンを押して解除する。引き続き表示されること

**シナリオ2: STAB-02（tray）— トレイアイコン操作**
1. 複数の付箋を開いた状態にする
2. タスクバーのトレイアイコンを右クリックする
3. 期待: メニューが正常に表示される（クラッシュしない）
4. メニューから操作を行い、正常動作することを確認する

**シナリオ3: STAB-02（logic）— frontmatter なしノートの行ジャンプ**
1. frontmatter（---区切り）のないシンプルなテキストノートを開く
2. 行ジャンプ機能（scroll_to_line イベント）が発火する操作を行う
3. 期待: クラッシュせず、先頭行（行0）に移動する

新たな回帰を発見した場合: その場では修正しない。発見内容を報告する（Phase 4 以降で対処）。
  </how-to-verify>
  <action>Task 1 完了後、上記3シナリオを手動で実行し、動作を確認する。新たな回帰が発見された場合は内容を記録する（その場での修正は行わない）。</action>
  <verify>
    <automated>MISSING — 手動テスト（Tauri Win32 動作）は自動化不可</automated>
  </verify>
  <done>3シナリオすべてで期待動作が確認されるか、新たな回帰が発見されて記録済みであること</done>
  <resume-signal>"approved" と入力するか、発見した問題を記述して報告してください</resume-signal>
</task>

</tasks>

<verification>
Phase 3 全体の最終確認:
- [ ] `npm run test`（vitest）全件グリーン（Plan 01 で確認済み）
- [ ] `npx playwright test` 全13件パス（Plan 01 で確認済み）
- [ ] `cargo check` または `npm run tauri build` エラーなし（Task 1 で確認）
- [ ] 手動テスト: ピンボタン後ウィンドウ消えない（Task 2 で確認）
- [ ] 手動テスト: トレイ操作クラッシュなし（Task 2 で確認）
- [ ] 手動テスト: frontmatter なしノート行ジャンプ正常（Task 2 で確認）
- [ ] 発見した新たな回帰（あれば）が記録済み
</verification>

<success_criteria>
- STAB-02: Tauri ビルド or cargo check がエラーなしで通る
- STAB-03: 手動テストでピンボタン後ウィンドウが消えないことが確認される
- Phase 3 全7要件（STAB-01〜03, DATA-01〜02, UI-01〜02）がすべて確認済みとなる
- 新たな回帰が発見された場合は記録済みであること（修正は Phase 4 以降）
</success_criteria>

<output>
完了後、`.planning/phases/03-kakunin-kensho/03-02-SUMMARY.md` を作成する。

記載内容:
- cargo check / tauri build 結果
- 手動テスト各シナリオの結果
- 発見した新たな回帰（あれば）の記録
- STAB-02, STAB-03 の確認結果
- Phase 3 全体の完了宣言（全7要件の確認状況）
</output>
