---
phase: 03-kakunin-kensho
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - STAB-01
  - DATA-01
  - DATA-02
  - UI-01
  - UI-02

must_haves:
  truths:
    - "npx playwright test が全13件パスする"
    - "npm run test（vitest）が全件パスする"
    - "Listener Leak・空body上書き・race condition・カーソル位置・FloatingFormatBar のいずれもテストで回帰なし"
  artifacts:
    - path: "e2e/sticky-note.spec.ts"
      provides: "Playwright E2E 13件のテストスイート"
    - path: "vitest.config.ts"
      provides: "Vitest ユニットテスト設定"
  key_links:
    - from: "Playwright テスト"
      to: "localhost:3003"
      via: "playwright.config.ts の webServer 設定"
      pattern: "baseURL.*3003"
    - from: "vitest テスト"
      to: "app/ ソースコード"
      via: "vitest.config.ts の include パターン"
      pattern: "npm run test"
---

<objective>
Phase 2 の修正に対して自動テスト（vitest + Playwright E2E）を実行し、回帰がないことを確認する。

Purpose: STAB-01, DATA-01, DATA-02, UI-01, UI-02 の全要件を自動テストで検証する
Output: テスト全件グリーンの確認記録
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kakunin-kensho/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest ユニットテスト実行</name>
  <files></files>
  <action>
以下のコマンドを実行する。watch モード（--watch）は使わない。

```bash
npm run test
```

期待結果: 全ファイル（5ファイル程度）グリーン。

失敗した場合の対応方針（CONTEXT.md 決定事項）:
- 失敗が小さな修正（1〜3行程度）で対処できる場合: その場で修正してから再実行する
- 修正が大規模になる場合（5行超・複数ファイルにまたがる）: 修正せず結果を記録し、Phase 4 以降に委ねる

注意: `npm run test:coverage` ではなく `npm run test`（vitest run）を使うこと。coverage 閾値で失敗する可能性があるため。
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>vitest が全件パスし、失敗ゼロであること</done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E テスト実行</name>
  <files></files>
  <action>
以下のコマンドを実行する。

```bash
npx playwright test
```

playwright.config.ts は `baseURL: 'http://localhost:3003'`、`webServer: { command: 'npm run dev -- -p 3003', reuseExistingServer: true }` に設定済み。手動でサーバーを起動する必要はない。

期待結果: 全13件パス。

失敗した場合の対応方針（CONTEXT.md 決定事項）:
- 失敗が小さな修正（1〜3行程度）で対処できる場合: その場で修正してから再実行する
- 修正が大規模な場合: 修正せず結果を記録し、Phase 4 以降に委ねる

Pitfall 注意（RESEARCH.md より）:
- ポート3003でサーバーが既に3002で起動していると "could not connect to localhost:3003" で失敗する。その場合は3003で起動し直す
- Tauri ビルド後にキャッシュが壊れている場合は `npx next dev -p 3003` を再起動する

要件対応:
- STAB-01: Listener Leak（全13件でカバー）
- DATA-01: 空body上書き（テスト3.1, 3.2）
- DATA-02: race condition（全13件でカバー）
- UI-01: カーソル位置（テスト1.1, 1.2）
- UI-02: FloatingFormatBar blur 除外（テスト1.7, 2.1）
  </action>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <done>Playwright E2E 全13件パス、失敗ゼロであること</done>
</task>

</tasks>

<verification>
両テストが完了したら以下を確認する:
- vitest: 全件グリーン（失敗 0）
- Playwright: 13件中13件パス
- いずれかで失敗した場合、CONTEXT.md の修正方針（小さい修正はその場で、大規模は Phase 4）を適用した記録が残っていること
</verification>

<success_criteria>
- `npm run test` が全件パス
- `npx playwright test` が13件パス
- STAB-01, DATA-01, DATA-02, UI-01, UI-02 の自動テスト検証が完了していること
</success_criteria>

<output>
完了後、`.planning/phases/03-kakunin-kensho/03-01-SUMMARY.md` を作成する。

記載内容:
- vitest 実行結果（件数・失敗数）
- Playwright 実行結果（件数・失敗数）
- 発見した問題と対処内容（あれば）
- 各要件（STAB-01, DATA-01, DATA-02, UI-01, UI-02）の確認結果
</output>
