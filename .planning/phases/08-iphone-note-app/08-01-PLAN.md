---
phase: 08-iphone-note-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/viewer/page.tsx
autonomous: true
requirements:
  - IPHONE-UI-01

must_haves:
  truths:
    - "書く画面にタイトル入力欄がなく、contenteditable div が本文エディタになっている"
    - "serializeEditor(el) が div の innerHTML をMarkdown文字列に変換して返す"
    - "hydrateEditor(el, markdown, blobMap) がMarkdownをcontenteditableのDOMに復元する"
    - "extractTitleBody(text) が1行目をtitle、残りをbodyとして分離する"
    - "insertTextAtCursor(text) がcontenteditable内カーソル位置にテキストを挿入する"
    - "insertNodeAtCursor(node) がcontenteditable内カーソル位置にDOMノードを挿入する"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "contenteditable基盤: 型変更・state変更・ヘルパー関数群・write UI差し替え"
      contains: "serializeEditor|hydrateEditor|extractTitleBody|insertTextAtCursor|insertNodeAtCursor"
  key_links:
    - from: "write step JSX"
      to: "editorRef (RefObject<HTMLDivElement>)"
      via: "ref={editorRef}"
      pattern: "ref=\\{editorRef\\}"
    - from: "serializeEditor"
      to: "img[data-filename]"
      via: "シリアライズ時にMarkdown画像記法に変換"
      pattern: "data-filename"
    - from: "hydrateEditor"
      to: "blobMap (Map<string, File>)"
      via: "ファイル名でblobを引いてObjectURLを生成"
      pattern: "blobMap"
---

<objective>
contenteditable エディタの基盤を作る。

Purpose: タイトル欄廃止・画像/MermaidのDOMインライン表示を実現するための土台。
Output: 型変更・state変更・5つのヘルパー関数・write UI差し替えが完了した viewer/page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-iphone-note-app/08-CONTEXT.md

<interfaces>
<!-- 現在の viewer/page.tsx から executor が把握すべき情報 -->

削除する state（187行付近 ViewerPage コンポーネント内）:
```typescript
const [writeTitle, setWriteTitle] = useState('');       // 削除
const [writeBody, setWriteBody] = useState('');         // 削除
const [attachedImages, setAttachedImages] = useState<{ file: File; preview: string; fileName: string }[]>([]); // 削除
const textareaRef = React.useRef<HTMLTextAreaElement>(null); // 削除
```

追加する state:
```typescript
const editorRef = React.useRef<HTMLDivElement>(null);
const [imageBlobs, setImageBlobs] = useState<Map<string, File>>(new Map());
const [writeTags, setWriteTags] = useState<string[]>([]);
const [showTagBar, setShowTagBar] = useState(false);
const [tagInput, setTagInput] = useState('');
const [cropFile, setCropFile] = useState<File | null>(null);
const [showCropModal, setShowCropModal] = useState(false);
```

型変更:
```typescript
// IphoneNote（187行付近）に追加
type IphoneNote = {
  id: string;
  title: string;
  body: string;
  status: 'sent' | 'draft';
  created_at: string;
  sent_at?: string;
  tags?: string[];   // ← 追加
};

// DraftRecord（279行付近）に追加
type DraftRecord = {
  id: string;
  title: string;
  body: string;
  created_at: string;
  images: { fileName: string; blob: Blob }[];
  tags?: string[];   // ← 追加
};
```

既存の insertAtCursor（app/viewer/utils.ts 29行付近、textarea専用）:
```typescript
export function insertAtCursor(el: HTMLTextAreaElement, text: string): string
```
→ contenteditable 用の insertTextAtCursor を新規作成（utils.ts には触らない）

buildImageFileName（page.tsx 11行）:
```typescript
function buildImageFileName(title: string, index: number): string
```
→ そのまま活用。タイトルは serializeEditor 後の extractTitleBody から取得
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 型変更・state変更</name>
  <files>app/viewer/page.tsx</files>
  <action>
以下の変更を行う。

**型変更（IphoneNote と DraftRecord に tags? を追加）:**
- `IphoneNote`（187行付近）に `tags?: string[]` フィールドを追加
- `DraftRecord`（279行付近）に `tags?: string[]` フィールドを追加

**ViewerPage コンポーネント内の state 変更（340行付近から）:**

削除する state と ref:
- `const [writeTitle, setWriteTitle] = useState('');`
- `const [writeBody, setWriteBody] = useState('');`
- `const [attachedImages, setAttachedImages] = useState<...>([]);`
- `const textareaRef = React.useRef<HTMLTextAreaElement>(null);`

追加する state と ref（既存 state の直後、fileInputRef の前後に挿入）:
```typescript
const editorRef = React.useRef<HTMLDivElement>(null);
const [imageBlobs, setImageBlobs] = useState<Map<string, File>>(new Map());
const [writeTags, setWriteTags] = useState<string[]>([]);
const [showTagBar, setShowTagBar] = useState(false);
const [tagInput, setTagInput] = useState('');
const [cropFile, setCropFile] = useState<File | null>(null);
const [showCropModal, setShowCropModal] = useState(false);
```

**注意:** この時点でコンパイルエラーが出る（削除した state を参照している箇所が残る）。Task 3 で write ステップ JSX を差し替えることで解消する。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || true</automated>
  </verify>
  <done>IphoneNote と DraftRecord に tags? が追加され、新 state 6個・ref 1個が宣言されている。削除した state の参照エラーは Task 3 完了まで許容。</done>
</task>

<task type="auto">
  <name>Task 2: ヘルパー関数5つを追加</name>
  <files>app/viewer/page.tsx</files>
  <action>
`buildImageFileName` 関数と `generatePKCE` 関数の間（19行付近）に以下の5つの関数を追加する。

```typescript
// contenteditable div の innerHTML を Markdown 文字列に変換
function serializeEditor(el: HTMLDivElement): string {
  // div[data-mermaid-code] → ```mermaid\ncode\n```
  // img[data-filename] → ![](filename)
  // その他テキスト・改行 → そのまま
  const clone = el.cloneNode(true) as HTMLDivElement;
  // mermaid ブロックを置換
  clone.querySelectorAll<HTMLElement>('[data-mermaid-code]').forEach((node) => {
    const code = node.getAttribute('data-mermaid-code') ?? '';
    const text = document.createTextNode(`\`\`\`mermaid\n${code}\n\`\`\``);
    node.replaceWith(text);
  });
  // img を Markdown 画像記法に置換
  clone.querySelectorAll<HTMLImageElement>('img[data-filename]').forEach((img) => {
    const filename = img.getAttribute('data-filename') ?? '';
    const text = document.createTextNode(`![](${filename})`);
    img.replaceWith(text);
  });
  // innerHTML から改行を復元（div → \n、br → \n）
  // innerText を使うと \n が付く
  return clone.innerText ?? clone.textContent ?? '';
}

// Markdown の1行目をタイトル、残りをbodyとして分離
// 1行目の # プレフィックスは除去
function extractTitleBody(text: string): { title: string; body: string } {
  const lines = text.split('\n');
  const firstLine = lines[0].replace(/^#\s*/, '').trim();
  const rest = lines.slice(1).join('\n').replace(/^\n+/, '');
  return { title: firstLine, body: rest };
}

// contenteditable のカーソル位置にテキストを挿入
function insertTextAtCursor(text: string): void {
  const sel = window.getSelection();
  if (!sel || sel.rangeCount === 0) return;
  const range = sel.getRangeAt(0);
  range.deleteContents();
  const node = document.createTextNode(text);
  range.insertNode(node);
  range.setStartAfter(node);
  range.setEndAfter(node);
  sel.removeAllRanges();
  sel.addRange(range);
}

// contenteditable のカーソル位置に DOM ノードを挿入
function insertNodeAtCursor(node: Node): void {
  const sel = window.getSelection();
  if (!sel || sel.rangeCount === 0) return;
  const range = sel.getRangeAt(0);
  range.deleteContents();
  range.insertNode(node);
  const after = document.createTextNode('\n');
  node.after(after);
  range.setStartAfter(after);
  range.setEndAfter(after);
  sel.removeAllRanges();
  sel.addRange(range);
}

// Markdown 文字列を contenteditable DOM に復元
// blobMap: fileName → File（画像の ObjectURL 生成用）
// Mermaid は初回はコードテキストとして表示（再レンダリングしない）
function hydrateEditor(
  el: HTMLDivElement,
  markdown: string,
  blobMap: Map<string, File>
): void {
  // まず空にする
  el.innerHTML = '';
  // 段落ごとに処理
  const lines = markdown.split('\n');
  let i = 0;
  while (i < lines.length) {
    const line = lines[i];
    // ```mermaid ブロック検出
    if (line.startsWith('```mermaid')) {
      const codeLines: string[] = [];
      i++;
      while (i < lines.length && !lines[i].startsWith('```')) {
        codeLines.push(lines[i]);
        i++;
      }
      i++; // closing ```
      const code = codeLines.join('\n');
      const div = document.createElement('div');
      div.setAttribute('data-mermaid-code', code);
      div.textContent = `[Mermaid: ${code.slice(0, 30)}...]`;
      div.style.cssText = 'background:#f3f4f6;padding:4px 8px;border-radius:4px;font-size:12px;color:#6b7280;margin:4px 0;';
      el.appendChild(div);
      el.appendChild(document.createElement('br'));
      continue;
    }
    // 画像記法 ![](filename) 検出
    const imgMatch = line.match(/^!\[\]\(([^)]+)\)$/);
    if (imgMatch) {
      const filename = imgMatch[1];
      const file = blobMap.get(filename);
      if (file) {
        const img = document.createElement('img');
        img.src = URL.createObjectURL(file);
        img.setAttribute('data-filename', filename);
        img.style.cssText = 'max-height:80px;border-radius:4px;margin:2px 0;';
        el.appendChild(img);
        el.appendChild(document.createElement('br'));
      } else {
        // blob がない場合（sent note）はテキストとして表示
        const span = document.createElement('span');
        span.textContent = line;
        el.appendChild(span);
        el.appendChild(document.createElement('br'));
      }
      i++;
      continue;
    }
    // 通常テキスト行
    const span = document.createElement('span');
    span.textContent = line;
    el.appendChild(span);
    el.appendChild(document.createElement('br'));
    i++;
  }
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "serializeEditor\|hydrateEditor\|extractTitleBody\|insertTextAtCursor\|insertNodeAtCursor" | head -5 || echo "no type errors for helpers"</automated>
  </verify>
  <done>5つのヘルパー関数が page.tsx に存在し、TypeScript のシンタックスエラーがない。</done>
</task>

<task type="auto">
  <name>Task 3: write ステップ UI を contenteditable に差し替え</name>
  <files>app/viewer/page.tsx</files>
  <action>
`{step === 'write' && (` のブロック（658行付近）を丸ごと以下に差し替える。

差し替え後の write ステップ構造:
1. ヘッダー: 「📋 履歴」ボタン / 「書く」タイトル / 右側は空（ツールバーボタンは Plan 02 で追加）
2. contenteditable div（エディタ本体）: `ref={editorRef}`、`contentEditable="true"`、プレースホルダーは CSS ::before 方式
3. 「iPhoneに置いておく」ボタンのロジック修正: `writeTitle/writeBody` の代わりに `serializeEditor(editorRef.current)` → `extractTitleBody()` を使う。imageBlobs を images として保存。writeTags も保存。
4. 「PCに送る」ボタンのロジック修正: 同様に serializeEditor → extractTitleBody を使う。imageBlobs から File を取り出して uploadImageWithAutoRefresh を呼ぶ。writeTags を payload に含める（tags フィールド追加）。
5. 送信後のリセット: `editorRef.current.innerHTML = ''`、`setImageBlobs(new Map())`、`setWriteTags([])`
6. Mermaid モーダル: 既存コードを維持しつつ、「挿入」ボタンの onClick を以下に変更:
   - `mermaidPreviewSvg` がある場合: SVGを `<div data-mermaid-code="...">` にラップして `insertNodeAtCursor` で挿入
   - SVGがない場合: コードテキストをそのまま `insertTextAtCursor` で挿入
7. `textareaRef` を参照していた箇所（fileInputRef onChange の `insertAtCursor` 呼び出し）を削除（画像挿入は Plan 02 の CropModal で行う）

**重要な実装ポイント:**
- `iPhoneに置いておく` のロジック:
```typescript
const rawText = serializeEditor(editorRef.current!);
const { title, body } = extractTitleBody(rawText);
const draftId = currentDraftId ?? crypto.randomUUID();
const imagesArr = Array.from(imageBlobs.entries()).map(([fileName, file]) => ({ fileName, blob: file }));
await saveDraft({ id: draftId, title, body, created_at: new Date().toISOString(), images: imagesArr, tags: writeTags });
editorRef.current!.innerHTML = '';
setImageBlobs(new Map());
setWriteTags([]);
setCurrentDraftId(null);
setStep('list');
```

- `PCに送る` の payload:
```typescript
await uploadWithAutoRefresh(token, 'fusen_from_iphone.json', {
  id: noteId,
  title,
  body: fullBody,
  sent_at: sentAt,
  tags: writeTags,
});
```

- contenteditable の CSS プレースホルダー（div の直後に style タグまたは className で）:
```
className="flex-1 px-4 py-3 text-base outline-none overflow-y-auto"
// data-placeholder 属性でCSSの::before疑似要素使用、またはonInputで空チェック
```
プレースホルダーの実装はCSSの `empty:before` パターンで可:
```tsx
<div
  ref={editorRef}
  contentEditable="true"
  suppressContentEditableWarning
  className="flex-1 px-4 py-3 text-base outline-none overflow-y-auto min-h-[200px] focus:outline-none"
  data-placeholder="メモを書く..."
  style={{ whiteSpace: 'pre-wrap', wordBreak: 'break-word' }}
/>
// グローバルCSSまたはinlineでプレースホルダーを表示するのが難しい場合は
// onInput で innerHTML が空かチェックして別要素で表示してもよい
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "error" | head -10 || echo "no errors"</automated>
  </verify>
  <done>TypeScript エラーがゼロ（または write ステップの変更に無関係なエラーのみ）。write ステップに textarea・タイトルinput・attachedImages が存在しない。editorRef を持つ contenteditable div が存在する。「iPhoneに置いておく」「PCに送る」がコンパイルを通る。</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` でエラーゼロ。
`grep -n "textareaRef\|writeTitle\|writeBody\|attachedImages" app/viewer/page.tsx` で削除済みを確認（fileInputRef onChange 等の残存コードは Plan 02 で完全削除）。
`grep -n "serializeEditor\|hydrateEditor\|extractTitleBody\|insertTextAtCursor\|insertNodeAtCursor" app/viewer/page.tsx` で5関数の存在確認。
</verification>

<success_criteria>
- IphoneNote.tags? と DraftRecord.tags? が型定義に存在する
- editorRef、imageBlobs、writeTags、showTagBar、tagInput、cropFile、showCropModal が state/ref として存在する
- 5つのヘルパー関数が page.tsx のファイルスコープに存在する
- write ステップに `<textarea` と `<input type="text"` （タイトル欄）が存在しない
- write ステップに `ref={editorRef}` を持つ contenteditable div が存在する
- TypeScript コンパイルがエラーなく通る
</success_criteria>

<output>
完了後、`.planning/phases/08-iphone-note-app/08-01-SUMMARY.md` を作成する。
</output>
