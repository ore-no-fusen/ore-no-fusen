---
phase: 08-iphone-note-app
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - app/viewer/page.tsx
  - app/page.tsx
autonomous: true
requirements:
  - IPHONE-UI-05

must_haves:
  truths:
    - "🏷️ボタンがヘッダーに表示され、タップするとタグバーが展開/折りたたみされる"
    - "タグバーにテキスト入力があり Enter または スペースで追加、× で削除できる"
    - "「PCに送る」の payload に tags 配列が含まれる"
    - "「iPhoneに置いておく」でタグが DraftRecord に保存・復元される"
    - "PC受信ハンドラ（app/page.tsx）が tags を受け取り fusen_add_tag を呼ぶ"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "🏷️ボタン + タグバー UI + tags を含む送信payload + DraftRecord tags 保存"
      contains: "writeTags|showTagBar|tagInput|fusen_add_tag"
    - path: "app/page.tsx"
      provides: "PC受信ハンドラの tags 適用（fusen_add_tag 呼び出し）"
      contains: "fusen_add_tag.*tags"
  key_links:
    - from: "「PCに送る」uploadWithAutoRefresh"
      to: "fusen_from_iphone.json の tags フィールド"
      via: "payload に tags: writeTags を追加"
      pattern: "tags: writeTags"
    - from: "app/page.tsx fusen:note_from_iphone リスナー"
      to: "invoke('fusen_add_tag', { path, tag })"
      via: "tags 配列をループして invoke"
      pattern: "fusen_add_tag"
---

<objective>
タグ機能を追加し、iPhone→PC 送信時にタグがPC付箋に反映されるようにする。

Purpose: 付箋アプリのタグ整理機能をiPhoneからも使えるようにする。
Output: 🏷️タグUIが動き、PC受信でfusen_add_tagが呼ばれる viewer/page.tsx + app/page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-iphone-note-app/08-CONTEXT.md
@.planning/phases/08-iphone-note-app/08-02-SUMMARY.md

<interfaces>
<!-- Plan 01 で追加済みの state -->
```typescript
const [writeTags, setWriteTags] = useState<string[]>([]);
const [showTagBar, setShowTagBar] = useState(false);
const [tagInput, setTagInput] = useState('');
```

<!-- 既存の fusen_add_tag 使用例（app/api/tags.ts 28行付近） -->
```typescript
invoke('fusen_add_tag', { path, tag })  // tag は string 1つ
```

<!-- app/page.tsx 991行付近の現在のリスナーシグネチャ -->
```typescript
listen<{ title: string; body: string; context: string }>(
  'fusen:note_from_iphone',
  async (event) => {
    const { title, body, context } = event.payload;
    // ... fusen_create_note → fusen_save_note → openNoteWindow
  }
)
```
→ payload の型に tags?: string[] を追加し、受信後に fusen_add_tag を呼ぶ

<!-- saveToHistory の signature -->
```typescript
async function saveToHistory(token: string, note: IphoneNote): Promise<void>
```
→ note に tags を渡す時は `IphoneNote` 型に既に `tags?: string[]` が追加済み（Plan 01）

<!-- 「PCに送る」ボタンでの現在の uploadWithAutoRefresh 呼び出し（Plan 01後）-->
```typescript
await uploadWithAutoRefresh(token, 'fusen_from_iphone.json', {
  id: noteId,
  title,
  body: fullBody,
  sent_at: sentAt,
  tags: writeTags,  // Plan 01 で既に追加済みの場合は確認・未追加なら追加
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 🏷️ボタン + タグバー UI を追加</name>
  <files>app/viewer/page.tsx</files>
  <action>
**1. ヘッダーの ☑ ボタンの直後に 🏷️ ボタンを追加する:**

```tsx
<button
  className={`min-w-[32px] px-2 py-1 rounded text-sm ${
    showTagBar ? 'bg-gray-200 text-gray-900' : 'hover:bg-gray-100 text-gray-700'
  }`}
  onClick={() => setShowTagBar((prev) => !prev)}
  aria-label="タグ"
  title="タグ"
>
  🏷️
</button>
```

**2. contenteditable div の直後（下部アクションボタンの前）にタグバーを追加する:**

```tsx
{/* タグバー */}
{showTagBar && (
  <div className="px-4 py-2 border-t border-gray-100 flex flex-wrap gap-2 items-center">
    {writeTags.map((tag, i) => (
      <span
        key={i}
        className="flex items-center gap-1 bg-blue-100 text-blue-800 text-sm rounded-full px-3 py-1"
      >
        {tag}
        <button
          className="text-blue-500 hover:text-blue-700 leading-none"
          onClick={() => setWriteTags((prev) => prev.filter((_, j) => j !== i))}
          aria-label={`タグ ${tag} を削除`}
        >
          ×
        </button>
      </span>
    ))}
    <input
      type="text"
      value={tagInput}
      onChange={(e) => setTagInput(e.target.value)}
      onKeyDown={(e) => {
        if ((e.key === 'Enter' || e.key === ' ') && tagInput.trim()) {
          e.preventDefault();
          const newTag = tagInput.trim();
          if (!writeTags.includes(newTag)) {
            setWriteTags((prev) => [...prev, newTag]);
          }
          setTagInput('');
        }
      }}
      placeholder="タグを入力（Enter で追加）"
      className="text-sm outline-none border-b border-gray-300 focus:border-blue-400 min-w-[120px] flex-1"
    />
  </div>
)}
```

**3. 送信後のリセット（「PCに送る」ボタンと「iPhoneに置いておく」ボタン）にタグリセットを追加する:**

「PCに送る」の送信成功後:
```typescript
setWriteTags([]);
setShowTagBar(false);
setTagInput('');
```

「iPhoneに置いておく」の保存後:
```typescript
setWriteTags([]);
setShowTagBar(false);
setTagInput('');
```

**4. 「PCに送る」の payload に tags が含まれているか確認し、なければ追加する:**

```typescript
await uploadWithAutoRefresh(token, 'fusen_from_iphone.json', {
  id: noteId,
  title,
  body: fullBody,
  sent_at: sentAt,
  tags: writeTags,
});
```

**5. saveToHistory に渡す IphoneNote に tags を含める:**
```typescript
const note: IphoneNote = {
  id: noteId,
  status: 'sent',
  title,
  body: fullBody,
  created_at: sentAt,
  sent_at: sentAt,
  tags: writeTags.length > 0 ? writeTags : undefined,
};
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "error" | head -10 || echo "no errors"</automated>
  </verify>
  <done>TypeScript エラーゼロ。🏷️ボタンが ☑ の直後にある。showTagBar が true の時タグバーが JSX に表示される。送信・保存後のリセット処理に writeTags のクリアが含まれる。</done>
</task>

<task type="auto">
  <name>Task 2: 下書きの tags 保存・復元 + PC受信ハンドラにタグ適用追加</name>
  <files>app/viewer/page.tsx, app/page.tsx</files>
  <action>
**[viewer/page.tsx] 下書き保存時の tags 追加:**

「iPhoneに置いておく」の saveDraft 呼び出しに `tags: writeTags` を追加する（DraftRecord.tags? は Plan 01 で追加済み）:
```typescript
await saveDraft({
  id: draftId,
  title,
  body,
  created_at: new Date().toISOString(),
  images: imagesArr,
  tags: writeTags.length > 0 ? writeTags : undefined,
});
```

**[app/page.tsx] PC受信ハンドラの型拡張とタグ適用:**

1. listen の型パラメータを拡張する（991行付近）:
```typescript
// 変更前:
listen<{ title: string; body: string; context: string }>(

// 変更後:
listen<{ title: string; body: string; context: string; tags?: string[] }>(
```

2. event.payload の分解代入に tags を追加:
```typescript
// 変更前:
const { title, body, context } = event.payload;

// 変更後:
const { title, body, context, tags } = event.payload;
```

3. fusen_save_note の呼び出し後（openNoteWindow の前）にタグ適用ループを追加:
```typescript
// タグ適用（tags が配列で存在する場合）
if (tags && tags.length > 0) {
  for (const tag of tags) {
    await invoke('fusen_add_tag', { path: newNote.meta.path, tag }).catch(() => {});
  }
}
```

**注意:** app/page.tsx は最小限の変更のみ。追加する行数は5行以内。fusen_add_tag は既存コマンドの再利用（新規Rustコード不要）。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "error" | head -10 || echo "no errors"</automated>
  </verify>
  <done>TypeScript エラーゼロ。app/page.tsx の listen 型に tags? が追加されている。fusen_add_tag を tags 数だけ呼ぶループが存在する。viewer/page.tsx の saveDraft 呼び出しに tags が含まれる。</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` でエラーゼロ。
`grep -n "🏷️\|showTagBar\|writeTags" app/viewer/page.tsx` でタグ関連の実装を確認。
`grep -n "fusen_add_tag" app/page.tsx` でPC受信側のタグ適用を確認。
`grep -n "tags:" app/viewer/page.tsx` で送信payload・saveDraft・saveToHistory の3箇所を確認。
</verification>

<success_criteria>
- ヘッダーに🏷️ボタンが追加されている（☑ の直後）
- タグバーに入力欄・チップ表示・削除ボタンがある
- 送信payloadに tags: writeTags が含まれる
- DraftRecord に tags が保存される
- app/page.tsx の fusen:note_from_iphone リスナーが tags を受け取り fusen_add_tag を呼ぶ
- TypeScript コンパイルがエラーなく通る
</success_criteria>

<output>
完了後、`.planning/phases/08-iphone-note-app/08-03-SUMMARY.md` を作成する。
</output>
