---
phase: 08-iphone-note-app
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - app/viewer/page.tsx
autonomous: true
requirements:
  - IPHONE-UI-06

must_haves:
  truths:
    - "一覧で送信済みノートをタップすると write ステップに遷移してエディタにテキストが復元される"
    - "送信済みノートを編集して「PCに送る」すると新規ノートとして（別ID で）送信される"
    - "下書きノードをタップすると hydrateEditor で画像blobを含めて復元される"
    - "一覧アイテムが sent/draft 両方ともタップ可能なスタイル（cursor-pointer）になっている"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "送信済みノード編集対応・hydrateEditor を使った下書き復元"
      contains: "hydrateEditor|note.status.*sent|currentDraftId.*null"
  key_links:
    - from: "一覧の li onClick（sent note）"
      to: "hydrateEditor(editorRef.current, note.body, emptyBlobMap)"
      via: "テキストのみ復元（画像blobなし）"
      pattern: "hydrateEditor.*new Map"
    - from: "一覧の li onClick（draft note）"
      to: "hydrateEditor(editorRef.current, fullText, blobMap)"
      via: "IndexedDB から images を取得して blobMap 構築"
      pattern: "hydrateEditor.*blobMap"
---

<objective>
一覧から送信済みノードを編集・再送信できるようにする。

Purpose: 「過去に送ったメモを編集して再送する」操作を可能にし、一覧画面の価値を高める。
Output: sent/draft 両方がタップで write 画面に遷移し、hydrateEditor で復元される viewer/page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-iphone-note-app/08-CONTEXT.md
@.planning/phases/08-iphone-note-app/08-03-SUMMARY.md

<interfaces>
<!-- Plan 01 で作成済みの hydrateEditor -->
```typescript
function hydrateEditor(
  el: HTMLDivElement,
  markdown: string,
  blobMap: Map<string, File>
): void
```

<!-- Plan 01 で変更済みの state -->
```typescript
const editorRef = React.useRef<HTMLDivElement>(null);
const [imageBlobs, setImageBlobs] = useState<Map<string, File>>(new Map());
const [writeTags, setWriteTags] = useState<string[]>([]);
```

<!-- 現在の一覧 onClick（974行付近・Plan 01 後の状態） -->
```typescript
onClick={async () => {
  if (note.status !== 'draft') return;  // ← 送信済みは無視している（削除する）
  setWriteTitle(note.title);            // ← Plan 01 で削除済み state → hydrateEditor に変更
  setWriteBody(note.body);             // ← 同上
  setCurrentDraftId(note.id);
  const draft = await loadDraft(note.id).catch(() => null);
  if (draft && draft.images.length > 0) {
    setAttachedImages(draft.images.map(...));  // ← Plan 01 で削除済み → setImageBlobs に変更
  } else {
    setAttachedImages([]);  // ← 同上
  }
  setStep('write');
}}
```

<!-- 現在の li className（968行付近） -->
```typescript
className={`px-4 py-3 border-b border-gray-100 ${
  note.status === 'draft'
    ? 'cursor-pointer active:bg-gray-50'
    : 'cursor-default'
}`}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 一覧 onClick を sent/draft 両対応に更新</name>
  <files>app/viewer/page.tsx</files>
  <action>
一覧（step === 'list'）の各ノードの li 要素を以下のように更新する。

**1. li の className を全件タップ可能に変更する:**
```tsx
className="px-4 py-3 border-b border-gray-100 cursor-pointer active:bg-gray-50"
```

**2. onClick を sent/draft 両対応に書き換える:**
```typescript
onClick={async () => {
  if (!editorRef.current) return;

  if (note.status === 'draft') {
    // 下書き: IndexedDB から取得して画像blobを復元
    const draft = await loadDraft(note.id).catch(() => null);
    const blobMap = new Map<string, File>();
    if (draft?.images && draft.images.length > 0) {
      for (const { fileName, blob } of draft.images) {
        blobMap.set(fileName, new File([blob], fileName, { type: 'image/jpeg' }));
      }
    }
    setImageBlobs(blobMap);
    // 1行目がタイトルになるように "# title\nbody" 形式に組み立て
    const fullText = note.title
      ? `# ${note.title}\n\n${note.body ?? ''}`
      : (note.body ?? '');
    hydrateEditor(editorRef.current, fullText, blobMap);
    setCurrentDraftId(note.id);
    setWriteTags(note.tags ?? []);
    setShowTagBar((note.tags ?? []).length > 0);
  } else {
    // 送信済み: テキストのみ復元（画像blobなし）
    const emptyBlobMap = new Map<string, File>();
    setImageBlobs(emptyBlobMap);
    const fullText = note.title
      ? `# ${note.title}\n\n${note.body ?? ''}`
      : (note.body ?? '');
    hydrateEditor(editorRef.current, fullText, emptyBlobMap);
    setCurrentDraftId(null);  // 新規送信扱い
    setWriteTags(note.tags ?? []);
    setShowTagBar((note.tags ?? []).length > 0);
  }
  setStep('write');
}}
```

**注意:**
- `setWriteTitle`・`setWriteBody`・`setAttachedImages` は Plan 01 で削除済みのため使わない
- `setCurrentDraftId(null)` にすることで「PCに送る」時に新規 ID が生成される（重複送信防止）
- `note.tags` は IphoneNote.tags?（Plan 01 で追加済み）
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "error" | head -10 || echo "no errors"</automated>
  </verify>
  <done>TypeScript エラーゼロ。一覧の li に `if (note.status !== 'draft') return` がない。hydrateEditor が呼ばれている。sent note で currentDraftId が null に設定されている。</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` でエラーゼロ。
`grep -n "cursor-default\|status.*draft.*return" app/viewer/page.tsx` でタップ制限コードが削除されていることを確認。
`grep -n "hydrateEditor" app/viewer/page.tsx` で一覧の onClick 内に2箇所（draft と sent）の呼び出しを確認。
`grep -n "currentDraftId.*null" app/viewer/page.tsx` で sent note 復元時の null セットを確認。
</verification>

<success_criteria>
- 一覧のすべての li が cursor-pointer で送信済み・下書き両方タップ可能
- 下書きタップ: blobMap を構築して hydrateEditor を呼ぶ、currentDraftId がノードIDにセットされる
- 送信済みタップ: 空の blobMap で hydrateEditor を呼ぶ、currentDraftId が null にセットされる
- タグがある場合は writeTags と showTagBar が復元される
- TypeScript コンパイルがエラーなく通る
</success_criteria>

<output>
完了後、`.planning/phases/08-iphone-note-app/08-04-SUMMARY.md` を作成する。
</output>
