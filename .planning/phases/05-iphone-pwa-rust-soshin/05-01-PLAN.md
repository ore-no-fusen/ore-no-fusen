---
phase: 05-iphone-pwa-rust-soshin
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/manifest.json
  - worker/index.js
  - app/RegisterPWA.tsx
autonomous: true
requirements:
  - PWA-01
  - PWA-02

must_haves:
  truths:
    - "public/manifest.json の start_url が /viewer になっている"
    - "worker/index.js に push イベントと notificationclick イベントのハンドラが実装されている"
    - "RegisterPWA.tsx が Tauri 環境では SW を解除し、Safari 環境では /sw.js を登録する"
  artifacts:
    - path: "public/manifest.json"
      provides: "PWA マニフェスト（start_url=/viewer, display=standalone）"
      contains: "\"start_url\": \"/viewer\""
    - path: "worker/index.js"
      provides: "next-pwa カスタム Service Worker（push + notificationclick）"
      contains: "showNotification"
    - path: "app/RegisterPWA.tsx"
      provides: "Tauri/Safari 分岐 SW 登録ロジック"
      contains: "__TAURI_INTERNALS__"
  key_links:
    - from: "worker/index.js"
      to: "public/sw.js"
      via: "next-pwa webpack build が worker/index.js を sw.js に自動 merge"
      pattern: "push.*showNotification"
    - from: "app/RegisterPWA.tsx"
      to: "navigator.serviceWorker"
      via: "__TAURI_INTERNALS__ 検出 → 分岐登録"
      pattern: "__TAURI_INTERNALS__"
---

<objective>
PWA 基盤（manifest + Service Worker + 登録ロジック）を構築する。

Purpose: iOS Safari で「ホーム画面に追加」できる PWA として機能させ、push 通知を受信できる Service Worker を配置する。
Output: manifest.json 更新済み、worker/index.js 新規作成、RegisterPWA.tsx に Tauri/Safari 分岐追加。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-iphone-pwa-rust-soshin/05-CONTEXT.md
@.planning/phases/05-iphone-pwa-rust-soshin/05-RESEARCH.md

<interfaces>
<!-- 既存ファイルの現在の内容（executor は探索不要） -->

From public/manifest.json (現在):
```json
{
  "name": "俺の付箋",
  "short_name": "付箋",
  "description": "Obsidian VaultのMarkdownを付箋UIで表示",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#ffffff",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

From app/RegisterPWA.tsx (現在 — 全SW解除のみ):
```typescript
"use client";
import { useEffect } from "react";

export default function RegisterPWA() {
  useEffect(() => {
    if ("serviceWorker" in navigator) {
      navigator.serviceWorker.getRegistrations().then(function (registrations) {
        for (let registration of registrations) {
          registration.unregister();
        }
      }).catch(function (err) {
        console.error("Service Worker unregistration failed: ", err);
      });
    }
  }, []);
  return null;
}
```

worker/index.js: 現在は存在しない（新規作成）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: manifest.json の start_url と description を更新する</name>
  <files>public/manifest.json</files>
  <action>
    既存の `public/manifest.json` を以下の2フィールドのみ変更する:
    - `"start_url": "/"` → `"start_url": "/viewer"`
    - `"description": "Obsidian VaultのMarkdownを付箋UIで表示"` → `"description": "PCの付箋をiPhoneで受け取るセットアップ"`

    他のフィールド（name, short_name, display, background_color, theme_color, icons）は一切変更しない。
  </action>
  <verify>
    <automated>node -e "const m=require('./public/manifest.json'); if(m.start_url!=='/viewer') throw new Error('start_url wrong'); if(!m.description.includes('iPhone')) throw new Error('description wrong'); console.log('OK');"</automated>
  </verify>
  <done>manifest.json の start_url が "/viewer"、description が "PCの付箋をiPhoneで受け取るセットアップ" になっている</done>
</task>

<task type="auto">
  <name>Task 2: worker/index.js を作成して push と notificationclick ハンドラを実装する</name>
  <files>worker/index.js</files>
  <action>
    プロジェクトルートに `worker/` ディレクトリを作成し、`worker/index.js` を新規作成する。

    **このファイルは next-pwa の Webpack ビルドが sw.js に自動 merge するカスタム Service Worker ソース。**
    sw.js を直接書き換えてはならない（build で上書きされる）。

    実装内容:

    ```javascript
    // worker/index.js
    // next-pwa custom worker — push / notificationclick を sw.js に注入
    // Source: github.com/shadowwalker/next-pwa custom-worker example

    self.addEventListener('push', (event) => {
      const data = event.data ? event.data.json() : {};
      event.waitUntil(
        self.registration.showNotification(data.title || '俺の付箋', {
          body: data.body || '',
          icon: '/icon-192.png',
          badge: '/icon-192.png',
          data: { url: self.location.origin + '/viewer' },
        })
      );
    });

    self.addEventListener('notificationclick', (event) => {
      event.notification.close();
      event.waitUntil(
        clients.matchAll({ type: 'window', includeUncontrolled: true }).then((clientList) => {
          for (const client of clientList) {
            if (client.url.includes('/viewer') && 'focus' in client) {
              return client.focus();
            }
          }
          // iOS Safari では相対パスが動かないケースがあるため絶対 URL を使用
          return clients.openWindow(self.location.origin + '/viewer');
        })
      );
    });
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./worker/index.js','utf8'); if(!c.includes('showNotification')) throw new Error('push handler missing'); if(!c.includes('notificationclick')) throw new Error('notificationclick missing'); if(!c.includes('self.location.origin')) throw new Error('absolute URL missing'); console.log('OK');"</automated>
  </verify>
  <done>worker/index.js が存在し、push → showNotification と notificationclick → clients.openWindow（絶対URL）が実装されている</done>
</task>

<task type="auto">
  <name>Task 3: RegisterPWA.tsx に Tauri/Safari 分岐を追加する</name>
  <files>app/RegisterPWA.tsx</files>
  <action>
    `app/RegisterPWA.tsx` を以下のように置き換える。既存の「全SW解除」ロジックを Tauri 環境のみに限定し、Safari 環境では /sw.js を登録する分岐を追加する。

    ```typescript
    /**
     * PWA登録コンポーネント (RegisterPWA)
     *
     * 責務:
     * - Tauri環境: Service Workerを全解除（既存動作を維持）
     * - Safari PWA環境: push対応カスタムSWを登録
     */

    "use client";

    import { useEffect } from "react";

    export default function RegisterPWA() {
      useEffect(() => {
        if (!("serviceWorker" in navigator)) return;

        const isTauri = typeof (window as any).__TAURI_INTERNALS__ !== 'undefined';

        if (isTauri) {
          // Tauri: 全SW登録解除（既存動作を維持）
          navigator.serviceWorker.getRegistrations().then((registrations) => {
            for (const reg of registrations) {
              reg.unregister();
              console.log("Service Worker unregistered: ", reg.scope);
            }
          }).catch((err) => {
            console.error("Service Worker unregistration failed: ", err);
          });
        } else {
          // Safari PWA: next-pwa が生成した push 対応 sw.js を登録
          navigator.serviceWorker.register('/sw.js', { scope: '/' })
            .catch((err) => console.error('SW register failed:', err));
        }
      }, []);

      return null;
    }
    ```

    変更点は `isTauri` 分岐のみ。コメントと既存の Tauri 側ロジックを維持する。
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>RegisterPWA.tsx が TypeScript コンパイルエラーなし、__TAURI_INTERNALS__ 検出分岐が含まれている</done>
</task>

</tasks>

<verification>
npm run build が成功することを確認する。
manifest.json の start_url が "/viewer" になっていることを確認する。
worker/index.js に push と notificationclick の両ハンドラが含まれていることを確認する。
RegisterPWA.tsx に __TAURI_INTERNALS__ 検出コードが含まれていることを確認する。
</verification>

<success_criteria>
- public/manifest.json: start_url="/viewer", description="PCの付箋をiPhoneで受け取るセットアップ"
- worker/index.js: push + notificationclick ハンドラ実装済み、clients.openWindow に絶対URL使用
- app/RegisterPWA.tsx: Tauri環境では全SW解除、非Tauri環境では /sw.js 登録
- npm run build が成功する
</success_criteria>

<output>
完了後、`.planning/phases/05-iphone-pwa-rust-soshin/05-01-SUMMARY.md` を作成すること。
</output>
