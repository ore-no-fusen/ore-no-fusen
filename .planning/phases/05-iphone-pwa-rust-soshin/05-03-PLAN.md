---
phase: 05-iphone-pwa-rust-soshin
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/hooks/useStickyNoteContextMenu.ts
autonomous: true
requirements:
  - SEND-02

must_haves:
  truths:
    - "右クリックメニューの「iPhoneに送る」が有効化（グレーアウト解除）されている"
    - "「iPhoneに送る」クリック時に fusen_send_to_iphone コマンドが selectedFile.path を引数に呼ばれる"
  artifacts:
    - path: "app/hooks/useStickyNoteContextMenu.ts"
      provides: "有効化された ctx_send_to_iphone コンテキストメニュー項目"
      contains: "invoke('fusen_send_to_iphone'"
  key_links:
    - from: "useStickyNoteContextMenu.ts"
      to: "src-tauri/src/lib.rs fusen_send_to_iphone"
      via: "invoke('fusen_send_to_iphone', { path: selectedFile.path })"
      pattern: "fusen_send_to_iphone"
---

<objective>
右クリックメニューの「iPhoneに送る」を有効化する。

Purpose: Phase 4 で実装済みの fusen_send_to_iphone Rust コマンドをフロントエンドのコンテキストメニューから呼び出せるようにする。
Output: useStickyNoteContextMenu.ts の ctx_send_to_iphone エントリが enabled: true + invoke 呼び出しに変更される。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-iphone-pwa-rust-soshin/05-CONTEXT.md

<interfaces>
<!-- 変更対象の現在のコード（app/hooks/useStickyNoteContextMenu.ts の line 335-342） -->
```typescript
// iPhoneに表示（将来実装予定）
menuItems.push(await PredefinedMenuItem.new({ item: 'Separator' }));
menuItems.push(await MenuItem.new({
    id: 'ctx_send_to_iphone',
    text: `📱 ${t('menu.sendToIphone')}`,
    enabled: false,
    action: async () => {}
}));
```

<!-- invoke は既にファイル先頭で import 済み -->
import { invoke } from '@tauri-apps/api/core';

<!-- selectedFile の型 -->
// selectedFile: NoteMeta | null — .path プロパティを持つ
// この hook の引数として props で渡されている（line 17-30 参照）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ctx_send_to_iphone を enabled: true + invoke 呼び出しに変更する</name>
  <files>app/hooks/useStickyNoteContextMenu.ts</files>
  <action>
    `app/hooks/useStickyNoteContextMenu.ts` の `ctx_send_to_iphone` MenuItem 定義（line 338-341 付近）を以下の通り変更する:

    変更前:
    ```typescript
    enabled: false,
    action: async () => {}
    ```

    変更後:
    ```typescript
    enabled: true,
    action: async () => {
        if (selectedFile) {
            await invoke('fusen_send_to_iphone', { path: selectedFile.path });
        }
    }
    ```

    コメント「将来実装予定」も削除する（「iPhoneに送る」に変更済みのため）。
    他の行は一切変更しない。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|Error|✓)" | tail -5</automated>
  </verify>
  <done>
    - ctx_send_to_iphone が enabled: true になっている
    - action が invoke('fusen_send_to_iphone', { path: selectedFile.path }) を呼んでいる
    - npm run build が成功する
  </done>
</task>

</tasks>

<verification>
npm run build が成功することを確認する。
useStickyNoteContextMenu.ts に invoke('fusen_send_to_iphone' が含まれていることを確認する:
grep "fusen_send_to_iphone" app/hooks/useStickyNoteContextMenu.ts
</verification>

<success_criteria>
- enabled: true に変更されている
- action 内で invoke('fusen_send_to_iphone', { path: selectedFile.path }) が呼ばれている
- selectedFile が null の場合は呼ばれない（null チェック済み）
- npm run build 成功
</success_criteria>

<output>
完了後、`.planning/phases/05-iphone-pwa-rust-soshin/05-03-SUMMARY.md` を作成すること。
</output>
