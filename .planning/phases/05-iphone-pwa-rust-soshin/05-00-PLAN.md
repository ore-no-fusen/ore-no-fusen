---
phase: 05-iphone-pwa-rust-soshin
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - app/viewer/viewer.test.tsx
  - worker/worker.test.js
autonomous: true
requirements:
  - PWA-01
  - PWA-02
  - PWA-03
  - SEND-01
  - SEND-02

must_haves:
  truths:
    - "viewer.test.tsx が存在し、3つのテストケーススタブが定義されている"
    - "worker.test.js が存在し、Service Worker の push/notificationclick ハンドラのテストスタブが定義されている"
    - "npm run test がテストスタブを検出して実行できる（PASS または 0 failures）"
  artifacts:
    - path: "app/viewer/viewer.test.tsx"
      provides: "viewer ページの振る舞いテスト（Nyquist 用スタブ）"
      contains: "display-mode: standalone"
    - path: "worker/worker.test.js"
      provides: "Service Worker push/notificationclick の単体テストスタブ"
      contains: "showNotification"
  key_links:
    - from: "app/viewer/viewer.test.tsx"
      to: "app/viewer/page.tsx"
      via: "Wave 1 (Plan 02) で実装されるコンポーネントを検証"
      pattern: "ViewerPage"
    - from: "worker/worker.test.js"
      to: "worker/index.js"
      via: "Wave 1 (Plan 01) で実装される SW ハンドラを検証"
      pattern: "push.*showNotification"
---

<objective>
Phase 5 テストスタブを Wave 0 として先行作成する。

Purpose: Nyquist ルール準拠のため、実装（Wave 1）前にテストファイルを用意する。Wave 1 の実装タスクはこのスタブを GREEN にする形で進める。
Output: app/viewer/viewer.test.tsx と worker/worker.test.js の2ファイル（スタブ状態）。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-iphone-pwa-rust-soshin/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: viewer.test.tsx のテストスタブを作成する</name>
  <files>app/viewer/viewer.test.tsx</files>
  <action>
    `app/viewer/viewer.test.tsx` を新規作成する。vitest + @testing-library/react を使用する。
    このファイルはスタブであり、Wave 1 (Plan 02) で ViewerPage が実装された後に GREEN になる。

    実装内容:

    ```typescript
    /**
     * app/viewer/page.tsx のテストスタブ
     * Wave 0 で先行作成 — Wave 1 (Plan 02) 実装後に GREEN になること
     */
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { render, screen } from '@testing-library/react';

    // Wave 1 で実装される — Plan 02 完了まで TODO
    // import ViewerPage from './page';

    // matchMedia モック（jsdom は matchMedia を持たない）
    beforeEach(() => {
      Object.defineProperty(window, 'matchMedia', {
        writable: true,
        value: vi.fn().mockImplementation((query: string) => ({
          matches: false, // 非standalone をデフォルトとする
          media: query,
          onchange: null,
          addListener: vi.fn(),
          removeListener: vi.fn(),
          addEventListener: vi.fn(),
          removeEventListener: vi.fn(),
          dispatchEvent: vi.fn(),
        })),
      });
    });

    describe('ViewerPage — 非standalone 時はバナーをレンダリングする', () => {
      it.todo('matchMedia display-mode:standalone が false の場合、ホーム画面追加バナーが表示される');
      it.todo('matchMedia display-mode:standalone が true の場合、バナーは表示されない');
      it.todo('standalone + ?code= パラメータなし の場合、Googleでログインボタンが表示される');
    });

    describe('ServiceWorker push handler', () => {
      it.todo('showNotification が push イベントで呼ばれる（worker.test.js で検証）');
    });

    // Placeholder: vitest が 0 failures で終わるための空テスト
    it('Wave 0 スタブが読み込める', () => {
      expect(true).toBe(true);
    });
    ```

    注意: `import ViewerPage` はコメントアウトしておく（Wave 1 完了まで page.tsx が存在しないため）。
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./app/viewer/viewer.test.tsx','utf8'); if(!c.includes('display-mode: standalone')) throw new Error('standalone check missing'); if(!c.includes('matchMedia')) throw new Error('matchMedia mock missing'); console.log('OK');"</automated>
  </verify>
  <done>app/viewer/viewer.test.tsx が存在し、matchMedia モックと 3 つの it.todo を含む</done>
</task>

<task type="auto">
  <name>Task 2: worker/worker.test.js のテストスタブを作成する</name>
  <files>worker/worker.test.js</files>
  <action>
    `worker/worker.test.js` を新規作成する。vitest を使用する。

    実装内容:

    ```javascript
    /**
     * worker/index.js のテストスタブ
     * Wave 0 で先行作成 — Wave 1 (Plan 01) 実装後に GREEN にすること
     */
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    // Service Worker グローバルのモック
    const mockRegistration = {
      showNotification: vi.fn().mockResolvedValue(undefined),
    };

    beforeEach(() => {
      global.self = {
        registration: mockRegistration,
        location: { origin: 'https://example.com' },
        addEventListener: vi.fn(),
      };
      mockRegistration.showNotification.mockClear();
    });

    describe('Service Worker — push handler', () => {
      it.todo('push イベント受信時に showNotification が呼ばれる');
      it.todo('push data が空の場合でもデフォルトタイトルで showNotification が呼ばれる');
    });

    describe('Service Worker — notificationclick handler', () => {
      it.todo('notificationclick で notification.close() が呼ばれる');
      it.todo('既存 /viewer クライアントがあれば focus() する');
      it.todo('既存クライアントがなければ clients.openWindow() で絶対URLを開く');
    });

    // Placeholder: vitest が 0 failures で終わるための空テスト
    it('Wave 0 スタブが読み込める', () => {
      expect(true).toBe(true);
    });
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./worker/worker.test.js','utf8'); if(!c.includes('showNotification')) throw new Error('showNotification missing'); if(!c.includes('notificationclick')) throw new Error('notificationclick missing'); console.log('OK');"</automated>
  </verify>
  <done>worker/worker.test.js が存在し、push と notificationclick のテストスタブを含む</done>
</task>

</tasks>

<verification>
npm run test がエラーなく実行できること（0 failures）:
npm run test 2>&1 | tail -10
</verification>

<success_criteria>
- app/viewer/viewer.test.tsx が存在し、matchMedia モック + it.todo 3件 + placeholder 1件を含む
- worker/worker.test.js が存在し、push/notificationclick の it.todo を含む
- npm run test が 0 failures で終了する
</success_criteria>

<output>
完了後、`.planning/phases/05-iphone-pwa-rust-soshin/05-00-SUMMARY.md` を作成すること。
</output>
