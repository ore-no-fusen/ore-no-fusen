---
phase: 02-bagu-shuse
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - STAB-03
  - UI-02

must_haves:
  truths:
    - "REQUIREMENTS.md の STAB-03 チェックボックスが [x] になっている"
    - "REQUIREMENTS.md の UI-02 チェックボックスが [x] になっている"
    - "lib.rs に fusen_set_always_on_top の生 Win32 実装と win.show() が存在することが確認されている"
    - "StickyNote.tsx に .floatBar 除外チェックが L960 と L1073 に存在することが確認されている"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "STAB-03 と UI-02 のチェック済みステータス"
      contains: "[x] STAB-03"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "src-tauri/src/lib.rs"
      via: "STAB-03 実装確認"
      pattern: "win.show"
    - from: ".planning/REQUIREMENTS.md"
      to: "app/components/StickyNote.tsx"
      via: "UI-02 実装確認"
      pattern: "floatBar"
---

<objective>
STAB-03（Win32/Tauri 状態同期）と UI-02（FloatingFormatBar blur 除外）が実装済みであることを確認し、REQUIREMENTS.md のチェックボックスを更新する。

Purpose: Phase 1 コードレビューで実装済みと判断された 2 要件の正式チェックを完了させ、Phase 3 検証に進む準備を整える。コード変更なし。
Output: REQUIREMENTS.md の STAB-03 と UI-02 が [x] に更新され、1 コミット。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-bagu-shuse/02-RESEARCH.md

<!-- 確認ポイント -->
<!-- STAB-03: lib.rs の fusen_set_always_on_top が生Win32 SetWindowPos を使用、fusen_show_at_position 末尾に let _ = win.show() がある -->
<!-- UI-02: StickyNote.tsx L960 に .floatBar を含む relatedTarget チェック、L1073 に .floatBar を含むグローバルポインタチェックがある -->

<interfaces>
<!-- 確認対象コードの既知パターン -->

From src-tauri/src/lib.rs (STAB-03 確認箇所):
```rust
// fusen_set_always_on_top — 生Win32 SetWindowPos 使用（L99-140付近）
// fusen_show_at_position — 末尾に let _ = win.show() （L1134付近）
```

From app/components/StickyNote.tsx (UI-02 確認箇所):
```typescript
// L960付近 (blur ハンドラ):
// e.relatedTarget.closest('.hoverBar') || e.relatedTarget.closest('.floatBar') || ...
// L1073付近 (グローバルポインタハンドラ):
// (target as HTMLElement)?.closest?.('.hoverBar') || (target as HTMLElement)?.closest?.('.floatBar')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STAB-03 実装確認と REQUIREMENTS.md 更新</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
**Step 1: src-tauri/src/lib.rs を確認**

以下の 2 点が実装されていることを確認する:
1. `fusen_set_always_on_top` 関数が生 Win32 `SetWindowPos(HWND_TOPMOST/NOTOPMOST, SWP_NOMOVE|SWP_NOSIZE|SWP_NOACTIVATE)` を使用している（Tauri の `window.set_always_on_top()` を使っていない）
2. `fusen_show_at_position` 関数の末尾に `let _ = win.show();` が存在する

**Step 2: REQUIREMENTS.md を更新**

確認完了後、.planning/REQUIREMENTS.md の以下の行を変更する:

Before:
```
- [ ] **STAB-03**: Win32 API 呼び出し後に Tauri の内部状態が正しく同期されていること
```

After:
```
- [x] **STAB-03**: Win32 API 呼び出し後に Tauri の内部状態が正しく同期されていること
```

**注意:** 実装が確認できなかった場合は REQUIREMENTS.md を更新せず、差異を SUMMARY.md に記録する。
  </action>
  <verify>
    <automated>grep -n "STAB-03" D:/Users/uck/Documents/curry-project/ore-no-fusen/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>lib.rs で Win32 実装と win.show() が確認され、REQUIREMENTS.md の STAB-03 が [x] になっている。</done>
</task>

<task type="auto">
  <name>Task 2: UI-02 実装確認と REQUIREMENTS.md 更新、コミット</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
**Step 1: app/components/StickyNote.tsx を確認**

以下の 2 点が実装されていることを確認する:
1. L960 付近の blur ハンドラに `.floatBar` を含む `closest()` チェックがある
2. L1073 付近のグローバルポインタハンドラに `.floatBar` を含む `closest?.()` チェックがある

**Step 2: REQUIREMENTS.md を更新**

確認完了後、.planning/REQUIREMENTS.md の以下の行を変更する:

Before:
```
- [ ] **UI-02**: FloatingFormatBar の blur 除外が正しく機能し、フォーマット操作中に編集モードが解除されないこと
```

After:
```
- [x] **UI-02**: FloatingFormatBar の blur 除外が正しく機能し、フォーマット操作中に編集モードが解除されないこと
```

**Step 3: コミット**

REQUIREMENTS.md の両チェックボックス更新をまとめて 1 コミット:
```
docs(requirements): STAB-03 と UI-02 を実装確認済みとしてチェック
```

**注意:** 実装が確認できなかった場合は REQUIREMENTS.md を更新せず、差異を SUMMARY.md に記録する。
  </action>
  <verify>
    <automated>grep -n "UI-02\|STAB-03" D:/Users/uck/Documents/curry-project/ore-no-fusen/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>StickyNote.tsx で .floatBar 除外チェックが確認され、REQUIREMENTS.md の UI-02 が [x] になっている。STAB-03 と UI-02 の両方が [x] の状態でコミット済み。</done>
</task>

</tasks>

<verification>
Plan 02 完了後の確認:

1. `.planning/REQUIREMENTS.md` で `STAB-03` と `UI-02` が両方 `[x]` になっていること
2. `git log --oneline -3` で「docs(requirements): STAB-03 と UI-02...」コミットが確認できること
3. lib.rs と StickyNote.tsx に変更がないこと（`git diff src-tauri/src/lib.rs app/components/StickyNote.tsx` が空）
</verification>

<success_criteria>
- REQUIREMENTS.md の STAB-03 が `[x]` に更新されている
- REQUIREMENTS.md の UI-02 が `[x]` に更新されている
- lib.rs と StickyNote.tsx にコード変更がない
- 1 つのコミット「docs(requirements): STAB-03 と UI-02...」が作成されている
</success_criteria>

<output>
完了後、`.planning/phases/02-bagu-shuse/02-02-SUMMARY.md` を作成する。

内容:
- STAB-03 確認内容（lib.rs の確認箇所と結果）
- UI-02 確認内容（StickyNote.tsx の確認箇所と結果）
- REQUIREMENTS.md の更新内容
- 作成したコミット
</output>
