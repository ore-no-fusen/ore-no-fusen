---
phase: 01-code-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-code-review/01-01-rust-review-notes.md
autonomous: true
requirements:
  - STAB-02

must_haves:
  truths:
    - "Rust本番コードのすべての unwrap() 残存箇所が特定されている"
    - "各 unwrap() がテストコードか本番コードかが分類されている"
    - "Win32 API 呼び出し後の Tauri 状態同期パターンが確認されている"
    - "storage.rs の保存フローでデータ消失につながるパスがないことが確認されている"
  artifacts:
    - path: ".planning/phases/01-code-review/01-01-rust-review-notes.md"
      provides: "Rustレビュー中間ノート（Plan 03 の FINDINGS.md 作成に使用）"
  key_links:
    - from: "src-tauri/src/tray.rs"
      to: "unwrap() 残存"
      via: "Grep + ライン精読"
      pattern: "\\.unwrap\\(\\)"
    - from: "src-tauri/src/logic.rs"
      to: "update_frontmatter_value 本番 unwrap()"
      via: "line 371 精読"
      pattern: "content\\.find.*unwrap"
---

<objective>
Rust バックエンド（lib.rs, storage.rs, logic.rs, tray.rs）を静的レビューし、unwrap() 残存・Win32状態同期・保存フローのリスクを洗い出す。

Purpose: STAB-02（unwrap() 残存なし）の充足状況を確認し、残存箇所を正確に分類する。
Output: `.planning/phases/01-code-review/01-01-rust-review-notes.md`（Plan 03 の FINDINGS.md 作成に使用する中間ノート）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-code-review/01-RESEARCH.md

## このプランの作業方針

コードを変更しない。Grep + Read で精読し、発見事項を中間ノートに記録する。

### 既知リスク（RESEARCH.md より）

- `tray.rs` 本番 unwrap() 2箇所（line 55, 131）: `state.lock().unwrap()`
- `logic.rs` 本番 unwrap() 1箇所（line 371）: `content.find("---").unwrap()`
- `lib.rs` は本番コードの unwrap() は削減済み（テストコードのみ残存の見込み）

### 注意（Pitfall 1）

`#[cfg(test)]` ブロック内の unwrap() は本番リスクではない。Grep 結果を文脈なしで列挙しない。各ラインが本番コードかテストコードかを必ず確認する。
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust unwrap() 残存箇所の精読・分類</name>
  <files>.planning/phases/01-code-review/01-01-rust-review-notes.md</files>
  <action>
以下の手順で Rust コードの unwrap() 残存を調査する。コードは一切変更しない。

1. Grep で各ファイルの unwrap() 行を列挙する:
   - `src-tauri/src/lib.rs`
   - `src-tauri/src/storage.rs`
   - `src-tauri/src/logic.rs`
   - `src-tauri/src/tray.rs`

2. 各 unwrap() について Read ツールでコンテキスト（前後10行）を確認し、以下を判定する:
   - 本番コード: `#[cfg(test)]` 外にある
   - テストコード: `#[cfg(test)]` ブロック内、または `#[test]` 関数内

3. RESEARCH.md の「確認済みリスク」と照合する:
   - tray.rs line 55, 131: state.lock().unwrap() — 本番コードであることを確認
   - logic.rs line 371: content.find("---").unwrap() — 本番コードであることを確認
   - lib.rs: 本番コードの unwrap() が残存していないことを確認

4. storage.rs を Read で全体精読し、保存フロー（write_note, save 系関数）でデータ消失につながるエラーハンドリングの欠落がないか確認する。

5. lib.rs の Win32 API 呼び出し箇所を Grep（`SetWindowPos`, `fusen_show_at_position`, `fusen_set_always_on_top`）で特定し、`win.show()` による Tauri 状態同期が行われているかを確認する（MEMORY.md のピンボタン修正パターン）。

調査結果を `.planning/phases/01-code-review/01-01-rust-review-notes.md` に以下の形式でまとめる:

```
# Rust レビューノート（Plan 01）

## unwrap() 残存一覧

### 本番コード（要対応）
| ファイル | ライン | コード | リスク |
|---------|--------|--------|--------|
| tray.rs | 55 | state.lock().unwrap() | Mutex ポイズンでパニック |
| ...（実際の発見内容） |

### テストコード（対応不要）
| ファイル | ライン | コード |
|---------|--------|--------|
| ...（実際の発見内容） |

## Win32 / Tauri 状態同期確認

[fusen_show_at_position, fusen_set_always_on_top の win.show() 同期状況]

## storage.rs 保存フロー

[write_note / save 系関数のエラーハンドリング確認結果]
```
  </action>
  <verify>
    <automated>test -f .planning/phases/01-code-review/01-01-rust-review-notes.md && echo "OK: ノートファイル存在確認"</automated>
  </verify>
  <done>
    - `.planning/phases/01-code-review/01-01-rust-review-notes.md` が存在する
    - 本番コードの unwrap() 残存箇所がファイル・ライン番号付きで列挙されている
    - テストコードの unwrap() が本番リスクから除外して分類されている
    - Win32 / Tauri 状態同期パターンの確認結果が記録されている
    - storage.rs の保存フローのエラーハンドリング評価が記録されている
  </done>
</task>

</tasks>

<verification>
`.planning/phases/01-code-review/01-01-rust-review-notes.md` を確認し、以下が揃っていること:
- 本番コードの unwrap() 残存一覧（ファイル・ライン番号）
- テストコードとの分類が正確
- tray.rs 2箇所、logic.rs 1箇所が本番コードとして記録されている（もしくは不存在なら「なし」と明記）
</verification>

<success_criteria>
- STAB-02 に関する Rust コードの現状が正確に把握されている
- 本番コードの unwrap() が何箇所あるかが明確になっている
- Phase 2 の修正対象リストとして使える精度の情報が揃っている
</success_criteria>

<output>
完了後、`.planning/phases/01-code-review/01-01-SUMMARY.md` を作成する。
</output>
