---
phase: 04-hono-api-kiban
plan: "04"
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - API-07

must_haves:
  truths:
    - "fusen_send_to_iphone コマンドが lib.rs に存在し、invoke_handler に登録されている"
    - "fusen_check_pro_setup コマンドが lib.rs に存在する"
    - "fusen_oauth_connect コマンドが lib.rs に存在する"
    - "cargo test が通る"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri コマンド3つの追加"
      contains: "fusen_send_to_iphone"
  key_links:
    - from: "lib.rs fusen_send_to_iphone"
      to: "gdrive::upload_json"
      via: "async fn で await 呼び出し"
      pattern: "gdrive::upload_json"
    - from: "lib.rs fusen_send_to_iphone"
      to: "webpush::send_web_push"
      via: "async fn で await 呼び出し"
      pattern: "webpush::send_web_push"
---

<objective>
`src-tauri/src/lib.rs` に `fusen_send_to_iphone`・`fusen_check_pro_setup`・`fusen_oauth_connect` の 3 つの Tauri コマンドを追加し、gdrive.rs と webpush.rs を組み合わせて送信オーケストレーションを完成させる。

Purpose: Phase 4 の最終成果物。このコマンドが動けば Rust バックエンドとして PC 付箋を iPhone に送信する全フローが完結する。
Output: lib.rs に 3 コマンド追加 + invoke_handler 登録
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hono-api-kiban/04-CONTEXT.md
@.planning/phases/04-hono-api-kiban/04-02-SUMMARY.md
@.planning/phases/04-hono-api-kiban/04-03-SUMMARY.md
</context>

<interfaces>
<!-- gdrive.rs 公開 API（Plan 02 作成） -->
```rust
// src-tauri/src/gdrive.rs
pub async fn get_access_token(client: &Client) -> Result<String, String>;
pub async fn upload_json(client: &Client, access_token: &str, filename: &str, data: &serde_json::Value) -> Result<(), String>;
pub async fn poll_push_config(client: &Client, state: &Mutex<AppState>) -> Result<(), String>;
pub async fn oauth_pkce_flow(app: &tauri::AppHandle) -> Result<SavedToken, String>;
pub fn get_token_path() -> PathBuf;
```

<!-- webpush.rs 公開 API（Plan 03 作成） -->
```rust
// src-tauri/src/webpush.rs
pub async fn send_web_push(client: &Client, pro_config: &ProConfig, plaintext_json: &str) -> Result<(), String>;
pub fn load_or_generate_vapid_keys() -> Result<VapidKeys, String>;
```

<!-- fusen_note.json 形式（CONTEXT.md より） -->
```json
{
  "title": "今日のタスク",
  "body": "# 今日のタスク\n- [ ] 資料作成\n",
  "tags": ["仕事"],
  "sent_at": "2026-03-03T06:05:00Z"
}
```

<!-- existing storage::read_note 相当 -->
lib.rs には既存のノート読み込みロジックが存在する。
storage モジュールで .md ファイルを読む関数が利用可能。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: lib.rs に 3 つの Tauri コマンドを追加する</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
lib.rs に以下の 3 つの async Tauri コマンドを追加する。既存コードは変更しない。

**1. fusen_oauth_connect（OAuth 初回認証）**
```rust
#[tauri::command]
async fn fusen_oauth_connect(app: tauri::AppHandle) -> Result<(), String> {
    gdrive::oauth_pkce_flow(&app).await?;
    Ok(())
}
```

**2. fusen_check_pro_setup（ProConfig キャッシュ確認）**
```rust
#[tauri::command]
async fn fusen_check_pro_setup(
    state: tauri::State<'_, Mutex<AppState>>,
) -> Result<bool, String> {
    let client = reqwest::Client::new();
    // Google Drive から push_config を取得して AppState にキャッシュ
    match gdrive::poll_push_config(&client, &state).await {
        Ok(_) => {
            let guard = state.lock().unwrap_or_else(|p| p.into_inner());
            Ok(guard.pro_config.is_some())
        }
        Err(_) => Ok(false),  // 設定未完了の場合は false を返す（エラーではない）
    }
}
```

**3. fusen_send_to_iphone（送信オーケストレーション）**
```rust
#[tauri::command]
async fn fusen_send_to_iphone(
    state: tauri::State<'_, Mutex<AppState>>,
    path: String,
) -> Result<(), String> {
    let client = reqwest::Client::new();

    // 1. ノートの内容を読む（既存 storage モジュールを使用）
    let note = {
        let guard = state.lock().unwrap_or_else(|p| p.into_inner());
        let _folder = guard.folder_path.clone()
            .ok_or_else(|| "Folder not set".to_string())?;
        // storage::read_note を呼ぶかファイルを直接読む
        drop(guard);
        std::fs::read_to_string(&path).map_err(|e| e.to_string())?
    };

    // 2. note JSON を生成
    let sent_at = chrono::Utc::now().to_rfc3339();
    let title = path.split(['/', '\\']).last().unwrap_or("note")
        .trim_end_matches(".md").to_string();
    let note_json = serde_json::json!({
        "title": title,
        "body": note,
        "tags": [],
        "sent_at": sent_at
    });

    // 3. Google Drive に fusen_note.json をアップロード
    let access_token = gdrive::get_access_token(&client).await?;
    gdrive::upload_json(&client, &access_token, "fusen_note.json", &note_json).await?;

    // 4. キャッシュ済み pro_config を取得（なければ Drive から再取得）
    let pro_config = {
        let guard = state.lock().unwrap_or_else(|p| p.into_inner());
        guard.pro_config.clone()
    };
    let pro_config = match pro_config {
        Some(c) => c,
        None => {
            // キャッシュなし → Drive から取得して AppState に保存
            gdrive::poll_push_config(&client, &state).await?;
            let guard = state.lock().unwrap_or_else(|p| p.into_inner());
            guard.pro_config.clone()
                .ok_or_else(|| "push_config not found in Google Drive. Please set up the iPhone app first.".to_string())?
        }
    };

    // 5. APNs に Push 通知送信
    let plaintext = serde_json::to_string(&note_json).map_err(|e| e.to_string())?;
    webpush::send_web_push(&client, &pro_config, &plaintext).await?;

    Ok(())
}
```

**invoke_handler への登録:**
lib.rs の `.invoke_handler(tauri::generate_handler![...])` の既存リストに以下を追加:
```
fusen_oauth_connect,
fusen_check_pro_setup,
fusen_send_to_iphone,
```

**use 宣言の追加（lib.rs 先頭の use ブロックに）:**
```rust
use reqwest;  // 既存のため不要な場合はスキップ
```
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen/src-tauri && cargo check 2>&1 | tail -10</automated>
  </verify>
  <done>
- fusen_send_to_iphone / fusen_check_pro_setup / fusen_oauth_connect が lib.rs に存在する
- 3 コマンドが invoke_handler に登録されている
- cargo check が error なし
  </done>
</task>

<task type="auto">
  <name>Task 2: cargo test でフルスイートを実行する</name>
  <files></files>
  <action>
以下のコマンドで全テストを実行し、gdrive / webpush の全テストが PASS することを確認する。
新規テストの追加は不要。既存テストが壊れていないことを確認するだけ。

失敗した場合は Plan 02 / Plan 03 の実装を確認して修正する。
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen/src-tauri && cargo test 2>&1 | tail -20</automated>
  </verify>
  <done>cargo test が全テスト PASS（gdrive 2件 + webpush 4件 + 既存テスト）。</done>
</task>

</tasks>

<verification>
cargo check が error なし。
cargo test が全件 PASS。
fusen_send_to_iphone が invoke_handler に登録されている（grep で確認可能）。
</verification>

<success_criteria>
- cargo check エラーなし
- cargo test 全件 PASSED
- fusen_send_to_iphone / fusen_check_pro_setup / fusen_oauth_connect の 3 コマンドが登録されている
- CONTEXT.md の Push 通知フロー（Drive upload → push_config 取得 → APNs POST）が実装されている
</success_criteria>

<output>
完了後、`.planning/phases/04-hono-api-kiban/04-04-SUMMARY.md` を作成する。
</output>
