---
phase: 04-hono-api-kiban
plan: "05"
type: execute
wave: 4
depends_on:
  - "04-04"
files_modified: []
autonomous: false
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07

must_haves:
  truths:
    - "cargo test が全件 PASS している"
    - "cargo build が通る（Tauri ビルドを含む）"
    - "fusen_oauth_connect でブラウザ認証が通り、refresh_token が保存される"
    - "fusen_check_pro_setup が true を返す（drive に push_config がある場合）"
  artifacts:
    - path: "src-tauri/src/gdrive.rs"
      provides: "Google OAuth2 PKCE + Drive R/W"
    - path: "src-tauri/src/webpush.rs"
      provides: "VAPID + AES-128-GCM + APNs POST"
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri コマンド登録"
      contains: "fusen_send_to_iphone"
  key_links:
    - from: "fusen_send_to_iphone"
      to: "gdrive::upload_json + webpush::send_web_push"
      via: "async コマンドの await チェーン"
      pattern: "fusen_send_to_iphone"
---

<objective>
Phase 4 の全自動テストを実行し、Tauri ビルドが通ることを確認する。実機 iPhone への Push 通知フローを手動で検証する（実機検証）。

Purpose: Phase 4 の成果物が揃ったことを確認して Phase 5（iPhone PWA）への移行を承認する。
Output: Phase 4 完了確認 + 手動検証レポート
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hono-api-kiban/04-CONTEXT.md
@.planning/phases/04-hono-api-kiban/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 自動テストとビルドを実行する</name>
  <files></files>
  <action>
以下を順番に実行する:

1. cargo test（全テストスイート）
2. cargo check（型チェック）
3. npm test（Next.js 側の vitest が壊れていないことを確認）

失敗した場合は該当ファイルを修正してから再実行する。
  </action>
  <verify>
    <automated>cd /d/Users/uck/Documents/curry-project/ore-no-fusen/src-tauri && cargo test 2>&1 | tail -20 && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>cargo test 全件 PASS。cargo check エラーなし。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4 の全成果物:
- src-tauri/src/gdrive.rs（OAuth PKCE + Drive R/W + poll_push_config）
- src-tauri/src/webpush.rs（VAPID + AES-128-GCM + APNs POST）
- src-tauri/src/lib.rs に fusen_send_to_iphone / fusen_check_pro_setup / fusen_oauth_connect 追加
- Cargo.toml に oauth2, tauri-plugin-oauth, p256, jwt-simple, aes-gcm, hkdf 追加

自動テスト結果:
- cargo test gdrive: 2 PASSED
- cargo test webpush: 4 PASSED
- cargo check: エラーなし
  </what-built>
  <how-to-verify>
**ステップ 1: ビルド確認（必須）**
```bash
cd src-tauri && cargo build 2>&1 | tail -20
```
"Finished" メッセージが表示されれば OK。

**ステップ 2: OAuth 認証確認（Google アカウントが必要）**

まず Google Cloud Console で OAuth 2.0 クライアント ID を作成し、環境変数をセットする:
- `GDRIVE_CLIENT_ID` = Google Cloud の OAuth クライアント ID
- `GDRIVE_CLIENT_SECRET` = OAuth クライアントシークレット

次に Tauri アプリを起動して開発コンソールから:
```javascript
await window.__TAURI__.tauri.invoke('fusen_oauth_connect')
```
ブラウザが開き Google ログイン画面が表示されれば OK。

**ステップ 3: push_config 確認（Google Drive に fusen_push_config.json がある場合のみ）**
```javascript
const result = await window.__TAURI__.tauri.invoke('fusen_check_pro_setup')
console.log(result)  // true なら ProConfig がキャッシュ済み
```

**ステップ 4: 実機送信（iPhone + Google Drive に push_config がある場合のみ）**
```javascript
await window.__TAURI__.tauri.invoke('fusen_send_to_iphone', { path: '/path/to/note.md' })
```
iPhone のロック画面に通知が届けば Phase 4 完了。
iPhone PWA はまだないため、このステップは Phase 5 完了後でも可。

**最低限の合格ライン（Phase 5 移行条件）:**
- cargo build が通る
- gdrive / webpush の unit test が全件 PASS している
- OAuth 認証フロー（fusen_oauth_connect）が起動する（ブラウザが開く）
  </how-to-verify>
  <resume-signal>
ビルドが通り unit test が全件 PASS していれば "approved" と入力してください。
実機テストまで完了した場合はその結果も記載してください。
問題がある場合は具体的な症状を記載してください。
  </resume-signal>
</task>

</tasks>

<verification>
cargo build 成功。
cargo test 全件 PASS。
fusen_oauth_connect がブラウザを開く（手動確認）。
</verification>

<success_criteria>
- cargo build 成功（Tauri バイナリが生成される）
- cargo test gdrive + webpush が全件 PASS
- ROADMAP.md の Phase 4 success criteria 1〜4 が達成されている
  - 1. fusen_check_pro_setup が Google Drive から fusen_push_config.json を読んで AppState にキャッシュできる
  - 2. fusen_send_to_iphone が note JSON を Google Drive にアップロードできる
  - 3. fusen_send_to_iphone が APNs に Push を送信できる（push_config が有効な場合）
  - 4. Google OAuth PKCE フローで取得したトークンがローカルに保存・再利用される
- Phase 5 移行の承認
</success_criteria>

<output>
完了後、`.planning/phases/04-hono-api-kiban/04-05-SUMMARY.md` を作成する。
Phase 4 全体の完了を STATE.md に記録する（completed_phases: 1 → 2, status: ready_to_plan for Phase 5）。
</output>
