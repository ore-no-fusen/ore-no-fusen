---
phase: 05-iphone-pwa-rust-soshin
plan: "02"
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - app/viewer/page.tsx
autonomous: true
requirements:
  - PWA-03
  - SEND-01

must_haves:
  truths:
    - "iPhone Safari（非standalone）でアクセスするとホーム画面追加バナーが表示される"
    - "standalone モードでは「Googleでログイン」→「通知を許可する」の2ステップが表示される"
    - "Googleログイン後に Push 購読を完了すると fusen_push_config.json が Drive に保存される"
    - "通知タップ後に viewer を開くと fusen_note.json が Drive から読み込まれて全文表示される"
    - "新規セッションで ?note= URL を開いても localStorage からの accessToken 復元または OAuth 再リダイレクトで fusen_note.json を取得できる"
  artifacts:
    - path: "app/viewer/page.tsx"
      provides: "iOS セットアップUI + 全文表示（Client Component）"
      exports: ["default (ViewerPage)"]
      min_lines: 200
  key_links:
    - from: "app/viewer/page.tsx"
      to: "https://accounts.google.com/o/oauth2/v2/auth"
      via: "startOAuth() → window.location.href リダイレクト"
      pattern: "accounts.google.com"
    - from: "app/viewer/page.tsx"
      to: "https://www.googleapis.com/drive/v3/files"
      via: "uploadToDrive() / downloadFromDrive() fetch 呼び出し"
      pattern: "googleapis.com/drive"
    - from: "app/viewer/page.tsx"
      to: "pushManager.subscribe()"
      via: "subscribePush() → navigator.serviceWorker.ready"
      pattern: "pushManager.subscribe"
    - from: "app/viewer/page.tsx"
      to: "localStorage"
      via: "accessToken を localStorage に保存し、新規セッション起動時に復元"
      pattern: "localStorage.*accessToken"
---

<objective>
iPhone PWA の viewer ページ（セットアップUI + 全文表示）を実装する。

Purpose: iPhoneユーザーが Google OAuth PKCE → Push購読 → Drive保存の一連フローを1ページで完結できるようにする。accessToken は localStorage に永続化し、通知タップによる新規セッション起動でも再認証なしで fusen_note.json を取得できるようにする。
Output: app/viewer/page.tsx（Client Component）— ホーム画面追加バナー、2ステップセットアップ、通知タップ時の全文表示。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-iphone-pwa-rust-soshin/05-CONTEXT.md
@.planning/phases/05-iphone-pwa-rust-soshin/05-RESEARCH.md

<interfaces>
<!-- executor が必要とする仕様・パターン（探索不要） -->

<!-- 環境変数（フロントエンド側） -->
NEXT_PUBLIC_GDRIVE_CLIENT_ID  — Google OAuth クライアントID
NEXT_PUBLIC_VAPID_PUBLIC_KEY  — VAPID 公開鍵（Base64 URL 形式）

<!-- fusen_push_config.json の形式（Phase 4 で確定済み） -->
{
  "endpoint": "https://api.push.apple.com/3/device/...",
  "keys": {
    "p256dh": "...",
    "auth": "..."
  }
}

<!-- fusen_note.json の形式 -->
{
  "title": "付箋タイトル",
  "body": "付箋の全文（Markdown）"
}

<!-- Drive REST API v3 アップロードパターン（RESEARCH.md より） -->
// ファイル検索 → 存在すれば PATCH、なければ POST multipart upload
// Authorization: Bearer {accessToken}
// https://www.googleapis.com/drive/v3/files?q=name='{fileName}'+and+trashed=false
// https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart

<!-- PKCE パターン（RESEARCH.md より） -->
async function generatePKCE() {
  const verifier = crypto.randomUUID().replace(/-/g,'') + crypto.randomUUID().replace(/-/g,'');
  const digest = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(verifier));
  const challenge = btoa(String.fromCharCode(...new Uint8Array(digest)))
    .replace(/\+/g,'-').replace(/\//g,'_').replace(/=/g,'');
  return { verifier, challenge };
}

<!-- urlBase64ToUint8Array ヘルパー（RESEARCH.md より） -->
function urlBase64ToUint8Array(base64String: string): Uint8Array {
  const padding = '='.repeat((4 - base64String.length % 4) % 4);
  const base64 = (base64String + padding).replace(/-/g,'+').replace(/_/g,'/');
  const rawData = window.atob(base64);
  return Uint8Array.from([...rawData].map(c => c.charCodeAt(0)));
}

<!-- accessToken の永続化方針 -->
// セッションをまたぐ場合（通知タップによる新規起動）に備えて localStorage に保存する
// OAuth コールバック時: localStorage.setItem('viewer_access_token', accessToken)
// セットアップ/表示時の accessToken 調達順序:
//   1. sessionStorage から読む（同セッション内）
//   2. なければ localStorage から読む（セッションをまたいだ復元）
//   3. どちらもなければ: セットアップ中のステップ表示 or ?note= の場合は OAuth 再リダイレクト

<!-- iOS Push の重要制約 -->
- pushManager.subscribe() は standalone モード（ホーム画面追加済み）でのみ動作
- Notification.requestPermission() はクリックハンドラ内からのみ呼ぶ
- window.matchMedia('(display-mode: standalone)').matches で判定
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: viewer/page.tsx を完全実装してビルドを確認する</name>
  <files>app/viewer/page.tsx</files>
  <action>
    `app/viewer/` ディレクトリを作成し、`page.tsx` を Client Component として完全実装する。
    UI・状態管理・OAuth/Drive/Push の全 wiring を一つのタスクで実装する。

    **状態変数:**
    ```typescript
    const [isStandalone, setIsStandalone] = useState(false);
    const [step, setStep] = useState<'banner' | 'login' | 'push' | 'ready' | 'note'>('banner');
    const [accessToken, setAccessToken] = useState<string | null>(null);
    const [noteData, setNoteData] = useState<{ title: string; body: string } | null>(null);
    const [errorMessage, setErrorMessage] = useState<string | null>(null);
    const [isLoading, setIsLoading] = useState(false);
    ```

    **ヘルパー関数（コンポーネント外に定義）:**

    generatePKCE:
    ```typescript
    async function generatePKCE() {
      const verifier = crypto.randomUUID().replace(/-/g,'') + crypto.randomUUID().replace(/-/g,'');
      const digest = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(verifier));
      const challenge = btoa(String.fromCharCode(...new Uint8Array(digest)))
        .replace(/\+/g,'-').replace(/\//g,'_').replace(/=/g,'');
      return { verifier, challenge };
    }
    ```

    startOAuth:
    ```typescript
    function startOAuth(challenge: string) {
      const params = new URLSearchParams({
        client_id: process.env.NEXT_PUBLIC_GDRIVE_CLIENT_ID!,
        redirect_uri: window.location.origin + '/viewer',
        response_type: 'code',
        scope: 'https://www.googleapis.com/auth/drive.file',
        code_challenge: challenge,
        code_challenge_method: 'S256',
        access_type: 'offline',
      });
      window.location.href = 'https://accounts.google.com/o/oauth2/v2/auth?' + params.toString();
    }
    ```

    urlBase64ToUint8Array:（interfaces ブロックのコードをそのまま使用）

    uploadToDrive:
    ```typescript
    async function uploadToDrive(accessToken: string, fileName: string, data: object) {
      const searchRes = await fetch(
        `https://www.googleapis.com/drive/v3/files?q=name='${fileName}'+and+trashed=false`,
        { headers: { Authorization: `Bearer ${accessToken}` } }
      );
      const searchData = await searchRes.json();
      const fileId = searchData.files?.[0]?.id;
      const body = JSON.stringify(data);
      const meta = JSON.stringify({ name: fileName, mimeType: 'application/json' });
      const form = new FormData();
      form.append('metadata', new Blob([meta], { type: 'application/json' }));
      form.append('file', new Blob([body], { type: 'application/json' }));
      if (fileId) {
        await fetch(`https://www.googleapis.com/upload/drive/v3/files/${fileId}?uploadType=multipart`, {
          method: 'PATCH', headers: { Authorization: `Bearer ${accessToken}` }, body: form,
        });
      } else {
        await fetch('https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart', {
          method: 'POST', headers: { Authorization: `Bearer ${accessToken}` }, body: form,
        });
      }
    }
    ```

    downloadFromDrive:
    ```typescript
    async function downloadFromDrive(accessToken: string, fileName: string) {
      const searchRes = await fetch(
        `https://www.googleapis.com/drive/v3/files?q=name='${fileName}'+and+trashed=false`,
        { headers: { Authorization: `Bearer ${accessToken}` } }
      );
      const searchData = await searchRes.json();
      const fileId = searchData.files?.[0]?.id;
      if (!fileId) throw new Error(`${fileName} not found in Drive`);
      const res = await fetch(`https://www.googleapis.com/drive/v3/files/${fileId}?alt=media`, {
        headers: { Authorization: `Bearer ${accessToken}` },
      });
      return res.json();
    }
    ```

    **useEffect（完全実装）:**
    ```typescript
    useEffect(() => {
      const standalone = window.matchMedia('(display-mode: standalone)').matches;
      setIsStandalone(standalone);
      if (!standalone) return; // バナー表示のみ

      const params = new URLSearchParams(window.location.search);

      // accessToken 調達: sessionStorage → localStorage → 再認証
      let token = sessionStorage.getItem('viewer_access_token')
               ?? localStorage.getItem('viewer_access_token');

      // OAuth コールバック（?code= あり）
      if (params.get('code')) {
        const code = params.get('code')!;
        const verifier = sessionStorage.getItem('pkce_verifier');
        if (!verifier) { setErrorMessage('セッションが切れました。再度ログインしてください。'); setStep('login'); return; }
        setIsLoading(true);
        fetch('https://oauth2.googleapis.com/token', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: new URLSearchParams({
            code,
            client_id: process.env.NEXT_PUBLIC_GDRIVE_CLIENT_ID!,
            redirect_uri: window.location.origin + '/viewer',
            code_verifier: verifier,
            grant_type: 'authorization_code',
          }).toString(),
        })
        .then(r => r.json())
        .then(data => {
          const t = data.access_token;
          if (!t) throw new Error('access_token missing');
          sessionStorage.setItem('viewer_access_token', t);
          localStorage.setItem('viewer_access_token', t);
          setAccessToken(t);
          token = t;
          window.history.replaceState({}, '', '/viewer');
          setStep('push');
        })
        .catch(err => { setErrorMessage('ログインに失敗しました: ' + err.message); setStep('login'); })
        .finally(() => setIsLoading(false));
        return;
      }

      // 通知タップ（?note= あり）
      if (params.get('note')) {
        if (!token) {
          generatePKCE().then(({ verifier, challenge }) => {
            sessionStorage.setItem('pkce_verifier', verifier);
            sessionStorage.setItem('pending_note', params.get('note')!);
            startOAuth(challenge);
          });
          return;
        }
        setAccessToken(token);
        setIsLoading(true);
        downloadFromDrive(token, 'fusen_note.json')
          .then(data => { setNoteData(data); setStep('note'); })
          .catch(err => setErrorMessage('メモの取得に失敗しました: ' + err.message))
          .finally(() => setIsLoading(false));
        return;
      }

      // OAuth 再リダイレクト後の pending_note 処理
      const pendingNote = sessionStorage.getItem('pending_note');
      if (pendingNote && token) {
        sessionStorage.removeItem('pending_note');
        setAccessToken(token);
        setIsLoading(true);
        downloadFromDrive(token, 'fusen_note.json')
          .then(data => { setNoteData(data); setStep('note'); })
          .catch(err => setErrorMessage('メモの取得に失敗しました: ' + err.message))
          .finally(() => setIsLoading(false));
        return;
      }

      // 通常フロー（セットアップ）
      if (token) { setAccessToken(token); setStep('push'); }
      else { setStep('login'); }
    }, []);
    ```

    **レンダリング分岐:**

    1. `!isStandalone` → バナー表示:
       - 黄色背景（bg-yellow-50 border border-yellow-200 rounded-lg p-4）
       - ① Safari 共有アイコン（↑）をタップ → ② 「ホーム画面に追加」を選択
       - "インストール後にセットアップを完了してください" の一文

    2. `step === 'login'` → Googleでログインボタン:
       - bg-blue-600 text-white rounded-lg px-6 py-3
       - onClick で generatePKCE → sessionStorage に verifier 保存 → startOAuth(challenge)

    3. `step === 'push'` → 通知を許可するボタン:
       - 同スタイル
       - onClick:
         ```typescript
         async () => {
           try {
             const perm = await Notification.requestPermission();
             if (perm !== 'granted') { setErrorMessage('通知を許可してください'); return; }
             const sw = await navigator.serviceWorker.ready;
             const sub = await sw.pushManager.subscribe({
               userVisibleOnly: true,
               applicationServerKey: urlBase64ToUint8Array(process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY!),
             });
             const { endpoint, keys } = sub.toJSON() as any;
             await uploadToDrive(accessToken!, 'fusen_push_config.json', { endpoint, keys });
             setStep('ready');
           } catch (err: any) {
             setErrorMessage('通知設定に失敗しました: ' + err.message);
           }
         }
         ```

    4. `step === 'ready'` → 待機画面:
       - "PCから付箋が送られたらここに表示されます"

    5. `step === 'note'` → 全文表示:
       - `<h1 className="text-xl font-bold">{noteData?.title}</h1>`
       - `<pre className="whitespace-pre-wrap mt-4">{noteData?.body}</pre>`
       - max-w-prose mx-auto px-4 でレイアウト

    6. `errorMessage` → `<p className="text-red-600">{errorMessage}</p>` を各分岐の末尾に表示

    最後に `npm run build` を実行して TypeScript エラーがないことを確認する。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|Error|✓)" | tail -10</automated>
  </verify>
  <done>
    - app/viewer/page.tsx が存在し TypeScript エラーなし
    - npm run build が成功する
    - 非standalone バナー・ステップUI（login/push/ready）・全文表示（note）の全状態が実装されている
    - OAuth PKCE フロー（generatePKCE + startOAuth + token exchange）が実装されている
    - Push購読 + Drive 保存（fusen_push_config.json）が実装されている
    - 通知タップ後の fusen_note.json 全文表示が実装されている
    - accessToken を localStorage に保存・復元するロジックが実装されている
    - ?note= URL で accessToken がない場合に OAuth 再リダイレクトするロジックが実装されている
  </done>
</task>

</tasks>

<verification>
npm run build が成功することを確認する。
app/viewer/page.tsx が存在し、以下のパターンが含まれていることを確認する:
- display-mode: standalone
- generatePKCE
- pushManager.subscribe
- googleapis.com/drive
- uploadToDrive
- localStorage.*viewer_access_token または localStorage.setItem('viewer_access_token'
</verification>

<success_criteria>
- app/viewer/page.tsx 実装完了（TypeScript エラーなし）
- 非standalone → バナー表示（ホーム画面追加手順）
- standalone → ステップ1（Googleでログイン）→ ステップ2（通知を許可）→ 待機画面
- 通知タップ → ?note= パラメータ検出 → accessToken を localStorage/sessionStorage から復元 → Drive から全文取得 → 表示
- accessToken がない場合は OAuth 再リダイレクトして note 表示を完了させる
- npm run build 成功
</success_criteria>

<output>
完了後、`.planning/phases/05-iphone-pwa-rust-soshin/05-02-SUMMARY.md` を作成すること。
</output>
