---
context: default
phase: image-attachments
task: 3
total_tasks: 6
status: in_progress
last_updated: 2026-09-12T19:48:33.9818049+09:00
---

# Vercelを通さない掲示板画像添付 — 中断位置

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 共有node_modulesでのNext build | 隔離worktreeから本体側node_modulesを参照するとWindows絶対パス解決でbuildが失敗した | advisory | コード不具合と扱わず、専用依存環境または本体へ統合後にbuildする |
| worktree所有者判定 | sandbox利用者とworktree所有者SIDが異なりgitがdubious ownershipで停止した | advisory | このworktreeのgit確認では`-c safe.directory=C:/tmp/ore-no-fusen-image-probe`を付ける |

<current_state>
`codex/image-attachments-probe`で実装途中。ユーザーの指示により、画像対応は本体リリースの次ではなく「次の次のバージョン」まで保留する。Vercelへ画像バイトを送らない設計へ切り替え済みだが、全経路の完成・検証前。
</current_state>

<completed_work>

- `POST /api/feedback/conversation/session`：会話資格情報を照合しAppwrite用短期JWTを返す骨格
- PC：PNG/JPEG/WebPを最大3枚、2048px以内・WebP品質88へ端末内圧縮しAppwriteへ直接送信する骨格
- UI：ファイル選択、Ctrl+V貼り付け、ドラッグ&ドロップ、プレビュー、取消、Ctrl+Enter送信の骨格
- Appwrite Function：画像取得、実体検査、縮小・圧縮、非公開保存のソース骨格
- 旧Vercelバイナリアップロード／ダウンロードrouteを専用ブランチ側から削除
</completed_work>

<remaining_work>

- `messages/route.ts`のFunction正規化結果とcleanupを完成させ、構文・型を確認
- Discord返信画像をVercelでfetchせず、URLをFunctionへJSON送信する実装へ置換
- 開発者がユーザー画像をDiscordで確認する短期file token経路のテスト
- `docs-v2/007_COMMUNICATION.md`を直送方式、トークン線・ファイル線、開発者線、4つの公開APIに全面更新
- 旧chunk用フィールド・定数・テストをすべて除去
- 対象Vitest、全Vitest、TypeScript、lint、docs build、E2E、Preview実機確認
</remaining_work>

<decisions_made>

- Vercelには画像を置かず、画像バイトを一度も通さない。
- Vercelの公開APIは、既存`messages`、`poll`、`ack`、`delete`を維持し、画像時だけ`session`を追加する。文字だけの送信は従来互換。
- 画像本体はPCとAppwrite Storageが直接送受信する。
- Discord画像はURLだけVercelからAppwrite Functionへ渡し、Functionが取得・検査・圧縮・保存する。
- 最大3枚、1枚5MiB、PNG/JPEG/WebP、長辺2048px、WebP品質88、30日保存。
</decisions_made>

<blockers>

- Appwrite Console上のFunction、bucket、権限、環境変数は未設定。コード完成後に検証環境で設定が必要。
- 本体リリースが先。画像対応を`develop`へ統合しない。
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — 機械可読の正確な中断状態
2. `docs-v2/007_COMMUNICATION.md` — 更新対象の正本
3. `app/api/feedback/conversation/messages/route.ts` — 利用者投稿の途中実装
4. `app/api/feedback/discord/ingest/images.ts` — Vercel fetchが残る置換対象
5. `appwrite/functions/feedback-images/src/main.js` — Appwrite側処理の骨格

## Infrastructure State

- 作業場所: `C:/tmp/ore-no-fusen-image-probe`
- ブランチ: `codex/image-attachments-probe`
- 外部環境変更: なし
- 本番・Previewデータ変更: なし
- 開発サーバー: 起動していない

<context>
本体`develop`には分離前の画像関連差分が残っているため、ユーザー承認のうえ画像部分だけ除去する。テーマ・音・バージョン取得など今回リリース対象の差分は維持する。画像ブランチは未テストの途中状態なので統合・配布しない。
</context>

<next_action>
再開時は`git -c safe.directory=C:/tmp/ore-no-fusen-image-probe status --short`で状態を確認し、`messages/route.ts`の対象テストから修正する。
</next_action>
