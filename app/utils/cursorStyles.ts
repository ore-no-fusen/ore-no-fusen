/** つかむ手の人差し指先端（SVG 内の x=11, y=1）を実際のポインター位置に合わせる。 */
export const NOTE_DRAG_CURSOR = "url('/cursors/note-drag.svg?v=2') 11 1, grab";

/** 小さな指さし手の指先（SVG 内の x=6, y=0）を実際のポインター位置に合わせる。 */
export const NOTE_POINT_CURSOR = "url('/cursors/note-point.svg?v=1') 6 0, pointer";
